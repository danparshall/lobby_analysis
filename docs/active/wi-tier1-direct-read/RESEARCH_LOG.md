# Research Log: wi-tier1-direct-read

Created: 2026-05-30
Purpose: Extend the Tier-1 direct-read legal-axis extraction harness from the OH 2025 pilot to Wisconsin 2025 — but first generalize the script so a new state cannot collide with or resume-skip into the OH results (state-key the results/checkpoint paths).

Plan: `plans/20260530_wi_2025_tier1_direct_read.md`

---

## Session history (newest first)

### 2026-06-03 — Prior-art adjudication of the 18 WI inter-model disagreement cells; Item 3 closed
Convo: [`convos/20260603_statute_disagreement_prior_art_review.md`](convos/20260603_statute_disagreement_prior_art_review.md)
- **Item 3 (Citations API) CLOSED.** Walked through the 18 inter-model disagreement cells before deciding the Citations API integration. Wrote enumeration + provenance lookup scripts ([`results/disagreement_audit/`](results/disagreement_audit/)) and re-derived the 18 from the 36 result JSONs. Three structural patterns: A (14 cells in `lobbyist_spending_report`, Claude TRUE × 3 / GPT FALSE × 3), B (3 lobbyist-side registration rows, Claude scored from principal-side rule / GPT abstained), C (1 cell, `lobbying_violation_penalties_imposed_in_practice`, mis-axed). Adjudicated all 18 against source rubrics per Dan's redirect ("the compendium unions prior art — what did the authors mean?").
- **Prior-art verdict.** Patterns A + B (17 of 18) favor GPT — source rubrics (CPI 2015 IND_201, PRI 2010 §III.E2, Newmark 2017) explicitly ask about the lobbyist as filer; Claude was over-projecting §13.68(4)'s "info flows through the lobbyist" onto rows whose source-author intent is about who the filer is. Structural corroboration: CPI maintains `lobbyist_spending_report_required` and `principal_spending_report_required` as separate rows (IND_201 vs IND_203), which would be redundant if "lobbyist spending report" meant "any report containing lobbyist info." Pattern C is neither model wrong — row is mis-axed (CPI assigns IND_209 to practical-only; v2.1 TSV registers `legal+practical`). Full writeup with verbatim source quotes at [`results/20260603_prior_art_adjudication_of_18_disagreements.md`](results/20260603_prior_art_adjudication_of_18_disagreements.md).
- **Load-bearing structural finding.** While scoping the "update the prompt-rubric text" task, discovered `render_legal_roster` (`scripts/tier_1_direct_read_legal_axis.py` lines 205–223) sends only `row_id` + `axis` + `expected_cell_class` to the model — **no question text at all**. Verbatim source-rubric questions exist in `docs/historical/compendium-source-extracts/results/projections/*.md` but never reach the model. The Pattern A/B disagreements are partly an artifact of prompt under-specification, not pure model-capability signal.
- **Three v2.2 ledger entries added** to [`results/v2_2_schema_inputs.md`](results/v2_2_schema_inputs.md): Entry 2 (Pattern C axis split into two siblings); Entry 3 (`prompt_text` column gap — v2.2-prerequisite, needed before MI dispatch); Entry 4 (source-quote provenance gap — two known row-ID renames hide audit trail).
- **Next action (deferred to fresh agent):** narrow `prompt_text` fix — populate for the 17 disagreement rows from source-rubric quotes; update `render_legal_roster`; re-dispatch the 3 affected chunks on WI (~$1, ~10 min). Validation criterion: Claude collapses onto GPT's reading. Needs Dan's sign-off on the spend. If validates, schedule wide pass (181 rows). If not, prompt-text alone isn't enough.
- **No API spend** this session (pure code reading + doc work + design synthesis).
- **Carry-overs that stayed closed this session:** Item 4 (MI standing instruction — but now with the prompt_text-before-MI sequencing note), Item 6's open OH re-dispatch question (now contingent on prompt_text fix landing first), Item 7 / GH #31 (parallelization, independent).

### 2026-06-01 — Phase 3 followups executed (items 1, 5, 6 + bonus item 2 capture)
Convo: [`convos/20260601_phase3_followups_execution.md`](convos/20260601_phase3_followups_execution.md)
- **Item 1 (Fix A int→Decimal regression on dict-shape value path) DONE.** Hypothesis (b) confirmed: original Fix A (commit `0403218`) was scoped to scalar `DecimalCell.value`; the dict-shape branch of `_instantiate_cell` (lines 542–561) bypasses any per-field coercion, so a bare int `magnitude` for `TimeThresholdCell` reaches Pydantic strict mode unchanged. Added `_coerce_dict_shape_inner_decimals(cls, kwargs)` helper that walks `cls.model_fields`, finds `Decimal`-typed fields, and coerces int/float→Decimal (excluding bool). 3 new tests (RED→GREEN); full suite **1553 pass / 3 skip / 3 xfail** (baseline preserved). Ruff lint clean on touched files (6 pre-existing E402s verified by stash-and-recheck, all unrelated).
- **Item 5 (principal-filings aggregation source) DONE.** Verdict: not parser scrape-loss, not portal-publication absence — it's a deliberate Tier-2/Tier-3 scope decision in archived `wi-disclosure-explore`. WI portal exposes the per-principal summary page (Total Lobbying Effort table has exactly 3 labeled rows: expenditures + 2 hours columns); the parser reads all 3. Itemized data lives on Tier-3 per-principal SLAE pages + 15-day notification reports — tracked at [GH #28](https://github.com/danparshall/lobby_analysis/issues/28). Open: no one has yet inspected a single Tier-3 page; a 1-page reconnaissance is the natural Phase-1 of #28. Writeup: [`results/20260601_principal_filings_aggregation_source.md`](results/20260601_principal_filings_aggregation_source.md); back-link banner added to [`results/20260601_wi_statute_vs_portal_spending.md`](results/20260601_wi_statute_vs_portal_spending.md).
- **Item 6 (σ_noise WI-vs-OH composition study) DONE.** Decomposed `pct_stable` into the 4 stability-class components per (state, model). Three findings: (a) **The Phase 2 convo's cross-state Δ uses a stale OH baseline** — cites pre-Step-D GPT OH = 73.8 %; the post-Step-D mixed OH is 79.76 %; no clean post-fix OH baseline exists. (b) **Within-state cross-model differs sharply**: Claude WI has 2 scor_un + 9 val_un, GPT WI has 7 scor_un + 5 val_un — and GPT's WI scor_un concentrates in `lobbying_definitions` (4 of 7), exactly where the §13.62(11) qualitative trigger sits. (c) **Claude is state-invariant at headline (85.71 % both states) but composition shifted** from scor_un-dominated (OH) toward val_un-dominated (WI). Writeup + script + saved output: [`results/20260601_sigma_noise_composition_oh_wi.md`](results/20260601_sigma_noise_composition_oh_wi.md), [`results/sigma_noise_composition/`](results/sigma_noise_composition/). Per Dan, **the Phase 2 convo is left as-is** — this session's docs carry the baseline-issue context.
- **Bonus item 2 capture (v2.2 `TimeThresholdCell.unit` enum gap) DONE.** Since item 1's investigation already established the exact evidence (6/6 runs on WI emitting `unit="days_per_reporting_period"`), opened the v2.2 design-input ledger now rather than ship-then-patch-each-gap. First entry: minimum additions `days_per_reporting_period` + `days_per_6_month_reporting_period`; candidate additions flagged for the v2.2 design pass; cross-row coordination question raised (row-ID `_time_percent` suffix becomes misleading for day-count units). Ledger: [`results/v2_2_schema_inputs.md`](results/v2_2_schema_inputs.md).
- **Carry-overs that stayed closed this session:** Item 3 (Citations API — gated on Dan's review of 18 disagreement cells), Item 4 (MI-session standing instruction), Item 7 / GH #31 (parallelization — independent).
- **No API spend** this session (analysis + code-investigation only). No Phase 2 convo edits. No OH re-dispatch (out of handoff scope; could be captured as a followup if Dan decides it's worth ~$2.50).

### 2026-06-01 (later still) — statute-required vs portal-exposed comparison; Citations API direction added to followups
Doc: `results/20260601_wi_statute_vs_portal_spending.md`
- Compared the 53 cells in lobbyist + principal spending-report chunks against `releases/wi/` portal columns. **Headline:** WI has no separate lobbyist-filed spending report — §13.68(4) routes lobbyist info to the principal, who files under §13.68(1). Portal data confirms: `WI_lobbyist_filings.tsv` has zero expenditure columns, only `total_hours_communicating`/`total_hours_other`.
- **Newly visible metric:** inter-model alignment. 65 of 84 cells are jointly within-model stable; **18 (27.7%) deterministically disagree across models at high confidence**. Concentrated in lobbyist_spending_report: Claude reads "info flows through lobbyist" → TRUE; GPT reads "lobbyist files" → FALSE. **Portal data settles the disagreement: GPT wins 13/13 on these cells.** Per-model σ_noise was correct as designed but doesn't capture this.
- **Initial framing error caught and corrected:** earlier draft said σ_noise "was masking" inter-model disagreement. Wrong — σ_noise was always per-model. Real claim: there's a *second* metric (inter-model alignment) that has no current report.
- **Principal-side mapping** identified 4 concrete transparency gaps (compensation paid to lobbyists, gifts/entertainment, indirect costs, itemized format) — all reduce to "statute requires itemization, portal exposes single `total_expenditure` aggregate." Open question: portal-publication choice or scrape-loss in `wi-disclosure-explore`?
- **Open candidate from Dan (2026-06-01, not yet decided):** the 27.7% inter-model disagreement *might* warrant Citations API for adjudication once Dan looks more closely at the 18 disagreeing cells. To evaluate, not committed. Captured as item 3 in `plans/20260601_post_phase3_followups.md`. Also added: item 4 (bake portal-cross-validation into MI), item 5 (investigate principal-filings aggregation source).

### 2026-06-01 (later) — followups handoff written; MI substitutes for NC
Doc: `plans/20260601_post_phase3_followups.md` (originally written as `HANDOFF_followups.md`; promoted to `plans/` per protocol later in the same session)
- Two concrete pre-MI investigations captured: (1) Fix A int→Decimal regression on dict-shape value path, (2) `TimeThresholdCell.unit` literal-enum gap for v2.2 design ledger.
- Decision: **next state is MI, not NC.** Carried into STATUS row + the followups doc.
- Issue #31 (dispatch loop parallelization) noted as independent, can be done in parallel with 1/2 or with MI extraction.

### 2026-06-01 — Phase 2 (paid run) DONE on Air; Phase 3 written up
Convo: `convos/20260601_wi_tier1_phase2_run.md`
- Pre-flight: recreated `.env.local` from `.env.corporate` (lost in laptop data-loss); symlink chain wi-tier1 → compendium-source-extracts → main resolves. Tests 34/34, dry-run probe matches HANDOFF reference (6 chunks / 84 cells / 36 dispatches).
- **Run: 36/36 dispatched, 0 skipped, 0 dispatch failures, 20 min wall.** Total cost **$2.5708** (HANDOFF predicted $2-4; all 36 well under $1/call ceiling). Integrity check: 36 files / `corrupt: []` / file-cost sum matches log session_cost exactly.
- **σ_noise:** Claude **85.71%** (= OH 85.7%, state-invariant), GPT-5.2 **84.52%** (vs OH 73.8%, **+10.7 pts**). GPT's `scoreability_unstable` rose 2→7 — different failure mode than value-drift, worth metric segmentation.
- **Load-bearing structural finding:** both models, every single run (6/6) failed `lobbyist_registration_threshold_time_percent` instantiation on WI §13.62(11)'s 5-days-per-reporting-period lobbyist gate. Two distinct schema problems: (a) `magnitude: int=5` rejected — Fix A int→Decimal coercion (claimed-fixed on archived `extraction-harness-brainstorm` Tier-2 Step D) appears to NOT apply to this dict-shape value path — possible regression; (b) `TimeThresholdCell.unit` literal-enum has no `days_per_reporting_period` — schema can't represent WI's time threshold structure. Both findings are concrete v2.2 inputs.
- Captured GH issue [#31](https://github.com/danparshall/lobby_analysis/issues/31) for the embarrassingly-parallel dispatch loop (~15-20× wall-time speedup on the table).
- Next: investigate Fix A regression; capture TimeThresholdCell unit gap; decide whether to extend to NC.

### 2026-05-30 → 06-01 — branch created, Phase 1 DONE; Phase 2 deferred (disk)
Convo: `convos/20260530_wi_tier1_setup_and_phase1.md` · Handoff: `HANDOFF.md`
- Branched off `origin/main` (9b3de44) into `.worktrees/wi-tier1-direct-read`.
- Decisions (handoff + user): new branch off main (Q1); **both models** opus-4-7 + gpt-5.2 (Q2, mirror OH); **required** `--state`/`--vintage` args (Q3, closes accidental-OH-re-run footgun).
- **Phase 1 complete (TDD):** `scripts/tier_1_direct_read_legal_axis.py` parameterized by `--state`/`--vintage`; results/checkpoint dir state-keyed `…/results/tier_1/<STATE>_<VINTAGE>/`. New `tests/test_tier_1_state_keying.py` (6 tests). Verified: 34 Tier-1 tests pass; ruff clean; dry-run confirms WI bundle (16 txt), 6 chunks / 84 legal cells, 36 planned dispatches.
- **Also fixed (commit `a3bc1af`, on Dan's instruction):** bumped `SNAPSHOT_DATE_DEFAULT` 2026-04-13 → 2026-05-01 — the 04-13 portal snapshots were lost in the laptop data-loss event and re-fetched as 05-01 across all 8 states. Resolved 3 pre-existing `test_pipeline.py` failures; **full suite now 1550 pass / 0 fail**.
- **Phase 2 (paid 36-dispatch run) deferred:** this machine at 98–99% disk threw recurring ENOSPC (volume hitting 0-free mid-write). User will run Phase 2 on a higher-disk machine per `HANDOFF.md`.
- **Phase 3** pending the run.
