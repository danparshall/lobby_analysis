# Research Log: wi-ralph-cpi-renewal-cadence

Created: 2026-06-04
Purpose: Phase B Ralph trial — first-row-by-hand iteration on `lobbyist_registration_renewal_cadence` (CPI 2015 IND_199, WI=MODERATE) to surface what is and isn't tractable about row-level rubric-anchored iteration before designing any automation. Outcomes feed (a) the automation-level decision (post-iteration), (b) the deferred Phase A pre-flight YAML audit scope (every fix this row needs in YAML is a fix Phase A needs to know how to make at scale).

Plan: [`plans/20260604_phase_b_ralph_renewal_cadence.md`](plans/20260604_phase_b_ralph_renewal_cadence.md)

Upstream lineage (this branch exists because of):
- [`wi-tier1-direct-read` Commit 3 convo](https://github.com/danparshall/lobby_analysis/blob/wi-tier1-direct-read/docs/active/wi-tier1-direct-read/convos/20260604_wide_pass_commit3_redispatch_and_audit.md) — Ralph loop brainstorm seeded.
- [`wi-tier1-direct-read` 9-rubric oracle audit](https://github.com/danparshall/lobby_analysis/blob/wi-tier1-direct-read/docs/active/wi-tier1-direct-read/results/20260604_oracle_granularity_audit.md) — confirmed CPI 2015 C11 (not PRI 2010) as the per-item × per-state oracle source.

---

## Session history (newest first)

### 2026-06-05 (latest) — Cross-state expansion planning: 10-state target locked (NY/CO/WI/CA/TX/IL/WA/FL/NC/OH supersedes handoff's 15-state list); vintage 2015; CPI 2015 C11 projection-accuracy as primary success metric; v2.1-to-main merge gates dispatch; fresh $10 envelope
Convo: [`convos/20260605_cross_state_planning.md`](convos/20260605_cross_state_planning.md) · Plan: [`plans/20260605_cross_state_cpi_2015_validation.md`](plans/20260605_cross_state_cpi_2015_validation.md)
- **Pre-flight clean.** Read STATUS + README + this branch's RESEARCH_LOG (9 prior entries) + 3 handoff-named docs (Phase A kickoff convo + execution convo + plan) end-to-end. Trajectory clear; cross-state expansion is the natural continuation of Phase A's done-criteria-met state on WI.
- **Two Dan course-corrections held the design together.** (1) **State scope:** I carried the handoff's 15-state framing into my first questions; Dan replied *"after researching data availability, our top-ten states are: NY CO WI CA TX IL WA FL NC OH. Let's be sure to update our docs, and make that the target for Ralph."* — drops 5 states (AK/AR/MA/MI/PA/WV); adds NY (whose 2015/2025 bundles exist contrary to handoff's claim); preserves Dan's order (likely priority/data-quality, not alphabetical). (2) **Vintage:** Dan answered the success-metric question with *"do vintage 2015 and apply the CPI"* — overriding my 2025 default. Internally coherent: CPI 2015 is the published rubric oracle, so the extraction vintage must match. Sharpens the plan substantially.
- **Statute bundle availability verified empirically** via `/tmp/check_statute_bundles.py` (no chains, Write-then-run): all 17 states (10 in-scope + 5 dropped from handoff + NY + WY) have `data/statutes/<STATE>/<VINTAGE>/sections/` present. **NY has 2010 + 2015 + 2025 bundles** (handoff was wrong to mark NY 2010-only). WY remains 2010-only (handoff correct). All 10 target states × 2015 vintage confirmed.
- **CPI 2015 C11 projection operational on main confirmed.** `src/lobby_analysis/projections/cpi_2015_c11.py` + 3 test files (`tests/projections/test_cpi_2015_c11_{aggregation,ground_truth,per_item}.py`) + oracle CSV at `docs/historical/compendium-source-extracts/results/cpi_2015_c11_per_state_scores.csv` (all 50 states + DC; all 10 target states covered) + projection mapping doc.
- **6 decisions locked via 2 AskUserQuestion rounds.** (D1) Target state list = 10 states in Dan's order; (D2) vintage 2015 single-vintage this round; (D3) Phase A validation subset chunks (actor_registration_required + registration_thresholds + enforcement_and_audits); (D4) primary success metric = CPI 2015 C11 projection accuracy per state; (D5) v2.1 promotion to main BEFORE cross-state dispatch (successor branch cut off main for execution); (D6) fresh $10 cross-state envelope atop wi-ralph $3.51.
- **Open Question #1 (highest-leverage) surfaced in plan for execution session.** Of the 3 Phase A validation-subset chunks, `actor_registration_required` (~48% of per-state spend) contributes 0% to CPI 2015 C11 projection — it's PRI/FOCAL-anchored. Swapping → `registration_mechanics_and_exemptions` (which hosts IND_196 + IND_199 incl. Phase B iter 1+2's renewal_cadence) shifts dispatch budget from ~52% → ~95% feeding the primary metric, at slightly lower cost ($7.30 vs $8.30 across 10 states). Cost: lose BinaryCell cross-state template test (deferrable). Plan flags this as the execution session's first decision to surface to Dan, with corrected per-chunk projection-coverage counts (after reading `cpi_2015_c11_projection_mapping.md`).
- **3 other open questions documented in plan for execution session.** (#2) Tolerance for "projected matches published" — read existing test fixtures or propose ±10 points. (#3) Threshold for per-state instantiation errors that triggers pause-and-surface — default >5%. (#4) Should NY 2010 also be in scope? Default skip unless Dan asks.
- **Nori-flow boundary held cleanly for a third consecutive session.** This was planning + finish-convo only. No code, no YAML, no API spend. Memory entry `[[nori-flow-plan-then-fresh-session]]` continues to be load-bearing.
- **Spend ledger:** This session $0. wi-ralph cumulative **$3.5127** (unchanged; its $3-5 ceiling is decoupled — cross-state has its own fresh $10 envelope). Cross-state envelope: **$0 of $10**. Grand total WI Phase 1/2 + Phase B + Phase A: **$10.8073** (unchanged).
- **Execution session shape:** picks up on a successor branch cut off main AFTER P1 (v2.1 + Phase A YAML merge from wi-ralph). Reads plan + this convo + Phase A execution convo + `cpi_2015_c11_projection_mapping.md` + projection module. Surfaces Open Q #1 to Dan with corrected coverage counts before any dispatch. Then RED → GREEN per TDD skill (RED: per-state results JSON existence + zero instantiation errors above threshold; GREEN: dispatch loop + CPI projection audit script). Audit produces per-state table (published score, projected score, diff, within-tolerance verdict, instantiation errors, notes).
- **Next-session candidates** (after cross-state validation lands): cross-vintage stability (2025 dispatch on the same 10 states; another fresh envelope); DecimalCell-Optional follow-up Ralph on `_filing_itemization_threshold_dollars`; long-tail typed singletons template design (11 cell types deferred from Phase A).

### 2026-06-05 (even later) — Phase A execution: 163 YAML additives landed under TDD; A2.b dispatch confirms BinaryCell + EnumCell templates at scale on WI; DecimalCell-Optional 2 of 4 stable; long-tail TimeThresholdCell DEFER confirmed
Convo: [`convos/20260605_phase_a_execution.md`](convos/20260605_phase_a_execution.md) · Plan: [`plans/20260605_phase_a_yaml_audit_at_scale.md`](plans/20260605_phase_a_yaml_audit_at_scale.md)
- **RED batch (167 tests in `tests/test_phase_a_yaml_additives.py`).** 3 parametrized cell-type-bucket contracts (binary, decimal-optional, enum-family) + 2 plan-anchor sanity tests. RED confirmed 163 failures: 150 binary + 4 decimal-optional + 9 enum-family, each for the right reason (missing additive marker).
- **GREEN batch (single-script `/tmp/apply_phase_a_yaml_additives.py`).** 13 hand-craft prompts replaced wholesale (4 DecimalCell-Optional + 9 EnumCell-family, each with hand-designed cell-type-aligned vocabulary). 150 BinaryCell rows received the boolean additive via bulk append. 1 BinaryCell row skipped (`_defined_in_law` already had marker). Total: 163 prompt lines modified (file diff: 163 insertions + 163 deletions). Phase A tests 167/167 GREEN; full pytest suite 1850/3-skipped/3-xfailed.
- **Mid-execution Dan-question on dispatcher constraint.** Plan + handoff called for dispatch on `actor_registration_required`, but the Tier-1 dispatcher hardcoded `_RESOLVED_CHUNKS` to the 6 CPI-2015 C11 chunks. Per plan §"Out of scope" ("If A0 surfaces a code-side surprise, surface to Dan, don't autonomously change code") surfaced 3 concrete options. **Dan picked (a) Extend `_RESOLVED_CHUNKS` via TDD.** Landed as `_DEFAULT_CHUNKS` (6 — used when `--chunks` omitted) + `_RESOLVED_CHUNKS` = `_DEFAULT_CHUNKS + ("actor_registration_required",)` split (preserves default-dispatch invariant, allows `--chunks actor_registration_required`). 1 new dispatcher test + 1 existing test text update; full suite 1851 passed.
- **Archived 12 prior JSONs** for `registration_thresholds` + `enforcement_and_audits` to `_pre_phase_a_<chunk>/` with SUPERSEDED.md banners (per "prefer mv over rm" memory). `actor_registration_required` has no priors.
- **A2.b dispatch — 3 chunks, $0.8290.** 18/18 dispatched, 0 skipped, ~5 min wall time. Per-chunk:
  - **`actor_registration_required`** (BinaryCell, 11 cells × 6 = 66 cell-instantiations): **0 instantiation errors.** 9 of 11 rows 6/6 stable at high confidence; `actor_lobbying_firm_*` 5/6 majority True (1 Claude run3 outlier); `actor_local_government_*` + `_public_entity_other_*` 6/6 True at medium confidence. **BinaryCell bulk additive works at scale.**
  - **`enforcement_and_audits`** (1 BinaryCell + 1 EnumCell, 2 cells × 6 = 12 cell-instantiations): **0 errors.** `_audit_required_in_law` **converged 6/6 'MODERATE'** under the new YES/MODERATE/NO EnumCell additive — the **predecessor session's Claude-run3-drift-to-YES value-stability flag is RESOLVED.** `_defined_in_law` 6/6 True (bonus BinaryCell stability re-test). **EnumCell hand-craft additive works.**
  - **`registration_thresholds`** (mixed types, 6 cells × 6 = 36 cell-instantiations, 6 instantiation errors all on the same DEFER row): `_filing_de_minimis_threshold_dollars` **6/6 '500' at high confidence**; `_registration_threshold_expenditure_dollars` **6/6 None at high confidence** (GPT cleared UNSCOREABLE×3 → None); `_filing_itemization_threshold_dollars` 2/6 '200' + 4/6 None (Claude=$200 / GPT=None disagreement — still ambiguous, Phase B candidate); `_threshold_compensation_dollars` GPT minor regression 3/3 '0' → 1/3 '0' + 2/3 None (consistent interpretation, sentinel-value flip); `_registration_threshold_time_percent` **6/6 TimeThresholdCell instantiation errors** (long-tail DEFER row — models emit valid magnitude+unit shape but `unit='days'` / `'days_per_reporting_period'` not in TimeUnitLiteral domain). **DecimalCell-Optional template: 2 of 4 rows value-stable; 2 need follow-up.**
- **Chunk-mate spillover, mostly positive.** GPT cleared UNSCOREABLE on 2 rows post-Phase-A (positive); minor Claude regression on 1 row (negative); net positive. Mechanism deserves a brainstorm — bulk per-row additives change chunk-wide prompt context, affecting unrelated rows.
- **6-vs-15 chunk-count question resolved empirically.** Dispatcher's `_RESOLVED_CHUNKS` was 6; chunks_v2 manifest has 15; Phase A extended to 7. Multi-state expansion may want further extensions if PRI/HG-anchored rows are needed.
- **Spend ledger:** This session $0.8290 (A2.b dispatch only; all other steps $0). wi-ralph cumulative **$3.5127** (against $3-5 ceiling; $1.49 remaining). wi-tier1-direct-read unchanged at $7.2946. Grand total WI Phase 1/2 + Phase B: **$10.8073**.
- **Phase A done-criteria met (5 of 6):** zero rubric-vocab-vs-cell-type mismatches on rows whose cell_type matches the 4 confirmed templates ✓; long-tail singletons flagged as Phase B candidates ✓; chunk-mate spillover characterized ✓; clean re-dispatch (0 errors on the 4-cell-type templates; 6 errors all isolated to the documented long-tail DEFER row) ✓; doc-graph self-consistent at commit ✓. Criterion 5 (v2.1 propagation to main) still **DEFERRED** per Decision 3.
- **Next-session candidates** (Dan's call): (i) Cross-state expansion design — 15 states × 2 vintages, batch-dispatch-per-loop, ~5x Phase A's per-state cost; (ii) DecimalCell-Optional follow-up Ralph on `_filing_itemization_threshold_dollars` (Claude=$200 / GPT=None disagreement, ~$0.30); (iii) Long-tail typed singletons template design (11 cell types deferred; TimeThresholdCell is the most concrete target); (iv) v2.1 propagation to main (templates confirmed at scale; safe to merge).

### 2026-06-05 (later still) — Phase A kickoff: plan written, A0 diagnostic dispatch dropped after Dan's "skip it, additive is free anyway" push-back; ready for fresh-session TDD execution
Convo: [`convos/20260605_phase_a_kickoff.md`](convos/20260605_phase_a_kickoff.md) · Plan: [`plans/20260605_phase_a_yaml_audit_at_scale.md`](plans/20260605_phase_a_yaml_audit_at_scale.md)
- **Pre-flight clean.** Read all 4 handoff-named convos end-to-end + sweep results + NAMING_CONVENTIONS + source_quotes.yaml (755 lines) + v2.1 TSV header. Confirmed state-vintage matrix with Dan (15 states have 2015 + 2025; CO is in; NY is NOT — only 2010).
- **Characterized YAML scope precisely** (vs. handoff's "~22 / ~161" estimate). Wrote `/tmp/phase_a_characterize.py`: 183 rows total, 22 already-additive (~12%), 161 raw rubric-vocab (~88%). **The key surprise: 135 of the 161 raw rows are BinaryCell** — most of Phase A's surface area is mechanically extending the BinaryCell template (`Answer with the boolean value true or false.`) to those 135 rows. DecimalCell-Optional: 3 raw. Enum-family: 9 raw. Long-tail typed singletons (11 distinct cell types): defer to Phase B follow-up. Practical-axis-only typed-int: 3 skip.
- **Dan's load-bearing course-correction: dropped Stage A0 (diagnostic dispatch, ~$0.40).** Original plan had A0 to test whether 135 raw-binary rows currently trip BinaryCell coercion error. Dan: *"why not just apply the template preemptively, it's free?"* Correct — the additive pattern is purely additive (iter 4 ablation already proved this), so we apply the template in both branches anyway. A0 has near-zero decision value. Defensive immunization beats post-hoc diagnosis, especially with multi-state expansion next (state-specific statute quirks could elicit "Yes" vs "true" inconsistently even on rows WI happens to currently clear). Plan simplifies 4 stages → 3.
- **Dan confirmed verification strategy: A2.b — one chunk per touched cell type (~$1.00-1.20).** Catching a template bug on one WI chunk is cheaper than catching it post-multi-state-dispatch. Fits ceiling cleanly (cumulative ~$3.88 worst case vs $5 max).
- **Nori-flow protocol catch (second-in-two-sessions).** Mid-session Dan asked: *"you're going to follow Nori flow, right? first make the updated plan, and then I'll start a fresh session to TDD?"* — caught me investigating `enum_domains` (execution prep dressed as planning prep). Walked back; saved as memory entry `[[nori-flow-plan-then-fresh-session]]` (sibling to [[doc-system-is-persistent-memory-not-patchwork]]); finalized plan with explicit "fresh-session-TDD" framing in §Pre-execution checklist + enum-domain-lookup note for implementing agent + linked artifact pointers to /tmp scripts.
- **Plan revised in-place** with "Plan revision 2026-06-05 (post-Dan-discussion)" banner up top capturing what changed and why. Per-cell-type targets enumerated with concrete row counts. Cost projection revised to ~$1.20 max for this branch (well under ceiling).
- **3 open decisions for the next session.** (1) RESOLVED: verification strategy = A2.b. (2) RESOLVED: BinaryCell verification chunk = `actor_registration_required` (11 PRI rows, ~$0.40). (3) DEFERRED: v2.1 propagation to main — waits until post-A2 verification.
- **No code, no YAML, no API spend.** wi-ralph cumulative unchanged at **$2.6837**.
- **Next session candidates** (Dan's call which to pick first, but Phase A is the obvious continuation):
  - (i) **Phase A execution** (fresh-session TDD per plan; ~$1.20 expected; ~30-50 min YAML edits + ~3 chunk dispatches + audit + finish-convo)
  - (ii) Long-tail typed singletons template design (11 distinct cell types; deferred from Phase A; ~$0.30-0.50 per cell-type design iteration)
  - (iii) `_audit_required_in_law` value-stability test (may be incidentally resolved by Phase A's EnumCell additive on this row)
  - (iv) v2.1 propagation to main (post-Phase A verification)

### 2026-06-05 (later) — Pattern C v2.1 execution: 6/6 on `_defined_in_law=True` (BinaryCell additive pattern confirmed); 4-cell-type matrix structurally closes; cell-type-vocabulary mismatch class characterized (`true`/`false` ≠ `yes`/`no`)
Convo: [`convos/20260605_pattern_c_v2_1_execution.md`](convos/20260605_pattern_c_v2_1_execution.md) · Plan: [`plans/20260605_pattern_c_row_split_v2_1.md`](plans/20260605_pattern_c_row_split_v2_1.md) (incl. "Expanded Step 2c" addendum)
- **Pre-flight clean.** Confirmed 2 open scoping Qs with Dan: (a) new row name = `lobbying_disclosure_audit_required_in_practice` (inverse-fix-symmetric mirror of `_audit_required_in_law`; NOT `_audit_conducted_in_practice` as plan defaulted); (b) v2.1 scope = branch-local (plan default).
- **Mid-session scope discovery.** Plan's Step 2c said "tier_1_direct_read_legal_axis.py pointer update"; actual cascade was 5 production touchpoints (loader DEFAULT + chunks_v2/manifest + CPI projection L287 + TSV + YAML) plus 7 test touchpoints (combined-axis sets + parametrize cases + COMBINED_AXIS_ROWS tuple + IND_208 fixtures × 2 + synthesizer split + row-count test). Surfaced 4 options to Dan; Dan picked **(1) charge through: full v2.1 swap on this branch**.
- **Protocol catch.** Dan asked "you ARE writing a plan and using Nori / TDD right?" — caught me about to edit code before updating the plan. Walked back, wrote substantial plan addendum (§"Expanded Step 2c — discovered scope"; 3 decisions + 5 production + 7 test touchpoints + TDD execution order + rollback policy), read `skills/test-driven-development/SKILL.md`, then executed RED → GREEN per skill.
- **RED batch (all test edits before any code change).** 6 enumerated test edits + 1 added explicit-schema-contract test (`test_v2_1_pattern_c_split_rows_present_and_wrong_axes_removed`) — the explicit test was needed because the shrunken `combined_row_ids` set passed against v2.0 (subset-assertions of true statements stay true). RED verified: 8 IND_208-related failures + 1 schema-contract failure, all for the right reasons.
- **GREEN batch (production changes).** Created `compendium/disclosure_side_compendium_items_v2.1.tsv` (183 data rows, 186 cells — splits are cell-count-neutral). Added 2 YAML entries (`_defined_in_law` with HG Q41/Q42 source quotes + initial BinaryCell prompt; `_audit_required_in_practice` with CPI IND_208 source quote). Swapped `DEFAULT_COMPENDIUM_V2_TSV` → v2.1 path. Extended `enforcement_and_audits` ChunkDef 2 → 4 rows. Flipped `project_ind_208` lookup → new practical-axis row. Targeted 4-file pytest: 95/95 green. **Full-suite pytest discovered 7th test touchpoint** (`tests/test_compendium_loader_v2.py::test_load_v2_compendium_returns_181_rows`, `EXPECTED_V2_ROW_COUNT=181`) — patched into plan addendum (touchpoints 6 → 7) BEFORE the code fix, fix landed, full suite green at 1683/1683.
- **Dispatch (3 attempts).** First: env vars unset (`--env-file .env.local` resolved). Second: dispatcher resumed from old wi-tier1 wide-pass checkpoints (`skipped(resumed)=6, $0`). Archived 6 stale JSONs to `_pre_v2_1_pattern_c_enforcement/` with SUPERSEDED.md banner. **Third dispatch: 6/6 errored with `BinaryCell: cannot coerce 'yes' to bool`** — both Claude (3/3) and GPT (3/3) correctly emitted `'yes'` per WI §13.69 forfeitures, but dispatcher's `_instantiate_cell` accepts only `'true'`/`'false'`. The iter-5 playbook's "cell-type instantiation failure" branch landed literally. Cost: $0.1710.
- **Cell-type-vocabulary mismatch finding documented.** Archived buggy-prompt JSONs to `_pre_v2_1_binarycell_vocab_fix/` with SUPERSEDED.md banner (preserved as negative-result evidence). Flipped `_defined_in_law` YAML prompt: `'yes'/'no'` → `true`/`false` (cell-type-aligned vocabulary per additive pattern). **Fourth dispatch: 6/6 success, all `cells=2 errors=0`** at $0.1554.
- **Load-bearing convergence:** **6/6 on `_defined_in_law = True` at high confidence**, all citing §13.69 forfeitures + §13.69(7) Class H felony for false statements. The de-jure pair CPI 2015 IND_209's projection mapping doc said should exist now reads cleanly. **4-cell-type matrix structurally complete** (IntCell ✓ iter1+2, EnumCell ✓ iter3+4, DecimalCell-Optional ✓ iter5, BinaryCell ✓ this session; DecimalCell-non-negative partially-confirmed via iter5 spillover).
- **Phase A pre-flight YAML audit template set documented (5 cell-type-aligned vocabularies):** IntCell (`"as an integer"`), EnumCell (`"one of: a, b, c"`), DecimalCell-Optional (`"non-negative decimal; 0 for no threshold; null for no statute"`), DecimalCell-non-negative (same minus null), BinaryCell (**`true`/`false`, NOT `yes`/`no`**). Phase A scope now well-defined.
- **Secondary observation: `_audit_required_in_law` Claude run3 drift to YES** (1/3 Claude, 0/3 GPT). Row's prompt NOT updated this session (still raw CPI source quote, no additive cell-type-aligned instruction). First sampling-time variation on the iter-5-flagged IND_207 CPI errata candidate. Next-session candidate: value-stability test with additive EnumCell pattern (~$0.15).
- **Per-iter cost on 2-cell `enforcement_and_audits` chunk: $0.1554** at the cell-type-aligned prompt (4 cells dispatched × 2 models × 3 runs ≈ $0.013/cell-call). Confirms per-cell linear cost model.
- **Spend ledger:** This session $0.3264 ($0.1710 buggy-prompt + $0.1554 fixed). wi-ralph cumulative **$2.6837** (against $3-5 ceiling; $0.32-$2.32 remaining). wi-tier1-direct-read unchanged at $7.2946. Grand total WI Phase 1/2 + Phase B: **$9.9783**.
- **No-API doc-graph discipline notes:** plan addendum patched mid-execution (6 → 7 test touchpoints) per "doc system is persistent memory, not patchwork" memory; 6 wi-tier1 stale JSONs + 6 buggy-prompt JSONs both archived with SUPERSEDED.md per "prefer mv over rm for research artifacts" memory; `git -C` used throughout per the `cd <path> && git` chain-block.
- **Next session candidates** (Dan's call): (i) Phase A pre-flight YAML audit at scale — now best-positioned (5 cell-type templates + ~21 swept rows + chunk-mate spillover constraint); (ii) value-stability test on `_audit_required_in_law` with additive EnumCell instruction; (iii) value-stability test on `lobbyist_filing_itemization_de_minimis_threshold_dollars` ($200 vs $500 ambiguity carry-forward); (iv) propagate v2.1 to main + other branches (structural fix dispatch-confirmed; safe to merge); (v) chunk-mate spillover mechanism investigation.

### 2026-06-05 — Pattern C audit Step 1 done (clean: 1 egregious + 1 inverse, anticipated scope); Dan picked v2.1 schema bump + parallel inverse fix; Steps 2+3 deferred to next-session plan doc
Convo: [`convos/20260605_pattern_c_audit_and_plan.md`](convos/20260605_pattern_c_audit_and_plan.md) · Plan: [`plans/20260605_pattern_c_row_split_v2_1.md`](plans/20260605_pattern_c_row_split_v2_1.md)
- **Step 1 (Pattern C audit on v2 TSV, 182 rows; $0 API)** — grepped `_in_practice|_in_law` substrings in `compendium_row_id`. Only 2 hits, matching iter-5 convo's anticipated scope exactly: `lobbying_violation_penalties_imposed_in_practice` (line 72, **egregious** — binary legal cell asks "in law, are penalties imposed in practice?" — category error) and `lobbying_disclosure_audit_required_in_law` (line 49, **inverse / less egregious** — legal name matches legal cell, only the practical cell mis-keyed by name). Cross-checked all 5 `legal+practical` rows; other 3 (`lobbyist_registration_required`, `lobbyist_spending_report_filing_cadence`, `lobbyist_registration_deadline_days_after_first_lobbying`) are neutrally-named and structurally coherent — NOT bugs.
- **Surfaced 2 scoping decisions to Dan before editing the shared TSV** (multi-committer repo norm): (a) v2 in-place vs v2.1 schema bump → **Dan picked v2.1** (cleaner historical record; v2 stays frozen); (b) fix inverse `_audit_required_in_law` this session or defer → **Dan picked parallel fix** (doubles structural work but addresses both bugs cleanly).
- **Session deliverable shifted from Steps 2+3 execution to Steps 2+3 plan doc** — context budget exhausted before structural edits could land. Plan doc at `plans/20260605_pattern_c_row_split_v2_1.md` is self-contained for cold pickup: captures Step 1 findings, Dan's 2 scoping decisions, 4 row-level changes (edit `_imposed_in_practice` → practical-only; add `_defined_in_law` binary legal; edit `_audit_required_in_law` → legal-only; add new `_audit_conducted_in_practice` typed-int practical), YAML population templates per cell type, downstream-consumer pointer updates (`tier_1_direct_read_legal_axis.py`, `silent_unit_mismatch_sweep.py`), Step 3 dispatch command (`--chunks enforcement_and_audits`, ~$0.30), and 2 open scoping questions for implementing agent.
- **Open scoping questions deferred to implementing session:** (a) `_audit_conducted_in_practice` vs `_audit_required_in_practice` naming choice; (b) v2.1 pointer scope (this branch only vs propagate to main). Plan defaults to "scoped to this branch; surface for merge after BinaryCell dispatch confirms fix."
- **No code, no API spend, no TSV/YAML edits this session.** wi-ralph cumulative unchanged at **$2.3573**. wi-tier1-direct-read unchanged at $7.2946. Grand total: **$9.6519**.
- **Implementing-session shape:** pre-flight reads (this plan + iter-5 convo §"Post-session refinement" + CPI 2015 projection mapping doc §IND_207/IND_209 + `compendium/NAMING_CONVENTIONS.md` + `source_quotes.yaml` existing entries) → confirm 2 open scoping Qs with Dan → Step 2a (v2.1 TSV creation, $0) → Step 2b (YAML population, $0) → Step 2c (downstream pointer updates, $0) → Step 3 (`enforcement_and_audits` chunk dispatch, ~$0.30) → audit → finish-convo. Net cumulative spend after = **$2.6573** of $3-5 budget.

### 2026-06-04 (later evening, immediately after iter 3+4) — Silent-unit-mismatch sweep + iter 5 confirmation on IND_197 (DecimalCell-Optional); 3 cell types confirmed + DecimalCell-non-neg via spillover; 2 CPI WI-row errata candidates surfaced
Convo: [`convos/20260604_phase_b_silent_unit_mismatch_sweep.md`](convos/20260604_phase_b_silent_unit_mismatch_sweep.md) · Sweep results: [`results/20260604_silent_unit_mismatch_sweep.md`](results/20260604_silent_unit_mismatch_sweep.md) · Iter 5 results: [`results/20260604_compensation_threshold_iter5.md`](results/20260604_compensation_threshold_iter5.md) · Plan: [`plans/20260604_silent_unit_mismatch_sweep.md`](plans/20260604_silent_unit_mismatch_sweep.md)
- **Picked up handoff from iter 3+4 and chose (b) sweep** of the (a')/(b)/(c) candidate set. Wrote `scripts/silent_unit_mismatch_sweep.py` (~470 lines, ruff clean, stdlib-only) enumerating 21 CPI-2015-readable rows × 14 CPI indicators → 20 (row × indicator) pairs. Loaded WI's CPI 2015 700-cell oracle, current top-level wide-pass JSONs, projected emissions per hard-coded indicator scoring rules from `cpi_2015_c11_projection_mapping.md`. **$0 API cost for the sweep.**
- **Sweep classifications**: 10 NOT_EMITTED (practical-axis-only IND, expected); 7 COMPOUND_ROLE (IND_196, IND_201, IND_203) with all 6/6 composite MATCH per indicator; 1 single-row MATCH (IND_199 `renewal_cadence` post-iter-2); **2 MISMATCH** (IND_197 + IND_207). Spot-checked both MISMATCH source JSONs by hand.
- **IND_197 `lobbyist_registration_threshold_compensation_dollars`** — both/and finding: (a) GPT 2/3 wide-pass emitted `'0.5'` (literal CPI tier score 50/100 as DecimalCell value) — **the silent-unit-mismatch class confirmed in the wild** on a row the wide-pass Commit 3 audit had cleared; (b) Claude 3/3 emit `'0'` (correct per §13.62(11): no compensation threshold) projecting to YES — disagreement with CPI's MODERATE rating suggests CPI 2015 mis-coded WI by conflating §13.621 principal-side $500 with a lobbyist-side comp threshold.
- **IND_207 `lobbying_disclosure_audit_required_in_law`** — pure CPI-oracle-vs-statute disagreement (no silent mismatch). 6/6 emit `'MODERATE'` citing §13.74(1) + §13.685(3) (Ethics Commission compliance review = MODERATE per projection rule, not impartial third-party = YES). Already pre-flagged in iter 3 convo as candidate CPI errata; this session promotes from suspicion to documented finding.
- **Sweep validates 16 of 17 cleared CPI-readable rows as trustworthy.** Only IND_197 newly surfaces. Bounds Phase A scope dramatically: silent-mismatch is real but rare (1 of 20 pairs, not 5+ as iter 1+2 plan worried). The wide-pass JSONs project cleanly via composite on the 3 compound legal indicators and via single-row on `renewal_cadence`.
- **Surfaced findings + (a')/(b)/(c) re-decision to Dan; Dan picked Step 5 on IND_197 only** (over combined (a') + Step 5 wave, or finish-convo here, or pause-and-discuss).
- **Iter 5 dispatch ($0.2853)** — added additive DecimalCell-Optional question to YAML prompt (CPI source quote preserved + appended "Answer with the dollar amount as a non-negative decimal (e.g., 500 for a $500 threshold). Use 0 if anyone paid any amount to lobby is a lobbyist (no threshold, e.g., the statute defines a lobbyist as anyone receiving 'any economic consideration' to lobby). Use null if no statute defines a compensation standard at all."). Archived pre-iter5 JSONs to `_pre_iter5_compensation_threshold/`. Dispatched `--chunks registration_thresholds` (6 cells, 2 min wall time). **6/6 converge on `'0'` at high confidence** citing §13.62(11); GPT shifted 2/3 → 0/3 emitting `'0.5'`. **Silent-unit-mismatch fixed.**
- **Three unexpected chunk-mate spillovers, all substantively positive:** (i) `lobbyist_filing_itemization_de_minimis_threshold_dollars` (candidate-(a') DecimalCell-non-negative row): Claude's `-1` sentinel pattern eliminated (2/3 FAIL → 0/3); 3/3 now instantiate with `$200`/`$500`/`$500` (value-stability not reached but failure mode fixed). (ii) `lobbyist_filing_de_minimis_threshold_dollars`: GPT outlier `'1'` → `'500'` (stability 5/6 → 6/6). (iii) `lobbyist_registration_threshold_expenditure_dollars`: Claude 3/3 swung `'0'` → `null` (plausibly more correct — WI has no lobbyist-side expenditure standard, only principal-side §13.621). **The additive instruction's effect propagates through chunk-wide prompt context, not just the single-row prompt.**
- **Load-bearing finding:** the 4-cell-type matrix now reads **3 explicitly confirmed + 1 partially-confirmed-via-spillover**: IntCell ✓ (iter 1+2), EnumCell ✓ (iter 3+4), DecimalCell `Optional[Decimal]` ✓ (iter 5), DecimalCell-non-negative ◐ (instantiation-failure mode fixed via spillover; value-stability untested), BinaryCell ✗ (untested; candidate row carries Pattern C v2.2 row-axis bug). Phase A pre-flight YAML audit's per-cell-type instruction templates documented in iter 5 results doc §Decisions.
- **Bonus finding 1 (chunk-mate spillover is bigger than prior iterations made visible).** The iter 1+2 plan flagged chunk-mate spillover as an edge case; iter 5 makes it concrete with 3 substantive shifts. Implication for Phase A: per-row prompt edits affect chunk-mate rows in this regime — could be designed-with (deliberate chunk-wide patterns) or designed-around (verify chunk-mate rows weren't dragged wrong).
- **Bonus finding 2 (CPI 2015 WI-row errata).** Two candidate errata documented this session: IND_197 (CPI MODERATE, model+statute YES) and IND_207 (CPI YES, model+statute MODERATE). Tracked across sweep + iter 5 results docs for future projection-mapping-doc footnote update.
- **Per-iteration cost on 6-cell chunk: $0.2853** (~$0.05/cell/model/run). Confirms per-cell linear cost model.
- **Spend ledger:** wi-ralph cumulative **$2.3573** (against $3-5 ceiling, $0.64-$2.64 remaining). wi-tier1-direct-read unchanged at $7.2946. Grand total WI Phase 1/2 + Phase B: **$9.6519**.
- **Next session candidates** (carry forward + better-defined):
  - (i) BinaryCell additive pattern test on `lobbying_violation_penalties_imposed_in_practice` — but row carries Pattern C v2.2 row-axis bug; brainstorm scope first to disentangle.
  - (ii) Value-stability test on `lobbyist_filing_itemization_de_minimis_threshold_dollars` ($200 vs $500 ambiguity needs disambiguating prompt edit).
  - (iii) Chunk-mate spillover mechanism investigation — informs Phase A design.
  - (iv) Phase A pre-flight YAML audit at scale (candidate (c)) — now well-defined: 3 cell-type templates + chunk-mate spillover as known constraint + 5 known target rows + 16 sweep-cleared rows.
- **Post-session refinement (Dan 2026-06-04, after finish-convo commit landed):** Dan's sharpening question on candidate (i) — *"Seems strange that '_in_practice' would exist without '_in_law'..."* — lands directly on the projection mapping doc's deferred item. CPI 2015 IND_209 mapping explicitly said the de-jure pair belongs in the compendium; 2026-05-13 v2 row-freeze collapsed it. **Next-session shape (one session, three pieces):** (1) Pattern C audit (grep TSV for analogous mis-axed rows; ~15 min, no API); (2) compendium fix — add `lobbying_violation_penalties_defined_in_law` binary legal row + strip legal axis from `_imposed_in_practice`; (3) BinaryCell additive pattern test on the new row via `enforcement_and_audits` chunk dispatch (~$0.30). Closes 4-cell-type matrix structurally; positions Phase A at scale for the session after. See convo doc §"Post-session refinement" for handoff sentence.

### 2026-06-04 (later evening, immediately after iter 1+2) — Iters 3 + 4 converged 6/6 on `'none'` for `filing_cadence` (EnumCell); additive pattern generalizes IntCell → EnumCell AND is robust to wrong-axis source-quote contamination
Convo: [`convos/20260604_phase_b_iter_3_filing_cadence.md`](convos/20260604_phase_b_iter_3_filing_cadence.md) · Iterations log: [`results/20260604_filing_cadence_iterations.md`](results/20260604_filing_cadence_iterations.md)
- **Target:** `lobbyist_spending_report_filing_cadence`, the EnumCell wide-pass-failure row per iter 1+2's handoff. Pre-flight surfaced two structural complications: (1) row is split-axis (`enum (legal) + typed int 0-100 step 25 (practical)`); CPI's only indicator touching it is **IND_202** (not IND_201 as handoff said — that was a mis-label), and IND_202 scores ONLY the practical axis. Dispatcher only extracts legal axis. So wide-pass YAML's prompt was structurally wrong-axis (practical-axis IND_202 source quote populating a legal-axis EnumCell cell). (2) **No CPI legal-axis oracle exists for this row** — the statute is the oracle. WI §13.68(1) puts the filing duty on the principal; §13.68(4) routes lobbyist info through the principal → expected legal-axis enum = `'none'` (lobbyists do not file separately in WI). Vintage check §13.68(1)/(4) identical 2015 → 2025 (just "board" → "commission" rename).
- **Iter 0 (pre-iter3) baseline inspection** (no dispatch): Claude 3/3 emitted string `"50"` at confidence=medium (semantically wrong-variable — CPI tier value, NOT cadence enum; EnumCell accepted because bare); GPT 3/3 emitted int `0` → instantiation_failed (EnumCell wants string). Both models' justifications correctly read §13.68(4)'s routing-to-principal pattern; disconnect was purely the wrong-axis prompt.
- **Iter 3 (vocab-strip + EnumCell-aligned legal-axis prompt):** Replaced YAML prompt with "Under state law, how often must a LOBBYIST file a separate spending report? Answer with one of: 'annual', 'semiannual', 'quarterly', 'monthly', 'biennial', 'triannual', 'other', or 'none'. Use 'none' if lobbyists are not required to file a separate spending report (e.g., if statutory filing duties are assigned only to the principal/employer, with lobbyists providing information to the principal). If 'other', describe the actual cadence in the justification field." Dispatched single chunk via `--chunks lobbyist_spending_report` (~$0.74 — chunk has 30 legal cells vs `renewal_cadence`'s 8; per-cell cost ~$0.025 per model per run). **6/6 converged on `value: 'none'` at confidence=high.** All 6 cite both §13.68(1) AND §13.68(4) (wide-pass baseline cited §13.68(1) only). Cost $0.7404.
- **Iter 4 (ablation: keep wrong-axis CPI IND_202 quote at front, append legal-axis question):** Stricter ablation than iter 1+2's iter 2 (wrong axis, not just wrong-vocab-same-axis). Restored CPI IND_202 practical-axis quote at front + kept iter-3 legal-axis question + EnumCell enumeration at end. Cost $0.7494. **6/6 ALSO converged on `value: 'none'`** at confidence=high; same citation pattern; substantively identical justifications. Cumulative wi-ralph **$2.0720**.
- **Load-bearing finding:** the additive cell-type-aligned-instruction pattern **generalizes to EnumCell** AND is **robust to wrong-axis source-quote contamination**, not just rubric-vocab-vs-cell-type mismatch. **Phase A pre-flight YAML audit's pattern is purely additive across both tested cell types AND both axis-cases (axis-aligned + wrong-axis):** append the cell-type-aligned actionable question; never need to strip source quotes (even wrong-axis ones). Production YAML now holds iter 4's prompt.
- **Bonus structural finding (silent-mismatch class instance):** the wide-pass Commit 3 audit (`docs/active/wi-tier1-direct-read/results/20260604_wi_wide_pass_audit.md` row 5) had read Claude's wide-pass `"50"` as semantically improved vs narrow-pass `"none"` ("more correct — matches CPI 50-tier"). With the legal-axis oracle now in hand from iter 3 + iter 4, the read **inverts** — narrow-pass `"none"` was substantively correct; wide-pass `"50"` was dragged WRONG by the CPI practical-axis vocab. **Direct evidence the silent-mismatch class is real** (motivating the sweep plan at `plans/20260604_silent_unit_mismatch_sweep.md`). No retro-edit of the wide-pass audit (consistent with iter 1+2's no-retro-correction posture); refinement preserved in the new iterations log §Findings 4 + §Recommendations 6.
- **4 of 4 iterations across 2 cell types** (IntCell from iter 1+2; EnumCell from iter 3+4) **converged 6/6 on statute-derived oracle values.** DecimalCell-non-negative + BinaryCell remain unconfirmed (~$0.60-1.50 to close the 4-cell-type matrix in a follow-up session).
- **Cross-checked alternative target `lobbying_disclosure_audit_required_in_law`** (IND_207, EnumCell, legal-axis) during pre-flight. Already converges 6/6 at `"MODERATE"` in wide-pass but WI's CPI 2015 oracle is YES — models cite §13.74(1) examination requirement and conclude compliance-review = MODERATE (plausibly models are right and CPI 2015 is stale/differently-interpreted). Worth a closer look as part of the sweep candidate.
- **Wide-pass YAML population's structural blind spot characterized:** for rows where the source rubric reads only an axis the row doesn't extract, the mechanical-population pass populates a structurally wrong-axis prompt. Candidate Phase A pre-flight script detection: per (row × source-rubric) pair, flag axis-mismatch via projection mapping + TSV axis column; for axis-mismatched rows, append actionable question to overcome the wrong-axis source quote (iter 4 confirmed appended question wins).
- **Per-iteration cost ~$0.75 on this chunk** (vs ~$0.29 on the 8-cell `registration_mechanics_and_exemptions`). Chunk size linearly drives cost. Budget OK; $0.93-$2.93 remaining of $3-5 ceiling.
- **Spend ledger:** wi-ralph cumulative **$2.0720**. wi-tier1-direct-read unchanged at $7.2946. Grand total WI Phase 1/2 + Phase B: **$9.3666**.
- **STATUS.md table row for `wi-ralph-cpi-renewal-cadence` added in this session's finish-convo commit** (per Dan's confirmation — branch was missing from Active Research Lines table despite Recent Sessions narrative covering both prior sessions).
- **Next session candidates** (carry forward + better-motivated): (a') DecimalCell + BinaryCell trials to close 4-cell-type matrix (~$0.60-1.50); (b) silent-unit-mismatch sweep — plan ready at `plans/20260604_silent_unit_mismatch_sweep.md`, now better motivated by iter 3's concrete instance; (c) Phase A pre-flight YAML audit at scale — now better-defined since additive pattern confirmed across both tested cell types in both axis-cases.

### 2026-06-04 (evening, immediately after kickoff) — Iters 1 + 2 converged 6/6 on CPI MODERATE; ablation surfaces additive Phase A pattern
Convo: [`convos/20260604_phase_b_iter_1_and_2.md`](convos/20260604_phase_b_iter_1_and_2.md) · Iterations log: [`results/20260604_renewal_cadence_iterations.md`](results/20260604_renewal_cadence_iterations.md)
- **Pre-flight (4 steps, all clean):** (1) merged wi-tier1-direct-read in (STATUS.md merge conflict resolved via one-off Python script); (2) vintage check on WI §13.63(1)(a) confirmed identical 2015 → 2025 text — biennial → CPI MODERATE oracle valid for 2025; (3) added `--chunks <chunk_id>` flag to dispatcher under TDD (4 new tests, full suite 1687 pass / 0 fail / 3 xfailed; ruff clean; commit `5351072`); (4) iter 0 inspection of merged-in wi-tier1 wide-pass JSONs confirmed the dual failure mode (Claude 3/3 instantiation_failed emitting `"YES"`/`"MODERATE"`; GPT 3/3 instantiated but emitting `2` for years-not-months — IntCell has no unit enforcement, so GPT's wrong-scale answer was passing silently).
- **Iter 1 (vocab-strip + explicit units):** edited `compendium/source_quotes.yaml` prompt to "How often must a lobbyist renew their registration under state law? Answer in months as an integer (e.g., 12 for annual, 24 for biennial, 36 for triennial). If no renewal is required, return null." Dispatched single chunk via `--chunks registration_mechanics_and_exemptions` (~$0.29/iter using the new flag; vs ~$2.50 for full-6-chunk re-dispatch). **6/6 converged on `value: 24`** matching CPI MODERATE oracle exactly. Cost $0.2931.
- **Iter 2 (ablation: keep CPI vocab, append units):** restored CPI's verbatim YES/MODERATE/NO text in front + kept the units instruction at the end. **6/6 ALSO converged on `value: 24`.** Cost $0.2891. Cumulative $0.5822.
- **Load-bearing finding:** the cell-type-aligned units instruction is the load-bearing fix; CPI's YES/MODERATE/NO vocab is **harmless** when paired with that instruction. Both prompt directions converge identically. This means **Phase A pre-flight YAML audit's pattern is ADDITIVE, not destructive** — append a cell-type-aligned instruction to every prompt with tier vocabulary against a typed cell. Don't strip the rubric scoring language (preserves provenance, doesn't sacrifice convergence). Substantially smaller Phase A scope than the wide-pass Commit 3 convo anticipated.
- **Other observations:** Claude's iter-2 justifications cite both §13.63(1)(a) AND §13.64(2) (iter 1 only cited §13.63); GPT stays with just §13.63 across both iters. Same value, slightly richer justifications in iter 2. Production YAML now holds iter 2's prompt (recommended Phase A pattern).
- **Generalizability not yet established.** Pattern confirmed on IntCell (CPI IND_199). Other 3 wide-pass-failure rows use EnumCell, DecimalCell-non-negative, BinaryCell — each ~$0.30 to test; ~$0.90 total to confirm generalization across cell types. Also: did NOT check the OTHER 17 CPI-readable rows for silent unit-mismatch issues (the GPT-emits-`2`-instead-of-`24` pattern that passes instantiation but is semantically wrong — wide-pass Commit 3 audit wouldn't have caught these).
- **Per-iteration cost ~$0.29, not $0.05-0.10 as plan estimated.** registration_mechanics_and_exemptions has 8 legal cells, so dispatch cost scales linearly with chunk size. At ~$0.29/iter, $5 budget supports ~17 iterations. Per-cell cost ~$0.03-0.04 per model per run.
- **Spend ledger:** wi-ralph cumulative **$0.5822** (against $3-5 budget, $2.42-$4.42 remaining). wi-tier1-direct-read cumulative unchanged at $7.2946. Grand total WI Phase 1/2 + Phase B spend: $7.8768.
- **Next session candidates:** (a) second-row trial on `lobbyist_spending_report_filing_cadence` to test pattern across EnumCell (~$0.30); (b) silent-unit-mismatch sweep on the 17 OTHER CPI-readable rows (~$2-3); (c) Phase A pre-flight YAML audit at scale once the pattern is verified across cell types. Dan's call.

### 2026-06-04 (evening) — Phase B kickoff: branch created, plan written, no code/dispatch
Convo: [`convos/20260604_phase_b_kickoff.md`](convos/20260604_phase_b_kickoff.md) · Plan: [`plans/20260604_phase_b_ralph_renewal_cadence.md`](plans/20260604_phase_b_ralph_renewal_cadence.md)
- **Branch created** from main (`28f3e47`) as `wi-ralph-cpi-renewal-cadence` worktree at `/Users/dan/code/lobby_analysis/.worktrees/wi-ralph-cpi-renewal-cadence/`. Symlinked `data → /Users/dan/data/lobby_analysis` and `.env.local → /Users/dan/code/lobby_analysis/.env.local` per CLAUDE.md multi-terminal data discipline. Seeded `docs/active/<branch>/{convos,plans,results}/`.
- **Plan written** at `plans/20260604_phase_b_ralph_renewal_cadence.md`. Self-contained Phase B Ralph plan targeting `lobbyist_registration_renewal_cadence` (CPI IND_199, WI=MODERATE, cell type `Optional[int_months]`). Plan covers: pre-flight reads (6 docs spanning this branch + wi-tier1-direct-read), target/oracle/starting-state spec, 3 dispatch-mechanism options (A=add `--chunks` flag $0.05-0.10/iter recommended; B=full re-dispatch ~$2.50/iter at chunk-cost averages; C=single-row script deferred), iteration loop (read state → compare oracle → decide change → edit YAML → re-dispatch → audit → log), results-logging schema, 4 stopping conditions (convergence; $5 budget hit; Dan-calls-it; structural-finding), edge cases (cell-type-vs-vocab is structural not just lexical; GPT vs Claude unit-axis behavior; cross-vintage drift; CPI data-quality glitches; chunk-mate row spillover; **branch starting state — wide-pass YAML lives on wi-tier1-direct-read, NOT main**), 4 pre-execution Dan questions, what's out of scope, optional test plan if Option A's dispatcher tweak is taken.
- **No code, no dispatch, no API spend** this session, per Dan's mid-session instruction: *"don't actually code Ralph up, just create a plan and then finish-convo."* This branch's cumulative spend ledger starts at $0.00.
- **Implementing-session shape:** agent reads plan + 6 pre-flight reads cold; confirms Dan's answers to the 4 questions (branch starting state, dispatch mechanism, vintage check, stopping rule); then iteration 0. Plan flags the merge step (`git merge wi-tier1-direct-read`) as the most likely first-action requirement since `compendium/source_quotes.yaml` on main lacks the wide-pass populated prompts.
- **Next:** implementing session. Plan deliberately stops short of execution — the first dollar of API spend waits on Dan's go-ahead to the dispatch mechanism choice.
