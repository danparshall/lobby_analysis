# STATUS — lobby_analysis

Last updated: 2026-06-02 (afternoon — `wi-allocation-matrix` Suhan-facing synthesis + LeMahieu SB 28 bill inspection + unknown-chamber audit; merge decision landed, branch entering `finishing-a-research-branch` workflow)

---

## ⭐ Compendium 2.0 success criterion (load-bearing)

The deliverable is a **populated data layer** — N canonical-question rows × 50 states × M vintages × {legal_availability, practical_availability} cells. The architecture commits to:

1. **ONE compendium.** A single canonical row set; no rubric-specific compendium variants.
2. **ONE extraction pipeline.** A single extraction methodology — same prompt structure, same model, same retrieval approach — applied uniformly across rows, states, and years. Not 9 different pipelines per rubric. The compendium row schema must be uniform enough that one extraction approach works for every row.
3. **Multi-year reliability.** The pipeline must extract reliably across vintages (e.g., OH 2010 + OH 2015 + OH 2024 + OH 2025), not just at one vintage.
4. **Per-rubric projections are SANITY CHECKS on extraction accuracy, not the goal.** Each source rubric (PRI 2010, CPI 2015, FOCAL 2024, Sunlight 2015, Newmark2005/2017, Opheim, HiredGuns, OpenSecrets) has a small projection function `f_rubric(compendium_cells_for_state_year) → rubric_score`. The published rubric per-state scores are **independent ground-truth yardsticks** against our extraction.

**Multi-rubric × multi-year validation = redundant per-row ground truth.** The same compendium row gets validated by every rubric that reads it (e.g., "expenditures benefitting officials" is read by Newmark2005 + Newmark2017 + Opheim + FOCAL.financials.10 — that's 4 independent checks on that one row's extraction). Different rubric vintages (PRI 2010, Sunlight 2015, CPI 2015, FOCAL 2024) validate different extraction vintages.

**Falsifiable test:** populate compendium for state S in vintage Y via the single extraction pipeline → apply each rubric's projection → compare each projected per-state score to the rubric's published score for (S, Y). Match within tolerance ⇒ extraction is sound on the rows that rubric reads, in vintage Y. **All rubrics must pass for the vintages they cover, on a sample of states.**

**Goal: minimum compendium where all rubrics still project correctly across all vintages.** Rows used by no rubric's projection are deletion candidates; rubrics that fail to project force missing rows in. Rows that pass on some rubrics but fail on others surface either a row-design issue or an extraction-pipeline issue — both are debuggable signals.

If we have a single compendium, with a single extraction prompt, that extracts reliably across multiple years, and every row is validated against multiple prior human-generated rubrics — we're in great shape.

This criterion was articulated 2026-05-07 after the 3-way × 3-run consensus pass and the CPI 2015 C11 fold-in. It sharpens the 2026-04-29 reframe ("compendium is the universe, rubrics are projections of it"). See `docs/active/compendium-source-extracts/RESEARCH_LOG.md` 2026-05-07 entry and the compare/contrast at `docs/active/compendium-source-extracts/results/20260507_cpi_2015_c11_vs_consensus.md`.

---

## Current Focus

**2026-05-24 update — Prong 1 paused; gather-first pivot ahead of v2.2 compendium.** First-state pilot on `extraction-harness-brainstorm` (OH 2025, Tier-1 + Tier-2) produced empirical evidence that frontier LLMs *can* read state lobbying statutes (high cross-vintage stability, low cost, no hallucinated citations) but the v2.1 typed-cell schema *cannot yet faithfully represent what statutes actually say* — most visibly on qualitative-trigger states like OH where the numeric `threshold_*` cells and `TimeThresholdCell` force a lossy encoding for the "main purpose" lobbyist gate (Tier-2 Step D writeup: "model right, schema can't represent the answer"). All three in-flight Prong 1 branches — `phase-c-projection-tdd` (5 of 8 rubric projections + 2 of 4 FOCAL plans shipped; HG + Opheim deferred on vintage), `extraction-harness-brainstorm` (Tier-0 + Tier-1 + Tier-2 harness), `oh-statute-retrieval` (stub) — merged to main and archived to `docs/historical/`. **Pivot: gather-first.** Collect per-(state, vintage, question) answers in a flexible intermediate JSON (freeform answer + statute citation + confidence) across all priority states, then design the **v2.2** typed schema from observed reality. v2.1 (current frozen 181-row TSV with column-rename refinements) remains the immediate reference. See README "Research question" + `docs/RESEARCH_ARC.md` "Status" header. Prong 2 (portal extraction) + Prong 3 (display) are unaffected; product focus continues there.

**Architectural pivot landed 2026-04-22:** project identity is **data infrastructure** (live, bulk-downloadable, cross-state lobbying disclosure data — LobbyView-for-states filling the 19-state gap OpenSecrets's 2022 scorecard acknowledges), with an N × 50 × 2 matrix (Required × {Legally Available, Practically Available}) as companion output. The field compendium unions 9+ source frameworks; the project **catalogs, does not grade**. No "Corda Rubric."

**Data model v1.1 shipped** (2026-04-22): generic `FrameworkReference` replacing hardcoded PRI/FOCAL IDs; availability axes + `EvidenceSource` on `FieldRequirement`; first-class `CompendiumItem` + `MatrixCell`; `ExtractionCapability` for per-state pipeline capability. The schema is the contract for both tracks below.

**Compendium populated + first multi-vintage-same-state SMR shipped (2026-04-30):** `statute-retrieval` merged to main with 118-row compendium, `cmd_build_smr` projection subcommand, and OH 2010 + OH 2025 SMRs (PRI-projection MVP). OH disclosure law structurally near-unchanged 2010 → 2025 as PRI projection sees it. Mid-session reframe: PRI's diagnostic yes/no rubric is *not* the right product shape for downstream filing extraction; the compendium-keyed `field_requirements` row IS, but the 22 fields populated by PRI projection are a thin slice of what the statute actually requires. Successor branches now split.

**Work splits into two tracks going forward:**

1. **Track A — Filing-schema-first extraction harness.** Replace the PRI-projection data source with direct statute extraction: required filings × required fields × cadence × triggers × cross-references. PRI/FOCAL/Sunlight become diagnostic *projections* of the schema, not data sources for it. The compendium-keyed `field_requirements` shape is preserved; only the data source changes. Successor branch to `statute-retrieval`; calibration anchor is the OH 2025 PRI-projection SMR (the new harness must be at least as comprehensive plus capture the dropped legal complexity).

2. **Track B — Pull disclosure data** (portal scraping + LLM extraction → `LobbyingFiling`). Unblocked by the OH SMR existing as a downstream contract. Other fellow has started this on `oh-portal-extraction` (not investigated this session). Portal snapshot corpus (981 artifacts, all 50 states) under `docs/historical/pri-2026-rescore/`.

**Fellow-to-track assignments pending** — Dan decides.

**Recently shipped (2026-04-22):** Fellow-facing landscape report at `docs/LANDSCAPE.md` (synthesis of 5 architectural pieces + 4-axis framing + positioning vs. OpenSecrets / Sunlight / LobbyView / CSG-Book-of-States, with Layer 1 diagram and verified OCDEP list). README "real-time" → "up-to-date" swap also landed (plus the equivalent swap in `CLAUDE.md` and the archived `pri_2026_methodology.md`) to match the framing set in `LANDSCAPE.md` §2.

**2026-05-02 pivot — compendium rebuild from sources, no PRI.** New active branch `compendium-source-extracts` off `origin/main`. The compendium-as-shipped (141 rows) is structurally PRI-shaped despite D9's vocabulary fix; the v3 audit on `statute-extraction` (186 concerns / 24.2% inter-auditor agreement) became evidence that patching is the wrong shape of work. Posture: re-extract indicator items from each non-PRI source paper independently (7 papers — Opheim 1991, Newmark 2005, CPI Hired Guns 2007, Sunlight 2015, Newmark 2017, OpenSecrets 2022, FOCAL/Lacy-Nichols 2024); user reviews each extract personally; design data-object-2.0 only after all reviews are in. Plan: [`docs/historical/compendium-source-extracts/plans/20260502_per_paper_source_extraction.md`](docs/historical/compendium-source-extracts/plans/20260502_per_paper_source_extraction.md). The harness work on `statute-extraction` (iter-2 onward) **was paused** until 2.0 landed.

**2026-05-14 update — compendium 2.0 landed and archived; 3 successors cut.** `compendium-source-extracts` merged to main as `cac1469` (2026-05-14) with the v2 row-freeze contract (181 rows) + 9 projection mappings + factual-audit-clean source-rubric summaries. The branch is now archived to `docs/historical/compendium-source-extracts/`. Three successor branches cut off main per Option B (locked 2026-05-13): `oh-statute-retrieval` (Track A — multi-vintage OH statute bundles + HG 2007 ground-truth retrieval), `extraction-harness-brainstorm` (Track B — design the single extraction pipeline per the Compendium 2.0 success criterion), `phase-c-projection-tdd` (TDD per-rubric projection functions in the locked rubric order). Each successor branch has a seeded `docs/active/<branch>/` skeleton (RESEARCH_LOG + empty convos/plans/results); no kickoff plans yet — those are each kickoff session's work. Also (2026-05-14, pre-archive): `data/compendium/` → repo-root `compendium/` (commit `7a107ea`) so `data/` is fully gitignored and the worktree data-symlink convention works without conflict.

**2026-05-14 update (later) — v2-promote landed; `⛔` PRI-out-of-bounds banner cleared.** Coordination check between the two parallel-running compendium-consumer branches (`extraction-harness-brainstorm` and `phase-c-projection-tdd`) surfaced a shared-infrastructure conflict surface that would otherwise force one branch to do v1→v2 deprecation work the other would step on. Cut `compendium-v2-promote` off main to land the deprecation in one place first: v1 CSVs moved to `compendium/_deprecated/v1/` (preserved, not deleted, per "mv over rm for research artifacts"); v2 TSV promoted from `docs/historical/compendium-source-extracts/results/projections/disclosure_side_compendium_items_v2.tsv` to repo-level `compendium/disclosure_side_compendium_items_v2.tsv`; `.MOVED.md` forwarding stub left at old path with update instructions; `load_compendium` renamed to `load_v1_compendium_deprecated` with deprecation docstring; new minimal `load_v2_compendium() → list[dict[str, str]]` added (raw dicts on purpose — typed Pydantic v2 models belong to `extraction-harness-brainstorm`'s surgery per the model-shape = extraction-output-shape principle). Same session also cleared the `⛔ AGENT-CRITICAL: PRI 2010 IS OUT OF BOUNDS` banner: Compendium 2.0 structurally treats PRI as 1 of 8 score-projection rubrics on even footing (see ⭐ success criterion #4); the substantive "no rubric privileged" content survives in that criterion. Banner was emergency posture for the bootstrapping-from-PRI failure mode; that mode is structurally resolved by the v2 cell-typed observable shape. **Phase C now operates** — `papers/text/PRI_2010__state_lobbying_disclosure.txt` is reading material for the PRI 2010 projection function (rubric #2 in the locked Phase C order), no longer rule-1-blocked.

## Active Research Lines

| Branch | Started | Focus | Status |
|--------|---------|-------|--------|
| wi-allocation-matrix | 2026-05-30 | Build the {principal, lobbyist, lawmaker, bill} influence graph for WI 2025-2026 in response to Suhan's "company → lobbyist → lawmaker → bill" ask. Three legs: (1) bipartite matrix completion via IPF on the WI release to infer `lobbyist→bill` from principal-side row sums + lobbyist-side column sums; (2) WI Legislature scrape (likely via OpenStates) for `lawmaker→bill` sponsorship; (3) WI CFIS scoping subtask (write-up only this branch) for the `principal→lawmaker` $-edge. **Phase 0+1 (audit + loaders + graph) landed 2026-05-30; Phase 2 (IPF + materialize) landed 2026-05-31; Phase 3 (bill loader + chain composer + materialize) landed 2026-06-01; Phase 3.1 refinement (per-sponsor normalization + `item_id` disambiguator) landed 2026-06-02.** Phase 3 pivot: abandoned `pyopenstates` / OpenStates API path for Plural Policy bulk CSV after probing surfaced rate-limit constraints (10/min + 500 records/day) AND structural advantages (CSV `bill_sponsorships` table has `person_id` for 99.8% of rows; JSON `sponsors` was bare name strings). Sponsor scope is primary-only (cosponsors structurally absent from any structured field — only in `bill_actions.description` text; deferred to Phase 3+). Chain output `data/allocations/WI/WI_chain_2025.tsv` is 115,229 rows × 15 columns (was 12 pre-3.1); 97.9% coverage of unique legislative effort rows (plan §217 ≥80% bar cleared by wide margin). DoorDash worked example end-to-end: 78 rows; arithmetic spot-checked. Pettack-not-in-chain finding: `aggregation_flagged` (Phase 2) propagates correctly through allocation matrix but produces 0 chain rows because her SAA-family principals didn't file legislative-bucket bill efforts. 4 Phase 3+ refinement candidates flagged at v1: TNYB bucket inclusion, cosponsor parsing, per-cell row-residual exposure, per-bill-normalized top-sponsor metric. **#4 landed in Phase 3.1** as additive schema bump (`num_sponsors_on_bill`, `modeled_hours_per_sponsor`, `item_id`); old `modeled_hours` preserved for backward-compat. Top-sponsor table reverses sign once the normalization is applied: old 10/10 Assembly (Mursau, O'Connor, Dittrich; 10-15k inflated hrs each) → new 8/10 Senate (Cabral-Guevara 1,848.7 / James 1,510 / Tomczyk 1,506.7 / Nass 1,421.9 / Feyen 1,414.1 / Testin / Wanggaard / LeMahieu 1,081 on only 4 bills); chamber lower:upper drops from 3.4× to 1.2×. Also: 3 cases of WI bill-id collision (multiple distinct `item_id`s under same canonical `bill_id` like "AB 1") surfaced under principal 11473; `item_id` in TSV resolves. 1,288 hr (2.6%) of `modeled_hours_per_sponsor` lands in "unknown" chamber bucket — likely collective entities + name-normalization gaps. 20 Phase 3 tests + 6 Phase 3.1 tests + zero regressions (full suite 1,636 passed + 3 baseline failures). | Phase 3.1 landed 2026-06-02; **paused for Dan review of new top-sponsor profile** — Phase 4 (CFIS scoping, write-only), cosponsor parsing (refinement #2), "unknown" chamber audit (~30 min), and Phase 5 (PR) all remain |
| compendium-v2-promote | 2026-05-14 | Single-session infrastructure branch landing the v1→v2 deprecation work in one place before either compendium-consumer branch (`extraction-harness-brainstorm`, `phase-c-projection-tdd`) edits shared infrastructure. Promotes v2 TSV from `docs/historical/` to repo-level `compendium/`; moves v1 CSVs to `compendium/_deprecated/v1/`; renames `load_compendium` → `load_v1_compendium_deprecated`; adds minimal `load_v2_compendium()` returning raw `list[dict[str, str]]` (typed models deferred to `extraction-harness-brainstorm`). Also removes the `⛔ PRI-out-of-bounds` banner from STATUS.md — Compendium 2.0 structurally treats PRI as 1 of 8 rubrics on even footing (⭐ success criterion #4). | In progress 2026-05-14; expected to merge same-day |
| oh-portal-extraction | (pre-2026-04-30) | (other fellow's branch — not investigated this session) | Not investigated |
| filing-schema-extraction | 2026-04-30 | Successor to statute-retrieval per the 2026-04-30 reframe — replace the PRI-projection MVP data source with direct filing-schema extraction from statute text. **v2 audit + v1.2 schema bump executed 2026-04-30 → 2026-05-01:** walked 5 previously-untouched rubrics (Newmark 2017/2005, Opheim 1991, CPI Hired Guns 2007, OpenSecrets 2022); compendium 118 → **141 rows** (139 post-v2 + 2 v1.2 symmetry-gap rows); added new `domain="definitions"` value (7 rows promoted/added) per Decision Log D11. Durable audit report at repo-level `docs/COMPENDIUM_AUDIT.md` (moved from branch results/ per its lifecycle on PR #5 merge, 2026-05-01). Kickoff plan re-scoped to **108-row statute-side MVP target** (141 total = 108 statute-side + 33 accessibility-side). All 7 notes-flagged rows resolved. Harness brainstorm next. | v2 audit + v1.2 schema bump complete; harness brainstorm pending |
| statute-extraction | 2026-05-01 | Successor to filing-schema-extraction. Built v2 scorer prompt + extraction harness; iter-1 dispatched against OH 2025 `definitions` chunk (3 temp-0 runs, 93.3% agreement). Iter-2 prep shipped (chunk-frame preamble + tightened descriptions). v3 compendium audit (Phase 0) ran 2026-05-02 with two parallel auditors + reconciliation: 186 concerns / 109 of 141 rows flagged / 24.2% agreement. **The audit became evidence the compendium is structurally PRI-shaped and needs rebuilding, not patching.** Phase 0 deliverable retained as historical evidence; Phase 1/2 superseded by `compendium-source-extracts`. Iter-2 dispatch and all further harness work paused. | Paused pending compendium-2.0 rebuild |

## Archived Research Lines

Lines moved to `docs/historical/` — not currently active, but available for reference.

| Branch | Summary | Archived | Material |
|--------|---------|----------|----------|
| wi-disclosure-explore | Wisconsin 2025-2026 disclosure-data ingest. **Tier-1 shipped:** lobbyist-side + principal-side authorization scrape with unification — 774 lobbyists + 944 principals fetched from `lobbying.wi.gov` (5 modules under `src/lobby_analysis/io/wi/`: parser, ID discovery, fetcher with checkpoint/resume, materialize, unification); 2,254 authorization rows materialized to a unified `WI_lobbyist_principal_authorizations_unified.tsv` with `discovered_via ∈ {lobbyist, principal, both}` + `lobbyist_in_grid` provenance; principal-side scrape is a strict superset of lobbyist-side on the 2026-05-26 snapshot (+3 edges); 40-principal directory gap fully explained (38 ceased + 2 privacy-redacted low-spend-pledge); **2 Schlaak-class lobbyists** enumerated (12694 Schlaak / WCTA, 11513 Steinbruecker / ACLU — the NEW case refutes the simple "filter = not surrendered" rule). **Tier-2 shipped:** principal_meta + lobbyist_time_report parsers + materializer + CLI; v1.2 schema bump on `LobbyingFiling` (added `total_hours_communicating` + `total_hours_other`); 6 output TSVs at **944 Orgs / 773 People / 1,706 expenditure rows / 3,092 activity rows / 7,345 bill-effort rows / 0 parse failures** in 19.2 s wall; headline aggregates $47.46M total principal-side spend, DoorDash $2.18M outlier; bucket distribution Legislative Bills 54.9% / Topics-Not-Yet 31.7% / Budget Bill 11.7% / Admin Rule 1.7%. **Final session:** pre-merge code review surfaced a real address-parser BLOCKER (phone + firm-name leaked into the typed `address` ContactDetail across both parsers); fixed under TDD (4 RED → 4 GREEN on Dairy / Lexia / Brooks / Pfaff fixtures); also fixed a `.gitignore` gap on per-worktree `data` symlinks. First PR (#29) merged at `33b8793` 2026-05-27 then reverted via force-push at user request so a fresh agent could re-run `finishing-a-development-branch` cleanly including this archive sweep; re-merged via a fresh PR with the archive housekeeping included. **Full suite at merge: 1541 pass + 3 pre-existing baseline failures + 3 skipped + 3 xfailed.** WI sets the convention for downstream state-extraction lines: `WI-{principal\|lobbyist}-{id}` ID scheme for `Organization.id` / `Person.id`, NC-parallel flat data layout under `data/disclosures/WI/`. **Follow-ups on dedicated branches:** `wi-data-root-env` (5 CLIs have hardcoded paths), `wi-xlrd-swap` (drop pandas dep), `wi-shared-table-helpers` (`_cell_value_text` + `_extract_optional_date` duplicated), `wi-materializer-error-discipline` (ParseFailure-vs-crash inconsistency between auth + tier_2 materializers); held over orthogonal: `lobbying@wi.gov` reply (Dan handling, only path to explaining Schlaak's grid exclusion), SAL parser/ingest (data at `WI_directory_state_agency_liaisons.xls`, 2,599 rows × 13 cols, not yet wired), cross-session `principal_id` stability for biennial time-series. | 2026-05-27 | `docs/historical/wi-disclosure-explore/` |
| phase-c-projection-tdd | Phase C TDD branch implementing per-rubric projection functions (`f_rubric(cells, vintage) → rubric_score`) in deterministic Python. **Shipped 5 of 8 projections:** CPI 2015 C11, PRI 2010, Sunlight 2015, Newmark 2017, Newmark 2005 — each with per-item helpers + aggregation dispatcher + tests against published per-state ground truth where available. **FOCAL 2024 partially shipped:** Plan 1 (legal-core, 27 items, US LDA verified raw 23 / weighted 81) + Plan 2 (contact_log, 11 items including OR-pair for bill-id α-split, US LDA raw `[1,0,1,0,0,0,0,0,1,0,1]`) of 4. Plans 3 (openness+timeliness, 12 items, first to read practical-axis) and 4 (top-level projector + aggregation + US LDA validation harness with ±15 raw tolerance) pending. **HG 2007 deferred 2026-05-21** — vintage finding: the rubric is actually a May 2003 CPI survey, not 2007 (L-N 2024/2025 cite the methodology page's 2007 'last updated' stamp as the methodology date); Justia 2003 coverage is ~7-8 states (most are 2005-2006), so 2003-vintage validation isn't tractable yet. **Opheim 1991 blocked** on 1988-89 statute data. Merged + archived 2026-05-24 as part of the Prong 1 pause + gather-first pivot. Shipped projections are formal specifications of each rubric's substantive scoring rule, re-adaptable to v2.2 cells when Prong 1 resumes. FOCAL Plans 3+4, HG, Opheim are pickup items for the next Prong 1 chapter. | 2026-05-24 | `docs/historical/phase-c-projection-tdd/` |
| extraction-harness-brainstorm | Built the first concrete extraction harness against v2.1 typed cells. **Shipped:** `models_v2/` (frozen `CompendiumCell` ABC + 15 typed subclasses + 186-cell `CompendiumCellSpec` registry); `chunks_v2/` (hand-curated 15-chunk partition of all 181 rows / 186 cells); `retrieval_v2/` (Anthropic Citations API + cross-reference walker with `record_cross_reference` / `record_unresolvable_reference` tools); `src/scoring/scorer_prompt_v2.md` + `chunk_frames_v2/` scaffolding. **Ran Tier-0 smoke (2026-05-19):** 4 cells, OH 2025 enforcement_and_audits chunk, both Claude Opus 4.7 + GPT-5.2, ~$0.10, surfaced the value-typing bug Tier-1 fixed. **Ran Tier-1 legal-axis (2026-05-21):** OH 2025, 84 legal cells × 2 models × 3 runs = 36 dispatches, $2.94 / ceiling $10. σ_noise Claude 85.7% / GPT 73.8% (both deflated by unpinned enum-label churn and 3 schema/adapter error classes; true model-reasoning noise floor is higher). Hand-eyeball confirmed all sampled `cited_section` values real (`§101.70`, `§101.72`, `§101.73`, `§101.79`, `§101.99` of OH Chapter 101). **Ran Tier-2 (2026-05-21):** Fixes A (int→Decimal coercion), B (dict-shape value roster hint), C (null FreeTextCell → abstention) shipped under TDD; Step D re-dispatch verified A and C against real API output, original class-B `TypeError` cleared. **Step D also surfaced the structural finding that drove the gather-first pivot:** `TimeThresholdCell` cannot represent OH's qualitative "main purposes" lobbyist trigger (no numeric time threshold exists to encode); Claude under-abstains by emitting out-of-domain `unit` values, GPT correctly abstains. Writeup verdict: *"model right, schema can't represent the answer."* Merged + archived 2026-05-24 as part of the Prong 1 pause + gather-first pivot. The dispatch/chunking/parser/multi-model infrastructure is reusable; the schema-first assumption at extraction time is what the v2.2 design pass will revisit. | 2026-05-24 | `docs/historical/extraction-harness-brainstorm/` |
| oh-statute-retrieval | Stub branch. `docs/active/` skeleton seeded 2026-05-14 with intended scope: 4-vintage OH statute bundles (2007, 2010, 2015, 2025) for multi-year reliability validation per Compendium 2.0 success criterion #3, plus HG 2007 ground-truth retrieval as a sub-task feeding `phase-c-projection-tdd`. No implementation sessions ran. The OH 2010/2015/2025 statute data on disk (under `~/data/lobby_analysis/statutes/OH/`) came from prior archived branches (`statute-retrieval`, `pri-calibration`) and remains available. The HG 2007 vintage finding on `phase-c-projection-tdd` (rubric is actually a 2003 CPI survey, not 2007) effectively removed the urgency of the OH 2007 sub-task — 2003-vintage statute retrieval is a separate research line. Merged + archived 2026-05-24 as part of the Prong 1 pause. | 2026-05-24 | `docs/historical/oh-statute-retrieval/` |
| compendium-v2-promote | 2026-05-14 | Single-session infrastructure branch landing the v1→v2 deprecation work in one place before either compendium-consumer branch (`extraction-harness-brainstorm`, `phase-c-projection-tdd`) edits shared infrastructure. Promotes v2 TSV from `docs/historical/` to repo-level `compendium/`; moves v1 CSVs to `compendium/_deprecated/v1/`; renames `load_compendium` → `load_v1_compendium_deprecated`; adds minimal `load_v2_compendium()` returning raw `list[dict[str, str]]` (typed models deferred to `extraction-harness-brainstorm`). Also removes the `⛔ PRI-out-of-bounds` banner from STATUS.md — Compendium 2.0 structurally treats PRI as 1 of 8 rubrics on even footing (⭐ success criterion #4). | In progress 2026-05-14; expected to merge same-day |
| compendium-row-id-renames | Execution branch for the 15 row-ID renames + 1 README typo fix accepted on `compendium-naming-docs`. Architecture: standalone find-and-replace tool (`tools/v2_update_names.py` + `src/lobby_analysis/row_id_renamer.py` with `RENAMES` dict) that sister branches run after merging main. **No v3** — Dan's "retroactively re-finalize v2" framing: filename `disclosure_side_compendium_items_v2.tsv` unchanged, content updated in place; this is a within-v2 contract refinement, not a generation bump. Skip-rules protect the renamer module + its tests (self-reference), `compendium/NAMING_CONVENTIONS.md` (surgical §10-DONE + §10.1 resolver hand-edit), `STATUS.md` (historical narrative), and `docs/historical/` + `compendium/_deprecated/` archives. Merged to main via PR [#14](https://github.com/danparshall/lobby_analysis/pull/14) 2026-05-14. Sister-branch absorption completed 2026-05-15: `extraction-harness-brainstorm` `151bad2`, `phase-c-projection-tdd` `a83ede3`, `oh-statute-retrieval` `54908f5`. Renamer skip-list defect surfaced + fixed via PR [#15](https://github.com/danparshall/lobby_analysis/pull/15) (added `docs/active/compendium-row-id-renames` to `_SKIP_SUBPATHS` to preserve the substring-trap example in the rename-execution convo). | 2026-05-15 | `docs/historical/compendium-row-id-renames/` |
| compendium-naming-docs | Audit branch for [GH issue #9](https://github.com/danparshall/lobby_analysis/issues/9) — the v2 row-naming taxonomy. Walked all 181 `compendium_row_id`s; produced `compendium/NAMING_CONVENTIONS.md` (13 sections: family taxonomy, α form-type split, three-threshold framework, def/actor triangle, prefix-choice decision tree) plus reproducible prefix-survey + per-row-provenance scripts. Scope locked at kickoff to **audit + flag, defer renames**. All 8 rename candidates ratified across 3 sessions (kickoff → walkthrough → sharpening pass): 15 row renames + 1 README typo fix; C3 unit-suffix rule (`_threshold_<measure>_<unit>`) memorialized in §7; LV-1 schema-coverage exception memorialized in §5 + §11. Merged to main via PR [#10](https://github.com/danparshall/lobby_analysis/pull/10) 2026-05-14. Execution moved to successor branch `compendium-row-id-renames`. | 2026-05-14 | `docs/historical/compendium-naming-docs/` |
| compendium-source-extracts | Rebuilt the compendium from non-PRI sources after the 141-row v1.2 compendium was found structurally PRI-shaped (v3 audit on `statute-extraction`: 186 concerns / 24.2% inter-auditor agreement). **26 papers extracted** (7 originals + 14 predecessors + 5 author-hunt); cross-rubric descriptives + embedding pass + 3-way × 3-run consensus pass (24 strict clusters / 63 items at ≥8/9; M2-blind most stable at 14.4%). **Projection-success criterion landed 2026-05-07** — ONE compendium + ONE extraction pipeline + multi-year reliability + per-rubric projections as sanity checks against published per-state scores. **Phase A complete:** 4 parallel atomic-item audits (OpenSecrets KEEP partial, LobbyView KEEP 46 fields, Sunlight 4-of-5, FOCAL fully populated with verbatim L-N 2025 Suppl File 1 weights — 1,372-cell ground truth). **Phase B complete (9 of 9 mappings):** CPI 2015 C11 → PRI 2010 → Sunlight 2015 → OpenSecrets 2022 (TABLED, no per-tier scoring def) → Newmark 2017 → Newmark 2005 → Opheim 1991 → HG 2007 → FOCAL 2024 → LobbyView 2018/2025 (schema-coverage, not score-projection). **Compendium 2.0 row-freeze landed 2026-05-13:** v2 TSV `results/projections/disclosure_side_compendium_items_v2.tsv` — **181 rows** (180 firm + 1 path-b unvalidated; down from 186 v1). Tier distribution: 8-rubric × 1 (`lobbyist_spending_report_includes_total_compensation` — most-validated row), 6-rubric × 4, 5-rubric × 3, 4-rubric × 6, 3-rubric × 10, 2-rubric × 24, 1-rubric × 132, 0-rubric × 1. 30-decision log (D1-D30); idempotent regen via `tools/freeze_canonicalize_rows.py`. **Pre-merge factual audit (2026-05-13)** on 3 load-bearing source-rubric summaries (Newmark 2017, FOCAL, PRI 2010) confirmed accuracy: Newmark 2017 r=0.04 CPI↔PRI-disclosure correlation (load-bearing for unification rationale) verified exactly at paper p.421-422; PRI 2010's dense numerical claims all clean; FOCAL had 1 real correction (contact-log count restored 13/15) + 1 clarity tightening (openness 11/15 vs open-data sub-theme 9/15) + 1 Newmark 2017 sourcing nit noted (0.71 comparator from Newmark 2005, not 2017 PDF). Merged to main as `cac1469`. v2 TSV is the contract for 3 successor branches per Option B: `oh-statute-retrieval`, `extraction-harness-brainstorm`, `phase-c-projection-tdd`. | 2026-05-14 | `docs/historical/compendium-source-extracts/` |
| statute-retrieval | Two-pass retrieval pipeline (LLM-driven cross-reference discovery → scoring agent) + opus-4-7 scorer with files-read enforcement. **Stage A: populated `CompendiumItem` data layer for the first time** (118 rows from PRI 2010 disclosure ∪ PRI 2010 accessibility ∪ FOCAL 2024 ∪ Sunlight 2015 unique, with `framework_references` and `framework_dedup_map.csv` audit trail). **Stage B: `cmd_build_smr` orchestrator subcommand + `smr_projection` module** — projects per-item PRI scores through the populated compendium into a compendium-keyed `StateMasterRecord` JSON. **First multi-vintage-same-state SMR shipped:** OH 2010 (anchor) and OH 2025 (latest Justia-hosted vintage); 30-section bundle ~143 KB; 3 concurrent opus-4-7 runs gave 21.3% inter-run disagreement (flagged); SMR projected from r2 for 2010-convention continuity. Diff doc `docs/historical/statute-retrieval/results/20260430_oh_2025_vs_2010_diff.md`: structurally OH disclosure law is near-unchanged 2010 → 2025 as PRI projection sees it (one flip, `governors_office.required: False → True`; all field_requirements + reporting_parties + de_minimis nulls identical). Two known scorer blind spots flagged for the successor branch: qualitative materiality gates (§101.70(F) "main purposes" test) invisible to current scorer; conjunctive rubric items scored disjunctively under prompt drift. **The PRI-projection SMR is parking-orbit MVP** — the long-term shape (compendium-keyed `field_requirements`) is correct, but the data source (PRI yes/no diagnostic answers) is wrong for downstream filing extraction. Successor branch pivots to filing-schema-first extraction. | 2026-04-30 | `docs/historical/statute-retrieval/` |
| pri-calibration | LLM statute-reading QA harness against PRI 2010 human-rater scores. Built Justia-unified retrieval (49/50 states eligible at ±2yr tolerance, CO the exclusion), curated 5-state pilot subset (CA/TX/NY/WI/WY), Phase 2 statute bundle construction + retrieval pipeline (21 section files, ~260 KB), Phase 2b calibration harness with paper-derived PRI rollup rule (9 methodology differences documented), Phase 3 scaffolding + live baseline dispatch (30 subagents scoring 5 states × 3 runs × 2 PRI rubrics). **Baseline result: 0% exact-match agreement vs PRI 2010 on disclosure-law**; H1 (bundle scope too narrow — missing cross-referenced support chapters like TX §311.005(2) "person" definition) empirically confirmed via walkthrough. 2026-04-22 pm reflection reframed the branch charter: its stable role is internal LLM statute-reading QA, not public rubric output, and calibration should be multi-rubric (not PRI-only) per Newmark 2017's r=0.04 CPI↔PRI finding — single-rubric agreement could mask frame overfitting. Infrastructure (`justia_client`, `statute_retrieval`, `bundle`, `calibration`, `consistency`, orchestrator subcommands) is framework-agnostic and carries forward to Track A. | 2026-04-22 | `docs/historical/pri-calibration/` |
| scoring | Unified PRI + FOCAL scoring pipeline against the 2026-04-13 snapshot corpus. Phase 1 (infra: subagent-based scorer + orchestrator prepare-run/finalize-run/analyze-consistency subcommands) shipped. Phase 2 (CA dry-run) validated end-to-end. Phase 3 three-state pilot (CA/CO/WY × 3 temp-0 runs × 3 rubrics = 27 cells) completed 2026-04-17: PRI accessibility stable on CO/WY (3.4%–8.5%), flagged on CA (11.9% WAF-stub variance); PRI disclosure-law flagged on all 3 states (CA 37.7%, CO 11.5%, WY 11.5%); FOCAL stable except CO (13.0%). Phase 4 (50-state scale-up) paused — disclosure-law root cause was same-host snapshot capture excluding state code. Work continued on `pri-calibration` (now also archived). | 2026-04-22 | `docs/historical/scoring/` |
| pri-2026-rescore | Data-collection phase for the 2026 PRI rescore: transcribed PRI 2010 rubrics (22 accessibility + 61 disclosure-law atomic items) with per-state 2010 scores reconciled programmatically; drafted 2026 accessibility rubric (59 items = 22 retained + 37 new for bulk download, open API, data dictionary, persistent URLs, timestamp/freshness) and 2026 disclosure-law rubric (61 items with B1/B2 reverse-scoring explicit); discovered and snapshotted all 50 state portals (981 artifacts, ~350 MB, 40 clean / 8 partial WAF-SPA / 2 near-empty). Scoring handed off to the `scoring` branch per the 2026-04-14 unification decision. | 2026-04-22 | `docs/historical/pri-2026-rescore/` |
| focal-extraction | Extraction of FOCAL 2024's 50 indicators (Lacy-Nichols Table 3) into `focal_2024_indicators.csv` (50 indicators × 8 categories, three validation spot-checks passed) with methodology note. Key mid-branch reframing: PRI–FOCAL overlap is two-dimensional (both rubrics span statutory-content + portal-accessibility halves), not edge-level — so score both rubrics independently and defer any composite to post-data collaborator review. Scoring handed off to the `scoring` branch per the 2026-04-14 unification decision. | 2026-04-22 | `docs/historical/focal-extraction/` |
| data-model-v1.1 | Clean-break schema update over v1.0: (a) availability axes (`legal_availability` × `practical_availability`) and evidence-source fields on `FieldRequirement`; (b) generic `FrameworkReference` + `framework_references: list[...]` replacing hardcoded `pri_item_id` / `focal_indicator_id` / `pri_item_ids` (covers PRI 2010 × 2, FOCAL 2024/2026, Hired Guns, Newmark 2005/2017, Opheim, Sunlight, OpenSecrets, LDA); (c) new `CompendiumItem` + `MatrixCell` models (the N in N × 50 × 2); (d) new `ExtractionCapability` companion model for per-state pipeline capability. Full suite 128/128 green. Clean break was safe because no live v1.0 artifacts existed at cut. | 2026-04-22 | `docs/historical/data-model-v1.1/` |
| lobbying-data-model | Universal output schema for state extraction pipelines: 18 pydantic models across entities, filings, provenance, state-master modules. v0.1 shipped 2026-04-17 with 4-day feedback window; no changes requested; accepted as v1.0 on 2026-04-21. Schema is the contract for the 3-fellow × ~17-state extraction rollout. Lives in `src/lobby_analysis/models/` + 24-test validation suite. Superseded by `data-model-v1.1` (2026-04-22). | 2026-04-21 | `docs/historical/lobbying-data-model/` |
| research-prior-art | Scoping phase 1 — prior-art survey, state infrastructure tiers, OCD schema analysis, scoring-rubric landscape (PRI + FOCAL + F Minus + GAO), PRI and FOCAL implementation plans. Decision: depend on Open States (don't fork); adopt Popolo via OCDEP 5; score all 50 states on both PRI rubrics in a follow-up branch. | 2026-04-12 | `docs/historical/research-prior-art/` |

## Recent Sessions

(One-line session summaries, newest first)

- **2026-06-02 (afternoon)** — [wi-allocation-matrix] **Suhan-facing synthesis + LeMahieu SB 28 bill inspection + unknown-chamber audit; merge decision landed.** Drafted Suhan-facing standalone synthesis of Phases 0 → 3.1 (`results/20260602_wi_chain_synthesis.md`) so a project-lead audience can read the chain's findings and limitations without walking the 5 per-phase docs. **LeMahieu bill-level inspection forced retraction of a leadership-vehicle hypothesis:** the #8 sponsor ranking is **98.8% one bill** — SB 28, electric-transmission right-of-first-refusal legislation where LeMahieu is sole primary sponsor. 29 principals filed effort, concentrated in the electric-utility industry (ATC Management 331 hrs, WEC 134, WIEG 124, plus the major IOUs). Americans For Prosperity at #4 (86 hrs) — AFP historically opposes ROFR elsewhere, but WI lobbying data has no support/oppose field, so position direction can't be inferred. Generalized to a project-wide finding: **the chain detects coalition activity, not coalition composition.** Method-level lesson: per-sponsor metrics can compress single-bill signals into apparent broad patterns — do per-bill inspection before sponsor-level external claims. **Unknown-chamber audit forced a Phase 3.1 numbers correction:** the 1,288 hr "unknown" bucket was inflated by a fragile surname-based join (B./L./J. Jacobson, L. Johnson — 698 hrs misclassified). True unknown is 590 hr / 1.2%, **all Joint Legislative Council** (collective entity). Law Revision Committee — hypothesized to contribute — has zero chain rows in 2025. Corrected synthesis chamber rollup: Assembly 25,892 → 26,543; Senate 21,610 → 21,657; ratio 1.20× → 1.23×. Substantive headline unchanged. Production code unaffected (only writes the name column, doesn't join on it). **Three-branch merge discussion** across wi-disclosure-explore (done + merged) / wi-tier1-direct-read (orthogonal workstream, owned by other agent; includes the snapshot-date fix for the 3 baseline failures) / wi-allocation-matrix (ours, decision needed). Dan chose **option B / slice merge** via `finishing-a-research-branch`: PR current state now, CFIS scoping + cosponsor parsing → new branches off post-merge main. **Chain TSV published** at `releases/wi/chain/WI_chain_2025.tsv` (38 MB, 115K rows) + companion README documenting Plural Policy bulk-CSV dependency, extending the `releases/` convention from `5fcc6ac`. Pytest baseline unchanged at 1,636 pass + same 3 pre-existing `test_pipeline.py` baseline failures (CA snapshot missing at 2026-04-13; already fixed on wi-tier1-direct-read `a3bc1af`). Convo: [`convos/20260602_synthesis_audits_and_merge_decision.md`](docs/active/wi-allocation-matrix/convos/20260602_synthesis_audits_and_merge_decision.md). Results: synthesis, [`results/20260602_lemahieu_bill_inspection.md`](docs/active/wi-allocation-matrix/results/20260602_lemahieu_bill_inspection.md), [`results/20260602_unknown_chamber_audit.md`](docs/active/wi-allocation-matrix/results/20260602_unknown_chamber_audit.md). Chain release commit `6c595b5`. **Next:** finish the `finishing-a-research-branch` workflow (audit-docs → archive → PR → merge); post-merge cut CFIS scoping branch + cosponsor parsing branch.
- **2026-06-02** — [wi-allocation-matrix] **Phase 3.1 — per-sponsor normalization + bill-id-collision disambiguator landed; 6 new tests, zero regressions; chain TSV schema goes from 12 → 15 columns (additive, non-breaking).** Refinement candidate #1 from yesterday's Phase 3 writeup. Added `num_sponsors_on_bill` + `modeled_hours_per_sponsor` (= modeled_hours / num_sponsors) so `SUM GROUP BY sponsor` stops inflating by sponsor count; `modeled_hours` preserved unchanged for backward-compat per-bill aggregations. Mid-implementation: 3 of 10,290 conservation groups failed → discovered WI biennium-internal bill-id collisions (principal 11473 filed effort on multiple distinct "Assembly Bill 1"s with different `item_id`s — voter-ID vs education-assessment; same for AB 6 + AB 10). Dan picked "add item_id to chain TSV" from a 3-option survey to make the collision visible to consumers without changing emit behavior; `item_id` is the unique source-row identifier, `bill_id` is its OpenStates-normalized projection. Conservation test rewritten to group by `item_id` (the emit-cycle granularity). **Headline analytical result: chamber bias in top-sponsor table reverses sign.** Old top-10 was 10/10 Assembly (Mursau, O'Connor, Dittrich at top, each ~10-15k inflated modeled_hours). New top-20 is 8/10 Senate at the top — Cabral-Guevara (Senate, 1,848.7), James (1,510.0), Tomczyk (1,506.7), Nass (1,421.9), Feyen (1,414.1), Testin (1,293.2), Wanggaard (1,140.2), **LeMahieu (Senate Majority Leader, 1,081.2 on only 4 bills)**, Petersen, Mursau. Two distinct lobbying-target profiles: concentrated Senate primaries (65-123 bills, high per-sponsor weight because Senate bills have few primaries) vs broad-named Assembly co-authors (198-234 bills, low per-sponsor weight). LeMahieu at #8 on 4 bills is the structurally interesting outlier the old metric buried — leadership-vehicle bills with few co-authors. Chamber rollup: lower:upper drops from 3.44× (426k:124k inflated) to 1.20× (25.9k:21.6k honest). Grand total `modeled_hours_per_sponsor` = 48,789 hr (vs 561,625 inflated by sponsor count — the new total conserves total bill-allocated effort by construction). 1,288 hr (2.6%) of `modeled_hours_per_sponsor` lands in an "unknown" chamber bucket from `family_name` mismatch against `wi.csv` — likely 60 collective entities (Joint Legislative Council × 26, Law Revision Committee × 34) plus name-normalization gaps; flagged for ~30-min future diagnostic. **Full suite: 1,636 passed** (+6 from prior 1,630) + same 3 pre-existing `test_pipeline.py` baseline failures (scoring/snapshot-loader, archived-line-owned). Uniform-share is the v1.1 assumption flagged in the writeup; position-weighted attribution (lead author > 9th co-sponsor) is a v1.2 candidate. Convo: [`convos/20260602_phase_3_per_sponsor_normalization.md`](docs/active/wi-allocation-matrix/convos/20260602_phase_3_per_sponsor_normalization.md). Results: [`results/20260602_phase_3_1_per_sponsor_normalization.md`](docs/active/wi-allocation-matrix/results/20260602_phase_3_1_per_sponsor_normalization.md). **Next:** paused for Dan review of the new top-sponsor profile; Phase 4 (CFIS scoping, write-only) sharpens with the per-sponsor-honest join target; cosponsor parsing (refinement #2) is next natural in-chain refinement; "unknown" chamber audit is a small bite-sized follow-up.
- **2026-06-01** — [wi-allocation-matrix] **Phase 3 — bill sponsorship loader + end-to-end chain composer + materialize landed; 20 new tests; first end-to-end chain TSV on disk.** Plan step 32 Q1 (OpenStates vs scrape) answered (c) by Dan: OpenStates first, fall back. Probed `pyopenstates` path; abandoned for Plural Policy bulk CSV after three findings: OpenStates v3 enforces API key server-side, free tier is 10 req/min + 500 records/day (not the "1 rps" Dan remembered — actual rate limit is tighter than the ~1,000-bill workload requires), and `pyopenstates` swallows HTTP status + headers making rate-limit telemetry invisible. Bulk-CSV pivot landed Q1 + the lawmaker_id question + the cosponsor-handling question all together: 15 normalized CSV tables for WI 2025 + separate legislators dataset (`wi.csv`) with `ocd-person/...` IDs, structural `person_id` join on 99.8% of sponsorship rows, primary-only scope (cosponsors NOT in any structured field — confirmed across both JSON and CSV; only in `bill_actions.description` text). 60 collective-entity sponsors (Joint Legislative Council × 26, Law Revision Committee × 34) round-trip via `is_collective=True` + name-as-id. Committee assignment requires regex over action descriptions — `organization_id` only identifies the chamber, not the receiving committee. Chain stats: 115,229 rows (46,446 H1 + 68,783 H2); 525 unique principals, 511 unique lobbyists, 984 unique bills, 133 unique sponsors. Confidence dist: 87.8% `ipf_fit` / 8.0% `exact` / 4.2% `zero_filed`. **Plan §217 coverage validation: 97.9% (3,947 of 4,030 unique legislative effort rows produce ≥1 chain row, target ≥80%).** DoorDash worked example: 78 rows; lobbyist 11077 SB 256 H1 arithmetic verified (37.806 + 9.209 = 47.014; × 0.21 = 9.873 = `modeled_hours`). Pettack (11072) emits 0 chain rows — her SAA-family principals didn't file legislative-bucket bill efforts. `aggregation_flagged` label propagates through Phase 2 allocation matrix correctly but is invisible in the legislative-only v1 chain. Top 10 sponsors all Assembly — likely confound from multi-author Assembly bill primary sponsorships, not a clean "most-lobbied" signal. 4 Phase 3+ refinement candidates flagged: TNYB bucket inclusion (2,327 effort rows currently skipped), cosponsor parsing (half-day regex work, doubles rowcount), per-cell row-residual exposure (Phase 2 carryover), per-bill-normalized top-sponsor metric. 5 commits: `4b71c03` RED legislature, `f830230` GREEN legislature, `67f9c30` RED chain, `cfc8342` GREEN chain, `a64f710` materialize + CLI subcommand. Full suite **1,630 passed** + same 3 pre-existing `test_pipeline.py` baseline failures. Convo: [`convos/20260601_phase_3_kickoff_and_bulk_data_pivot.md`](docs/active/wi-allocation-matrix/convos/20260601_phase_3_kickoff_and_bulk_data_pivot.md). Results: [`results/20260601_phase_3_chain.md`](docs/active/wi-allocation-matrix/results/20260601_phase_3_chain.md). **Next:** paused for Dan review. Phase 4 (CFIS scoping, write-only) and Phase 5 (PR) remain.
- **2026-05-31** — [wi-allocation-matrix] **Phase 2 — IPF fit + materialize + CLI landed; 27 new tests, zero regressions; first sharable allocation matrix on disk.** Empirical ipfn probe before tests verified the API at giant-CC scale (312L × 523P × 1,441E converges in ~340 ms hours_comm) AND surfaced two findings that revised the plan: (1) **Pettack is in her own 6L × 6P × 11E CC, not the giant** — the plan and Phase 1 results had assumed exclusion mechanics inside the giant CC fit, but her actual CC has internally-balanced marginals (730.2 hrs comm both sides, 3,454.5 vs 3,466.8 other), so IPF converges cleanly with no marginal surgery. The "exclusion" collapses from a fit-modification question to a labeling question. (2) **Bipartite support encoded via seed = 1.0 on edges, 0.0 elsewhere** — ipfn preserves zeros at machine precision (max leak = 0.0), no explicit constraint handling needed. Confidence-label schema negotiated with Dan: `exact` (singleton CC), `ipf_fit` (free CC, nonzero marginal, not flagged), `zero_filed` (lobbyist filed 0 hrs — DELIBERATELY preserved per Dan's call so downstream consumers can spot "principal paid, lobbyist filed 0" patterns), `aggregation_flagged` (Pettack-class). Pushback exchange with Dan on Pettack labeling: avoided "illegal" framing without WI §13.62 evidence; landed on descriptive `aggregation_flagged`. Phase 2 also added `min_hours_for_ratio_flag` param to `flag_outliers` (Phase 1 follow-up, live default 10.0 in `fit_all` — suppresses the 4 H1/H2 tiny-hours false positives). Surfaced a Phase 3+ candidate: **small-CC over-attribution pattern** — 8 H1 CCs have principal-side hours exceeding the sum of their authorized lobbyists' filed hours, IPF assigns all of it to the one filing lobbyist (CC #4: 19.9 prin hrs / 14 lob hrs → 42% over-attribution). Mathematically correct but invisible in the `ipf_fit` label; flagged but not fixed this phase. **Materialized output:** `data/allocations/WI/WI_lobbyist_principal_hours_h{1,2}_2025.tsv` with schema `(lobbyist_id, principal_id, hours_comm, hours_other, confidence)` — 1,912 H1 / 2,055 H2 rows. H1 confidence dist: 6.4% `exact` / 90.8% `ipf_fit` / 2.5% `zero_filed` / 0.3% `aggregation_flagged`. Hand-spot-check 4 cells: DoorDash principal-col-sum EXACTLY matches source (83.90 == 83.9, 87.50 == 87.5). 5 commits this session (`945cefb` `min_hours` param, `6a6ae63` RED IPF, `5bfb476` GREEN IPF, `fc08b2a` materialize, `03702b8` CLI + stdout cleanup). Full suite at **1,610 passed** + same 3 pre-existing `test_pipeline.py` baseline failures. Convo: [`convos/20260531_phase_2_ipf_design_and_execution.md`](docs/active/wi-allocation-matrix/convos/20260531_phase_2_ipf_design_and_execution.md). Results: [`results/20260531_phase_2_ipf_fit.md`](docs/active/wi-allocation-matrix/results/20260531_phase_2_ipf_fit.md). **Next:** Phase 3 (bill sponsorship scrape + chain composition) — pause immediately at plan step 32 to ask Dan Q1 (OpenStates vs direct WI Legislature scrape).

- **2026-05-30 (afternoon → evening)** — [wi-allocation-matrix] **Executed Phase 0 (audit, no code) + Phase 1 (TDD: loaders + bipartite graph + CC decomposition + outlier flagging).** Phase 0 surfaced 4 findings reshaping the plan: lobbyist filings are semester-granular (release README mislabels as quarterly — source code at `tier_2_materialize.py:12` is correct), moots the convo's "time-granularity mismatch" framing; percent-rounding is structural (only 41% of 1,428 (principal, period) groups sum to 100%, max 100% — asymmetric undercounting that adds Q6 for Phase 3 boundary); active edges per semester ~1,912 H1 / 2,055 H2 (not 2,254 biennium-union); one giant 835-node CC dominates H1 (900-node in H2) — confirms the plan's "What could change" worst case. Phase 1 landed loaders (4 fns, semester="2025-H1" string form, null-`authorized_on` exclusion + orphan-lobbyist-faithful) and graph layer (NetworkX-backed CC decomp; BipartiteGraph/Component/ExactlyPinned/FreeComponent/OutlierFlag dataclasses; orphan-lobbyist singletons fall back to principal marginal) via strict RED→GREEN→writeup cycle. **Most surprising Phase 1 finding: the plan's marginal-ratio outlier heuristic alone does NOT catch Pettack** — her 6 SAA-family principals (10960, 10963, 10965, 10966, 10967, 11211) collectively have 4,197 H1 marginal hrs, "explaining" her 4,007.5 hrs at ratio 0.95×. The impossibility is per-day arithmetic (32 hrs/day for one person). I added a per-semester absolute check (>2000 hrs ≈ 16 hrs/day non-human) to `flag_outliers`; that's what catches her. **4 low-hours ratio-flag false positives** (lobbyists with <10 hrs against zero-marginal low-spend-exempt principals) — Dan approved adding `min_hours_for_ratio_flag` param (default 0, recommended 10) in Phase 2. Test counts: 1,578 pass (= baseline 1,541 + 37 new = 20 load + 17 graph) + 3 known `test_pipeline.py` baseline failures, zero regressions. New dep: `networkx>=3.6.1`. 5 commits: `75dd826` Phase 0 audit, `2d160ca` RED tests, `3711e99` loaders GREEN, `78ceddd` graph GREEN, `b1388f6` Phase 1 writeup. Convo: [`convos/20260530_phase_0_and_1_execution.md`](docs/active/wi-allocation-matrix/convos/20260530_phase_0_and_1_execution.md). Results: [`results/20260530_phase_0_data_audit.md`](docs/active/wi-allocation-matrix/results/20260530_phase_0_data_audit.md), [`results/20260530_phase_1_graph_structure.md`](docs/active/wi-allocation-matrix/results/20260530_phase_1_graph_structure.md). **Next:** Phase 2 (IPF) in a fresh session — add `ipfn` dep, verify API handles ~835-node giant component + Pettack column-sum exclusion mechanism, then RED tests for toy 3×3 + sparse + max-entropy + real giant CC, then implement + materialize CLI + writeup.

- **2026-05-30** — [wi-allocation-matrix] **Plan-only kickoff in response to Suhan's "company → lobbyist → lawmaker → bill" ask.** Walked the merged WI 2025-2026 release (`releases/wi/`, $47.5M total, 944 principals, 773 lobbyists, 2,254 authorization edges, 7,345 bill-efforts) against the 6 pairwise relations across {principal, lobbyist, lawmaker, bill}. Mid-session correction: **company → bill is a DIRECT sworn disclosure** in WI (filed by the principal on their own expenditure report with effort %), not an inference — my earlier framing muddled "direct" with "modeled." Final classification: 3 of 6 relations directly in WI lobbying disclosures (principal↔lobbyist, principal↔bill with %, lobbyist↔hours-aggregate-marginal); 1 inferable via bipartite matrix completion (lobbyist↔bill — IPF/RAS on principal-side row sums + lobbyist-side column sums with authorization edge support); 1 free external scrape (lawmaker↔bill via WI Legislature / OpenStates); 2 need WI CFIS to close (principal↔lawmaker $-flow, lobbyist↔lawmaker personal donations) — contact-log relations are structurally absent from WI disclosure. Branch cut as `wi-allocation-matrix` off main with worktree + `~/data/lobby_analysis` symlink + clean baseline (1541 pass + 3 pre-existing). Plan landed at `docs/active/wi-allocation-matrix/plans/wi_allocation_matrix.md` — 5 phases + Phase 0 setup covering all 3 legs; CFIS leg explicitly scoped as write-up only this branch. **No implementation this session** per Dan; handoff to a fresh-context session that picks up Phase 0. Three things flagged for the implementing agent: (a) Pettack outlier handling (lobbyist 11072 reports 7,611 hrs ≈ 32 hrs/day — needs explicit outlier-flag step), (b) the "proportional attribution" assumption (lobbyist Y attacks principal P's bill mix in proportion to P's filed %) is a modeling choice that can't be validated against WI alone, (c) the "lobbyist X targeted bills sponsored by lawmaker Y" derived edge is a defensible proxy for influence target but NOT for direct contact — flag in any Suhan-facing output. Convo: [`convos/20260530_wi_allocation_matrix_kickoff.md`](docs/active/wi-allocation-matrix/convos/20260530_wi_allocation_matrix_kickoff.md). Plan: [`plans/wi_allocation_matrix.md`](docs/active/wi-allocation-matrix/plans/wi_allocation_matrix.md).

- **2026-05-27** — [api-multi-vintage-retrieval] **2025 statute cohort expanded 3→15 states; NC/FL added at both 2015 and 2025; CO 2025 TOC-page bug found and fixed.** Original ask was 2025 statutes for MI/WI/NC/FL; expanded to a full Method B fan-out across all 12 states with existing 5/19 2025 URL bundles after the CF posture cleared from Dans-MacBook-Pro (vs the 9/9 CF wall on Dans-MacBook-Air on 5/26). Method A dispatches (FL 2025, NC 2025, FL 2015 from-scratch, NC 2015 retry): 4/4 clean. Method B section fetches: 14 bundles, ~380 URLs, zero CF stubs. CO 2025 audit caught the original 5/19 subagent had produced 3 article-directory URLs instead of section-leaf URLs — `<500-byte` CF-stub check missed the 1.5-2 KB TOC pages. Fixed via mechanical `/2016/→/2024/` URL swap from CO 2015 bundle (CO 2015 itself was substituted to 2016, CO 2025 substitutes to 2024). Broken bundle preserved at `subagent_canaries/CO_2025_20260519_article_dir_urls/`; replacement bundle at `subagent_canaries/CO_2025/` with `prompt_git_rev: "n/a-mechanical-swap-from-CO_2015"`. **2015 handoff Step 3 template tightened** to add median-size <2 KB → STOP sanity check (driver pattern). Final inventory: 15 states paired 2015+2025; 13 with triples (2010+2015+2025); 361 files at 2025 (up from 73), 317 at 2015 (up from 271). Data is machine-local under `~/data/lobby_analysis/statutes/`, needs sync to Air/tarragon. **Open observation:** CF flip from 9/9 to 0/many overnight could be machine-vs-machine, time-aging, or URL-family difference — three confounds, none disentangled today; the 5/26 stealth-Playwright recommendation is not moot. **Open**: OH 2025 (52→30 vs 2015, predates this branch) has not been audited for the same TOC-page failure mode. Convo: [`convos/20260527_2025_cohort_expansion_and_co_fix.md`](docs/active/api-multi-vintage-retrieval/convos/20260527_2025_cohort_expansion_and_co_fix.md). Results: [`results/20260527_statute_inventory.md`](docs/active/api-multi-vintage-retrieval/results/20260527_statute_inventory.md).

- **2026-05-27** — [main, post-archive addendum to wi-disclosure-explore] **First MVP data release: `releases/wi/` (6 TSVs + README, ~2.9 MB) committed to `main`.** Packaged the merged `wi-disclosure-explore` Tier-2 output for sharing with collaborators. Six canonical files at the new `releases/wi/` path: `WI_principals.tsv` (944 rows), `WI_lobbyists.tsv` (773), `WI_lobbyist_principal_authorizations_unified.tsv` (2,254), `WI_principal_filings.tsv` (1,706), `WI_lobbyist_filings.tsv` (3,092), `WI_principal_bill_efforts.tsv` (7,345) — all join-compatible via `principal_id` / `lobbyist_id`. Excluded: the two side-only authorization TSVs (subsumed by the unified file), the XLS directory inputs (raw scrape, not output), and the smoke / sample / checkpoint subdirs. Verified on-disk TSVs reflect the post-address-fix state (`fbd8a4c` — Brooks 11052 spot-checked clean). `README.md` (9.6 KB) covers provenance (source portal, merge commit `5fcc6ac`, ~19 s reproducer wall time), schema (Popolo-style `contact_details_json`, OCD-style filings; link to `src/lobby_analysis/models/`), headline aggregates ($47.5M total spend, top-10 led by DoorDash $2.18M, bucket distribution), and the seven caveats already documented in `results/20260526_wi_tier_2_parser_results.md` (Pettack outlier, low-spend-exempt, 56 zero-filing principals, Neumann-Ortiz null-html silent skip, address sub-field split open follow-up, Nels Rude duplicated state-zip portal artifact, WCTA acronym ambiguity). **New convention: `releases/<state-or-collection>/`** for deliberately-published snapshots, distinct from gitignored `data/` which stays the home for scrape outputs and scratch artifacts. **Worked directly on `main` with no branch** per user choice for the small packaging task — flagged once as a deliberate exception to `CLAUDE.md`'s "no changes directly on main" rule, then proceeded. Single commit [`6dda3f1`](https://github.com/danparshall/lobby_analysis/commit/6dda3f1), pushed (`5fcc6ac..6dda3f1`). Convo: [`convos/20260527_wi_mvp_dataset_release.md`](docs/historical/wi-disclosure-explore/convos/20260527_wi_mvp_dataset_release.md). **Open items:** (a) flag the new `releases/` convention to other fellows / Suhan in the next weekly Corda update so nobody dumps raw scrape outputs here; (b) subsequent state pulls (NC, etc.) presumably want a parallel `releases/nc/` — pattern established here.

- **2026-05-27** — [wi-disclosure-explore] **Archive sweep + re-merge.** Picked up the prior agent's handoff: PR #29 was merged at `33b8793` but the archive housekeeping (move `docs/active/<branch>` → `docs/historical/<branch>` + STATUS.md table update) was missing; user asked for a clean re-run including the archive. Three reversion options surfaced: (A) hard-reset + force-push, (B) `git revert -m 1` + later revert-of-revert, (C) archive-as-follow-up-PR. **Option A chosen** via AskUserQuestion. Safety check before rewind: fresh `git fetch` confirmed `origin/main` unchanged at `33b8793`, merge commit not contained in any other branch (no fellow's branch absorbed it), zero open PRs depending on the merge — all clean. Rewind executed via checkout-dance (since `git reset --hard` is deny-listed): created throwaway `temp-rewind-target` at `94dc75d`, `git branch -f main 94dc75d`, `git checkout main`, deleted temp branch. User executed `git push --force-with-lease=main:33b879333bd50f4fd7ba324b7707cae32688d9f4 origin main` — hardcoded expected SHA gave belt-and-suspenders safety. `origin/main` confirmed back at `94dc75d`. **Skill not modified**: `finishing-a-development-branch` SKILL.md does NOT include archive housekeeping (re-read confirmed); user chose to keep that separation (archive stays an on-demand step per the Nori managed block). **Archive sweep:** `git mv docs/active/wi-disclosure-explore docs/historical/wi-disclosure-explore` (19 files: RESEARCH_LOG + 11 convos + 3 plans + 4 results); STATUS.md updated (active row removed, archived row added with full-branch summary, Last updated bumped, this Recent Sessions entry added, `docs/active/wi-disclosure-explore/...` references rewritten to `docs/historical/...` for link-graph consistency per the "doc system is persistent memory, not patchwork" feedback note); RESEARCH_LOG gained this session's entry at top. **Re-merge:** new PR opened with archive included; merged. PR #29 stays MERGED in GitHub's DB even though its merge commit is no longer in `main`'s history — fresh PR replaces its role. Test deltas: none (doc + path moves only); full suite remains at **1541 pass** + 3 pre-existing baseline failures + 3 skipped + 3 xfailed. Convo: [`convos/20260527_wi_archive_sweep_and_remerge.md`](docs/historical/wi-disclosure-explore/convos/20260527_wi_archive_sweep_and_remerge.md). **Next:** `clean-worktrees` to retire `.worktrees/wi-disclosure-explore`; the 4 wi-* follow-up branches remain queued (data-root-env, xlrd-swap, shared-table-helpers, materializer-error-discipline).

- **2026-05-27** — [wi-disclosure-explore] **Pre-merge code review surfaced a real address-parser BLOCKER misdiagnosed in yesterday's results doc; fixed under TDD; PR #29 opened + merged at `33b8793`, then user requested reversion for a fresh-agent re-run of `finishing-a-development-branch`.** Ran `finishing-a-development-branch` flow: full pytest (1537 pass + 3 pre-existing baseline failures on `test_pipeline.py`, archived-line-owned), confirmed no CI workflows on the repo, ran `ruff check` and fixed 2 F401 unused imports, dispatched `nori-code-reviewer` subagent for fresh-eyes review. Reviewer flagged a **BLOCKER**: both Tier-2 parsers leak phone digits + (lobbyist-side) firm-name strings into the typed `address` ContactDetail. My results-doc §8 framing from yesterday claimed the parser correctly preserved what the portal serves — **that was wrong**. Direct fixture inspection: the WI portal renders each contact field as `<i class="fa-{phone,envelope,globe}"></i> {value}<br/>` where value is a NavigableString *sibling* of the icon (not a descendant); both parsers' `_extract_address` were over-collecting those sibling strings. User chose "fix BLOCKER before merge" + "SHOULD-FIX items go to follow-up branches" via in-session AskUserQuestion. **TDD fix:** 4 RED tests (Dairy 11590 + Lexia 11348 principal-side; Brooks 11052 + Pfaff 11042 lobbyist-side) asserting exact-equality on the expected clean address; all 4 RED → all 4 GREEN after parser fixes. **Principal-side fix:** track whether the previous child during the sibling-walk was an `<i>` tag; if so, skip the next NavigableString. **Lobbyist-side fix:** replace the descendants-walk with a structural target — find the `col-lg-6` div with no `<i>` children (the address column, distinguished from the contact column by absence of icons) and walk only its NavigableString children. Re-materialized full corpus (18.9 s; identical 944/773/1706/3092/7345/0 row counts; addresses verified clean across 6 spot-check rows; one preserved portal-side artifact — lobbyist 11308 still shows `"Madison, WI 53703, WI 53703"` from the source HTML). Results-doc §8 framing rewritten in-place. Also fixed a `.gitignore` gap: `data/` only matched directories on `main`; per-worktree `data` symlinks needed a bare `data` entry too. **4 SHOULD-FIX items deferred to follow-up branches** per user: `wi-data-root-env` (5 CLIs have hardcoded `/Users/dan/data/...` paths), `wi-xlrd-swap` (drop pandas dep, use xlrd-already-a-dep), `wi-shared-table-helpers` (`_cell_value_text` + `_extract_optional_date` duplicated between auth + principal parsers), `wi-materializer-error-discipline` (`authorization_materialize` lets ParseError crash while `tier_2_materialize` catches → ParseFailure rows). Full suite at **1541 passed** (+4 from prior 1537) + same 3 pre-existing baseline failures. 5 commits this session (`b293401` lint, `af2f739` merge main pulling in 2026-05-22 weekly update, `fbd8a4c` parser fix + tests + results-doc §8 rewrite, `2cee7ea` gitignore, plus this finish-convo commit), all pushed. Convo: [`convos/20260527_wi_parser_address_fix_and_pr.md`](docs/historical/wi-disclosure-explore/convos/20260527_wi_parser_address_fix_and_pr.md). **PR opened with merge intent.**

- **2026-05-26** — [wi-disclosure-explore] **Tier-2 Phase 6 (run + writeup) + Phase 7 (WCTA doc-drift fix) shipped; all 41 steps of `wi_tier_2_parser.md` complete.** Ran `tier_2_materialize_cli` at default paths against the 944 principal + 773 lobbyist corpus: **19.3 s wall, 944 / 773 / 1706 / 3092 / 7345 / 0 rows** — idempotent vs the prior session's smoke run. Plan spot-checks pass cleanly: Lexia 11348 = **$65,225.58 YTD exact** ($32,537.58 H1 + $32,688 H2); Dairy 11590 = $88,568.50 YTD + 72 bill-effort rows across 4 buckets; WCTA 12997 = 1 H2 filing at $0/0/0 (no H1 column in portal). **Two prior-session findings confirmed in the writeup:** (1) 0 parse failures via the *null-html branch*, not ParseError path — the fetcher's body-marker soft-404 detection from the auth-scrape session stored Neumann-Ortiz 12717 as `html=null` and the materializer's iterator silently skips null-html checkpoints; she has 0 Person and 0 LobbyistFiling rows. Handling is correct, observation channel is null-skip rather than ParseFailure-row; (2) WCTA 12997 emits 1 filing, not 2 — low-spend-exempt principals don't necessarily emit zero-rows for every period. **Headline aggregates:** $47,458,304.69 total spend / 888 of 944 principals with ≥1 filing / 812 with > $0 spend. Top-10 by spend led by DoorDash $2.18M (sharp outlier), then WIIN $1.01M, WMC $911k, WHA, REALTORS, Farm Bureau, AFP, Property Taxpayers, Insurance Alliance, Counties Assn. Bucket distribution: Legislative Bills 54.9% / Topics-Not-Yet 31.7% / Budget Bill 11.7% / Admin Rule 1.7% — and the parser declares 6 buckets but `Minor Efforts` + `Other Matters` have **0 rows** in 2025-2026. Lobbyist-filing hours: median 15 hrs communicating per non-zero filing (1,128 / 3,092 = 36.5% of rows); max 651 / max-other 3,356.5. **Data-quality observation:** lobbyist 11072 (Deanna Pettack, School Administrators Alliance) reports 7,611 hrs total H1+H2 — ≈32 working hrs/day, not possible for one individual; probable interpretation is org-wide hours aggregated under a single registered lobbyist; portal pattern, not parser bug; cross-state validation pending. **Held-over `contact_details_json` eyeball folded in** as Finding §8: the typed `address` entry is the full 4-line block (firm + street + city-state-zip + phone-already-duplicated-in-dedicated-field); some rows have email mashed in instead of street; "Self-Employed Lobbyist - No Firm or Org" appears verbatim for sole proprietors. Parser preserves what the portal serves correctly; downstream geocoding wants a `_parse_address_blob` helper. **Phase 7 doc-drift fix** landed in-place on `results/20260526_wi_principal_side_scrape_results.md` line 65: "Wisconsin Cable Telecommunications Association" → "Wisconsin County Treasurers Association" + 2026-05-26 correction-note block with the verify-from-page-body principle. No code changes this session (run + analysis + docs only); test suite remains 1537 pass + 3 baseline failures. Convo: [`convos/20260526_wi_tier_2_phase_6_7_results.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_tier_2_phase_6_7_results.md). Results: [`results/20260526_wi_tier_2_parser_results.md`](docs/historical/wi-disclosure-explore/results/20260526_wi_tier_2_parser_results.md). **Next:** PR + merge of `wi-disclosure-explore` is the natural milestone — Dan's call. Logical follow-up branches (each scope-creep on this one): `_parse_address_blob` refactor, synthetic ParseFailure rows for null-html-skipped checkpoints, low-spend-exempt flag on `Organization` (v1.3), classify the 56 zero-filing principals, re-check 6-vs-4 bucket count.

- **2026-05-26** — [wi-disclosure-explore] **Tier-2 Phase 4 (materializer) + Phase 5 (CLI) shipped end-to-end; first run against real corpus passes spot-checks.** Picked up prior session's Phase 4 handoff. Authored 36 RED tests at `tests/test_wi_tier_2_materialize.py` covering ParseFailure value object + two checkpoint iterators (parsed-tuple yield vs ParseFailure routing, null-html silent skip, sorted iteration, non-numeric filename skip) + 5 TSV writers + 1 warnings writer (fieldname schemas, sort orders, None→empty-cell vs zero→"0.0" distinction, JSON-serialized `contact_details`) + end-to-end orchestrator (6 output files, known-value round-trip on Dairy P1, ParseError → warnings TSV not crash, `WI-{role}-{id}` convention, principal_id/lobbyist_id join compatibility with `WI_lobbyist_principal_authorizations.tsv`) + idempotency (byte-identical re-runs). Implemented `src/lobby_analysis/io/wi/tier_2_materialize.py` against the contract — **36/36 GREEN on first implementation pass.** RED committed at `1132529`, GREEN at `69a268b`. Then ran the materializer against the real on-disk corpus (944 principal + 776 lobbyist checkpoints): **19.2 s wall, 944 + 773 + 1,706 + 3,092 + 7,345 + 0 rows across the 6 output files**. Spot-checks of Dairy 11590 (P1 `$37,840 / 158.5 / 307.0`, P2 `$50,728.5 / 100.5 / 254.0`), WCTA 12997 (`$0 / 0 / 0`), Brooks 11052 (P1 `102.5 / 566.0`, P2 `195.0 / 673.9`, 2026 zeros) match the parser tests' fixture-derived anchor values exactly. Phase 5 CLI wrapper (`tier_2_materialize_cli.py`) shipped at `eff2cda` — thin argparse pass-through over `materialize_tier_2()`, no new tests per the plan, verified end-to-end. Full suite at **1537 passed** (+36 from prior 1501) + 3 pre-existing `test_pipeline.py` baseline failures (archived-line owned) + 3 skipped + 3 xfailed; no regressions. **Two findings for Phase 7 writeup:** (1) **0 parse failures, not the 1 the plan + prior convos anticipated** — Neumann-Ortiz 12717 soft-404 is already stored as `html=null` by the fetcher's body-marker detection from the auth-scrape session, so the materializer's iterator silently skips it via the null-html branch; it never reaches the ParseError → ParseFailure path. Soft-404 IS handled correctly, just via a different code path than the plan framed; (2) **WCTA 12997 emits 1 filing (2025-H2 at $0/0/0), not 2** — the 2025-H1 column on its page is empty rather than populated-with-zero. Low-spend-exempt principals file in some periods but not necessarily all of them. **Idempotency strategy:** TSV row schemas omit `extracted_at` (parser stamps `datetime.now()`, which would defeat byte-identity) and include `source_url` (stable from URL template). Provenance stays correct in-memory; only the TSV schemas are trimmed. **Pause point:** Dan picked "Phase 5 only, then finish-convo" via in-session AskUserQuestion; Phases 6 (proper run + spot-check writeup) and 7 (WCTA doc-drift fix + `results/20260526_wi_tier_2_parser_results.md`) deferred to a follow-up session. Smoke output landed under `~/data/lobby_analysis/disclosures/WI/_tier_2_smoke/` (subdir, intentionally NOT the documented Phase 6 path so the next session can re-run cleanly there). 3 code commits + this finish-convo commit, all pushed. Convo: [`convos/20260526_wi_tier_2_phase_4_materialize.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_tier_2_phase_4_materialize.md). **Next:** Phase 6 + Phase 7 in one session, OR PR + merge `wi-disclosure-explore` as a natural milestone — Dan's call.

- **2026-05-26** — [wi-disclosure-explore] **Tier-2 Phase 2 GREEN + Phase 3 RED+GREEN shipped; one structural finding surfaced.** Picked up the prior session's Phase 2-RED handoff. Implemented `src/lobby_analysis/io/wi/principal_meta_parser.py` per the locked four-element contract (`Organization`, extras dict, `list[LobbyingFiling]`, list[item dict]) against the 25 driving tests — 24 turned GREEN on first implementation. The 25th (`test_dairy_contains_known_bill`) RED is a clerical fixture/expectation mismatch: test asserts Assembly Bill 30 → `"1%"`, but fixture HTML on `principal_11590_populated.html` line 5221 shows `"2%"` for `panel-billeffort-24598`. Likely a swap with adjacent `panel-billeffort-24710` (Assembly Bill 93, which IS at `1%`). Dan explicitly chose "pause and surface separately" via AskUserQuestion rather than self-patch either side, so Phase 2 GREEN landed with 24/25 + 1 documented surfaced RED. Phase 2 GREEN committed at `ef7b8dd`. **Phase 3:** authored 14 RED tests at `tests/test_wi_lobbyist_time_report_parser.py` against Brooks 11052 + Pfaff 11042 fixtures, committed RED at `194d6b4`. Implemented `src/lobby_analysis/io/wi/lobbyist_time_report_parser.py`, 14/14 GREEN on first run, committed at `b65c245`. **Two structural differences from the principal-side parser** encoded in the lobbyist module: `<h4>Time Report Summary</h4>` (not `<h3>`); period header format `"January 2025 to June 2025"` (not `"2025\nJanuary - June"`); in-progress 2026 columns carry explicit `0` (not empty cells), so the lobbyist parser emits exactly 4 filings per page (including zero-hour rows) vs. principal-side's 2 (in-progress columns suppressed). **Pfaff fixture's `[125.00, 74.00, 0, 0]` / `[259.50, 276.00, 0, 0]`** is a fresh cross-check measurement not in the prior convo; matches the 2-populated/2-zero norm. Filing IDs use `WI-{principal\|lobbyist}-{id}-{expenditure\|activity}-{year}-{H1\|H2}` per the locked entity-ID scheme. Full suite **1500 pass + 3 pre-existing baseline failures + 1 surfaced RED (AB30)**; no regressions. Address ContactDetail on lobbyist Person is best-effort (likely conflates firm name + phone digits into the address value; no test asserts on contents) — flagged for Phase 6 spot-check, not blocking. 3 code commits + this finish-convo commit, all pushed. Convo: [`convos/20260526_wi_tier_2_phases_2_3_green.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_tier_2_phases_2_3_green.md). **Next:** (a) Phase 4 materializer next session (TDD against on-disk checkpoint JSONs; soft-404 ParseError rows route to `_tier_2_parse_failures.tsv`); or (b) PR + merge of `wi-disclosure-explore` if Dan calls Tier-2 done at the parser layer. **Post-session:** the AB30 RED was resolved at `d15571e` by a parallel-session agent picking option 1 (test expectation `"1%"` → `"2%"` to match fixture body); all 25 principal-meta parser tests now GREEN; suite at **1501 passed + 3 pre-existing baseline failures**, no surfaced RED.

- **2026-05-26** — [wi-disclosure-explore] **Tier-2 parser implementation: Phase 0 + Phase 1 shipped + Phase 2 RED tests landed.** Picked up at Phase 0 of [`plans/wi_tier_2_parser.md`](docs/historical/wi-disclosure-explore/plans/wi_tier_2_parser.md). **Phase 0:** captured 3 high-volume fixtures from the gitignored data store — `principal_11590_populated.html` (Dairy Business Association, 698 KB, the only top-30-by-size principal with `no_results=0` in the Percent Allocation section — all 6 buckets populated), `principal_11637_populated.html` (Wisconsin Manufacturers & Commerce, 2.1 MB, 10 lobbyists, $911,593.49 spend, sparse-allocation variant with heavy Lobbying Interests), and `lobbyist_11052_populated.html` (Bryan Brooks, top lobbyist at 41 principals, 64 KB with realistic 2-populated/2-zero Time Report Summary pattern). **Snapshot-timing structural finding:** only 3 of 770 lobbyists have all 4 Time Report Summary periods populated; 420 match Brooks's 2-populated/2-zero norm because P3 is in progress and P4 hasn't started — the plan's fixture-selection spec "all 4 periods showing non-zero hours" was unachievable but the test spec works fine with Brooks's data. **Phase 1:** v1.1 → v1.2 schema bump on `LobbyingFiling` under strict TDD — 7 RED tests at `tests/test_models_v1_2.py` → added `total_hours_communicating: float | None` + `total_hours_other: float | None` to `src/lobby_analysis/models/filings.py` → 7 GREEN tests + 126/126 across all 3 model test files + 49/49 WI tests, full suite 1462 pass with same 3 pre-existing `test_pipeline.py` baseline failures (archived-line ownership). Noridoc updated at `src/lobby_analysis/models/docs.md`. **Phase 2 RED:** 21 driving tests at `tests/test_wi_principal_meta_parser.py` against the parser contract `parse_principal_meta(html, principal_id) -> tuple[Organization, dict, list[LobbyingFiling], list[dict]]`. ImportError-level RED at collection (parser module doesn't exist yet) per the plan's step 15. **Decisions locked with Dan this session:** (Q1) `LobbyingFiling.provenance` populated YES with `source_url` + `extracted_at`; (Q2) NO additional cheap add-ins beyond the Phase 7 WCTA name fix; (Q3) Percent Allocation rows emit at per-item level (not bucket-totaled) — "long term I think we'll go B [typed sub-entity], but let's try A just to see what the data look like"; (Q4) CEO Name + Business Or Interest + Lobbying Interests prose flow through a side-channel dict alongside the typed Organization record (no v1.2.1 schema bump; long-term v1.3 lifts to typed Organization fields). **Same-h4-text-different-section structural finding:** `<h4 class="card-title">Legislative Bills/Resolutions</h4>` appears under TWO different h3 parents on a principal page (Lobbying Interests vs Percent Allocation) — parser must distinguish by h3 ancestor. **Total Lobbying Effort table only shows COMPLETED semesters** on this snapshot (2025 H1 + H2). **`REDACTED_PRINCIPAL_IDS = {11530, 13137}`** module constant locked for the parser. 7 commits this session (`3ccc042`, `01388e6`, `0debed0`, `f50c7e7`, `698897b`, `a5dae17`, `0481559`), all pushed. Convo: [`convos/20260526_wi_tier_2_parser_implementation.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_tier_2_parser_implementation.md). **Next agent picks up at Phase 2 GREEN** — implement `src/lobby_analysis/io/wi/principal_meta_parser.py` per the locked contract, watch the 21 tests go green, then proceed to Phase 3 against `lobbyist_11052_populated.html`.

- **2026-05-26** — [wi-disclosure-explore] **Tier-2 parser plan landed; v1.1 → v1.2 disclosure-data schema bump scoped for hours fields.** Dan asked what data the existing scrapes capture beyond authorization edges. Inspection of the 4 committed fixtures (12997 WCTA, 11348 Lexia, 11530 redacted, lobbyist 11042) surfaced a substantial Tier-2 layer: per-principal lobbying-interests prose, per-(principal, semester) total expenditures + hours-communicating + hours-other + activity-allocation across 6 WI-specific buckets (Legislative Bills, Budget Bills, Rulemaking, Topics-Not-Yet-Assigned, Minor Efforts, Other), per-(lobbyist, semester) Time Report Summary. **All accessible from the 944 + 774 HTMLs already on disk; zero new fetches needed.** Tier 3 (per-(lobbyist, principal, semester) detailed time reports + per-principal SLAE itemizations) explicitly held over to a separate plan. Two genuine schema gaps surfaced against the existing v1.1 `LobbyingFiling`: (a) no hours fields (Dan decided: bump to v1.2 with `total_hours_communicating` + `total_hours_other`); (b) current scrape never emits `Organization` records for principals (added as part of this plan). **Critical schema-layer clarification from Dan:** `LobbyingFiling` in `src/lobby_analysis/models/` is the contract for *actual disclosure data*; `models_v2/` cells are the contract for *statute-metadata* (Prong 1). They're related but version independently — this plan's v1.2 bump applies only to the disclosure-data layer. **Side-finds during inspection:** (1) **Documentation drift on principal 12997** — the principal-side scrape results doc names it "Wisconsin Cable Telecommunications Association" but the fixture HTML title is just "WCTA" with body content unambiguously identifying it as **Wisconsin County Treasurers Association** (Schlaak is a county treasurer / legislative chair of the treasurers' association, confirmed via 3 external web-search sources). Two same-day session writeups expanded the ambiguous "WCTA" acronym from context and one guessed wrong. Doc-drift fix scoped into plan Phase 7. (2) The Schlaak-class Mechanism A reframes from "unknown grid-AJAX filter" to "likely a public-sector-self-advocacy filter" — Schlaak is a public official, not a paid lobbyist. Testable downstream but not in scope. (3) The hyphen-encoding hypothesis for Neumann-Ortiz's soft-404 is dead (9 other hyphenated surnames in the grid fetched cleanly; her URL is keyed by ID anyway). (4) No `nc-disclosure-explore` branch exists in the repo despite WI RESEARCH_LOG claiming WI is "parallel to" it — WI is the first state-extraction line; sets conventions (e.g., `WI-principal-{id}` / `WI-lobbyist-{id}` for `Organization.id` / `Person.id`, matching the uppercase-two-letter `source_state` pattern). **Plan:** [`plans/wi_tier_2_parser.md`](docs/historical/wi-disclosure-explore/plans/wi_tier_2_parser.md) — 7 phases: Phase 0 fixture capture (high-volume principals, Dan's data store), Phase 1 v1.2 schema bump under TDD, Phases 2-3 principal-side + lobbyist-side parsers under TDD, Phase 4 materializer producing 4 TSVs (`WI_principals`, `WI_lobbyists`, `WI_principal_filings`, `WI_lobbyist_filings`) from checkpoint JSONs, Phase 5 CLI, Phase 6 spot-check, Phase 7 docs + drift fix. Convo: [`convos/20260526_wi_tier_2_parser_plan.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_tier_2_parser_plan.md). **Next:** implementing agent executes Phase 0 (blocked until Dan captures fixtures from gitignored data store) then proceeds through Phase 1's schema bump.

- **2026-05-26** — [wi-disclosure-explore] **Principal-side completeness scrape executed end-to-end; Schlaak-class population bounded at 2 lobbyists; NEW case surfaced.** Executed [`plans/wi_principal_side_scrape.md`](docs/historical/wi-disclosure-explore/plans/wi_principal_side_scrape.md) under TDD: 8 commits, 4 new modules (parser / id-discovery / materialize / unification) + a refactored generic `entity_fetcher` core (lobbyist-side `authorization_fetcher` becomes thin wrapper) + 2 CLIs + 27 new behavior tests, all green (97 WI tests total). Pre-flight Step 6 confirmed page-size upper bound at ~340 KB (WHA); corrected the prior session's SAL endpoint URL (actual: `/Who/StateAgencies/2025REG/ExcelExport`, not `?outRpt=Excel`); grabbed the SAL table at 2,599 liaisons × 13 cols. Full 944-principal scrape: **19.5 min wall, 0 hard or soft 404s** (vs 1 soft-404 on lobbyist side), 2,254 authorization rows, 748 distinct lobbyists. Unified table headline: **`discovered_via = {lobbyist: 0, principal: 3, both: 2251}`** — the principal side is a strict superset of the lobbyist side on this snapshot. **Schlaak-class lobbyist count: 2** — the known 12694 (Schlaak / WCTA) plus a NEW case at **11513 (Steinbruecker / ACLU of Wisconsin)**, who surrendered his license on 5/25/2026 (same day as the .xls print). The Steinbruecker case **refutes** the "directory filter = surrendered=NULL" hypothesis: he IS in the .xls with an empty Surrendered Date column, but NOT in the live grid AJAX. The .xls is a point-in-time snapshot, not a filtered query. Schlaak's exclusion remains structurally unexplained (license current, authorization current, in neither roster). 1 soft-404 recovery on lobbyist 12717 (Neumann-Ortiz / Voces) — both rosters know about her, but her detail page is broken; principal-side recovered her edge via the back-link. **Net: lobbyist-side scrape was ~99.9% edge-complete and ~99.7% lobbyist-complete at the 2026-05-26 snapshot.** Principal-side scrape is now the strictly-more-complete edge source going forward. Pandas + xlrd added as production deps for the .xls discovery read. Convo: [`convos/20260526_wi_principal_side_scrape_implementation.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_principal_side_scrape_implementation.md). Results: [`results/20260526_wi_principal_side_scrape_results.md`](docs/historical/wi-disclosure-explore/results/20260526_wi_principal_side_scrape_results.md). **Next:** Dan's call — email exchange with WI Ethics Commission to explain the Schlaak filter, cross-session principal_id stability investigation, SAL parser/ingest, or PR + merge of `wi-disclosure-explore` as a natural milestone.

- **2026-05-26** — [wi-disclosure-explore] **Principal-side completeness scrape plan written; bilateral Schlaak omission re-confirmed.** Re-fetched lobbyist 12694's detail page (still HTTP 200, byte-identical sha256 `bf616576fb1b2632`) and re-POSTed the LobbyistList grid AJAX (still 774 IDs, byte-identical sha256 `68b792835c41547f`, Schlaak still absent). Byte-identity at the 5-hr mark suggests edge-cached / daily-snapshot serving — weaker re-fetch evidence than "1 day later" would have been, but the 16-month tenure pinpoint from the prior session remains the dominant structural-omission argument. Reconnaissance over 42 captured principal HTMLs corrected two estimates inherited from the kickoff convo: real page-size mean is 47 KB / max 157 KB (not 560 KB); wall time at delay=1.0 is ~17 min for 944 pages (not 5 hr) — same envelope as the lobbyist scrape's 14 min for 774. Plan written at [`plans/wi_principal_side_scrape.md`](docs/historical/wi-disclosure-explore/plans/wi_principal_side_scrape.md): 12 implementation steps, 6 RED-phase parser tests against WCTA/4-lobbyist/privacy-redacted fixtures, unification module producing a `discovered_via ∈ {lobbyist, principal, both}` + `lobbyist_in_grid` provenance schema to surface the Schlaak-class population, decision point on whether to refactor `authorization_fetcher.py` generic or duplicate it for principals. Convo: [`convos/20260526_wi_principal_side_scrape_plan.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_principal_side_scrape_plan.md). **Next:** implementing agent runs the plan; first asks Dan on the refactor-vs-duplicate decision.

- **2026-05-26** — [wi-disclosure-explore] **Gap investigation: 40-principal gap explained + a structural finding on lobbyist-side completeness.** Took the prior session's held-over follow-up (2): investigate the 942 vs 904 principal gap. Reconciled cleanly — `942 − 904 = 38 = 40 auth-only − 2 dir-only`. Of the 40 auth-only principals: **38 cleanly ceased** in 2025-2026 (directory `.xls` filter is empirically `cessation_date IS NULL`); **2 are privacy-redacted "low-spend pledge"** entities under the WI Ethics Commission's <$500/year exemption (principal-info detail suppressed but auth graph fully published). No principal-side fetch errors. But while reconciling the 2 dir-only principals, **a separate, more important structural finding surfaced**: Voces de la Frontera Action (12900) had 0 auths because its lobbyist (12717) was the prior session's already-known soft-404; WCTA (12997) had 0 auths because its lobbyist (Schlaak, 12694) is silently omitted from BOTH the LobbyistList grid AJAX (774 IDs) AND the `WI_directory_lobbyists.xls` (776 rows), even though his lobbyist detail page resolves cleanly by direct URL (HTTP 200, license 1/28/2025, current WCTA auth 1/8/2026, 16 months tenure — rules out race condition). **Implication:** neither roster source is exhaustive; our lobbyist-side discovery has an unbounded blind spot. The principal-side scrape (handoff option 4) is **the only mechanism** to enumerate the Schlaak-class population — reframes (4) from "cheap insurance" to "completeness sweep." Saved as committed test fixtures: `principal_{10949,10973,11017}.html` (canonical ceased-principal examples for future parser tests). Investigation artifacts under `~/data/lobby_analysis/disclosures/WI/_principal_gap_investigation/` (gitignored). Convo: [`convos/20260526_wi_principal_gap_investigation.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_principal_gap_investigation.md). Results: [`results/20260526_wi_principal_gap_investigation_results.md`](docs/historical/wi-disclosure-explore/results/20260526_wi_principal_gap_investigation_results.md). **Held over:** (1) `lobbying@wi.gov` reply, (3) State Agency Liaisons table pull, (4) principal-side completeness scrape — now better-motivated.

- **2026-05-26** — [wi-disclosure-explore] **Plan execution: WI lobbyist↔principal authorization scrape shipped.** Built 5 modules under `src/lobby_analysis/io/wi/` test-first (parser + ID-discovery + polite-fetcher-with-checkpoint-resume + materialize + CLI), 22 behavior tests all green. Resolved the plan's "lobbyist IDs not in .xls" known unknown by discovering the LobbyistList grid AJAX endpoint (`POST /Who/Lobbyists/2025REG/ShowLobbyistList` with `pageSize=1000` returns all 774 IDs in one 353 KB response — URL derived from `/Content/site.js`'s `refreshGrid`). Full scrape of 774 lobbyists in 851 sec wall (~14 min) at delay=1.0, materialized to `WI_lobbyist_principal_authorizations.tsv`: **2,251 authorization rows / 745 active lobbyists / 942 distinct principals / 258 currently-withdrawn / 4 pending-authorization**. Top principal (tied at 15 lobbyists): Wisconsin Automobile and Truck Dealers Association + Wisconsin Hospital Association. Top lobbyist: Bryan Brooks (41 principals). Two real-data bugs surfaced during materialize, fixed TDD-style: (1) `Authorized On = N/A` on 4 rows → `authorized_on: date \| None`; (2) WI portal returns HTTP 200 with "Page Not Found" body for nonexistent IDs (1 case: 12717) → soft-404 detection added in the fetcher. **Anomaly worth investigating:** 942 distinct principals in the auth graph vs 904 in `WI_directory_principals.xls` — 40-entry gap. Plus 2 directory principals with 0 lobbyist authorizations. Dan declined sycophancy on a mid-session ad-hoc bash/curl pattern (curl is in ASK list; `Python over Bash for non-trivial`); switched to Write/Edit + `uv run python` for remaining work. Convo: [`convos/20260526_wi_authorization_scrape_implementation.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_authorization_scrape_implementation.md). Results: [`results/20260526_wi_authorization_scrape_results.md`](docs/historical/wi-disclosure-explore/results/20260526_wi_authorization_scrape_results.md). **Held over:** send drafted email to `lobbying@wi.gov`, investigate 40-principal directory gap, pull State Agency Liaisons table, principal-side cross-validation scrape.

- **2026-05-26** — [wi-disclosure-explore] **Kickoff session: WI data ingest + portal join-key investigation.** New branch + worktree created off main. Two WI Ethics Commission `.xls` directory exports moved into `data/disclosures/WI/` with NC-parallel naming (`WI_directory_lobbyists.xls` 776 rows, `WI_directory_principals.xls` ~905 rows; both for 2025-2026 session, printed 5/25/2026). Files don't carry a foreign-key relationship — the lobbyist file references employer by *Organization Name* (string), not by *Principal ID*. Portal investigation: the four `/Who/.../ReportExport?outRpt=Excel` endpoints cover only the three entity rosters (lobbyists, principals, state agency liaisons); no bulk authorization export exists. Authorization data IS reachable as embedded HTML — per-lobbyist detail pages (34 KB each × 776) carry a `Principals Represented` section with stable `/Who/PrincipalInformation/.../Information/{id}` hrefs and `Authorized On` dates (spot-checked: lobbyist 11042 → 9 principal IDs). SSRS direct endpoint (`/Reports/Report.aspx?...`) returns `Failed call to SSRSAgent.GetReportList()` server-side, even with browser UA — same failure mode noted in the 2026-05-01 portal snapshot. Plan [`plans/wi_authorization_scrape.md`](docs/historical/wi-disclosure-explore/plans/wi_authorization_scrape.md) written: parse fixture-driven TDD → polite fetcher with checkpointing → materialize TSV → spot-check. Wildcard flagged but not pursued: email `ETHLobbying@wi.gov` to ask whether they can run the authorization CSV from SSRS directly (Dan to decide). Convo: [`convos/20260526_wi_data_ingest_and_join_key_investigation.md`](docs/historical/wi-disclosure-explore/convos/20260526_wi_data_ingest_and_join_key_investigation.md). **Next:** next agent executes the plan.

- **2026-05-21 (Tier-2 Step D — re-dispatch verification)** — [extraction-harness-brainstorm] **The deferred integration test for the A/B/C fixes ran against real API output; 2 of 3 fixes verified, 1 new finding.** Re-dispatched the 18 triples that each carried an `instantiation_failed` error in Tier-1 (`registration_thresholds` + `lobbyist_spending_report` + `principal_spending_report` × 2 models × 3 runs); originals preserved in `results/tier_1/_superseded/`. Cost **$1.8157** (the handoff's "~12 calls/~$1" undercounted — all 18 files carried errors). **Fix A** (`int`→`Decimal`) positively verified — a bare JSON `"value": 50` instantiated cleanly. **Fix C** (null-`FreeTextCell`→abstention) verified — 12/12 spending-report dispatches error-free. **Fix B** partial — the keys-only hint steered claude off the bare-string failure (original class-B `TypeError` gone), but a **new error** surfaced: claude emits a structurally-valid `{magnitude, unit}` dict with out-of-domain `unit` values because OH's lobbyist definition is a qualitative "main purposes" test with no numeric time threshold, and `TimeThresholdCell` cannot represent that. 3/3 claude `registration_thresholds` runs still error → plan's zero-error pass criterion **not met**. The finding routes to **blocker 3 (abstention calibration)** — GPT abstains on this cell correctly, claude *under-abstains*; enum-domain expansion (blocker 2) would not help. No patch (Step-D discipline). Writeup: [`results/tier_1/20260521_tier_2_step_d_redispatch_writeup.md`](docs/active/extraction-harness-brainstorm/results/tier_1/20260521_tier_2_step_d_redispatch_writeup.md); convo: [`convos/20260521_tier_2_step_d_redispatch_verification.md`](docs/active/extraction-harness-brainstorm/convos/20260521_tier_2_step_d_redispatch_verification.md). **Next:** blocker 3 (Phase-2 verifier abstention-calibration policy); then blocker 2 (enum-domain pinning).

- **2026-05-21 (Tier-2 schema/adapter fixes — plan executed end-to-end)** — [extraction-harness-brainstorm] **The 3 Tier-1 `instantiation_failed` error classes cleared in code under strict TDD.** Executed [`plans/20260521_tier_2_schema_adapter_fixes.md`](docs/active/extraction-harness-brainstorm/plans/20260521_tier_2_schema_adapter_fixes.md). Plan's 3 Questions settled with the user first: Q1 → option **(c)** (adapter-side sentinel — no shared-`models_v2` change, no scorer change); Q2 → **defer Step D** (the ~$1 / ~12-call re-dispatch verification; both API keys unset this session); Q3 → enum-domain pinning + the Phase-2 verifier abstention-calibration policy **stay out of scope**. **Fixes:** A — `_coerce_scalar_value` now coerces a bare `int`/`float` to `Decimal` (bool excluded; float via `str()` for exactness); B — `render_legal_roster` appends a keys-only JSON-object hint for dict-shape cells; C — `_parse_and_instantiate` routes a `null`-valued `FreeTextCell` `record_cell` to an abstention. All 10 Step-E behavior tests written first (RED: 5 driving fail, 5 guards pass), then each fix turned its tests green. 4 commits `0403218`→`00e7257`; full suite **525 pass**, 8 skip, 3 pre-existing `test_pipeline.py` baseline failures (unrelated). No API spend. **Fix B scope call (flagged):** keys-only hint, not the plan example's `unit`-`Literal`-domain expansion — the Step-E test spec specifies keys only and Step D (which would verify a richer hint) is deferred. Convo: [`convos/20260521_tier_2_schema_adapter_fixes_execution.md`](docs/active/extraction-harness-brainstorm/convos/20260521_tier_2_schema_adapter_fixes_execution.md). **Next:** Step D re-dispatch verification (both API keys + ~$1, fresh `results/` dir, zero-error pass criterion); then enum-domain pinning; then the abstention-calibration policy.

- **2026-05-21 (Tier-1 direct-read legal-axis plan executed end-to-end)** — [extraction-harness-brainstorm] **All 8 steps of [`plans/20260520_tier_1_direct_read_legal_axis.md`](docs/active/extraction-harness-brainstorm/plans/20260520_tier_1_direct_read_legal_axis.md) shipped.** Step 1 resolved CPI items IND_196/197/199/201/203/207 → exactly 6 chunks (`lobbying_definitions`, `registration_thresholds`, `registration_mechanics_and_exemptions`, `lobbyist_spending_report`, `principal_spending_report`, `enforcement_and_audits`; 84 legal cells). Steps 2/3/5/6 (TDD, 18 behavior tests written test-first): `_coerce_scalar_value` value-typing fix in `_instantiate_cell`, `axis=='legal'` roster filter, σ_noise metric. Step 4: `scripts/tier_1_direct_read_legal_axis.py` 36-dispatch runner with checkpoint/resume. **Run:** `[claude-opus-4-7, gpt-5.2] × 6 chunks × 3 runs` against OH 2025, **$2.94** (ceiling $10). 4 commits `aa970a5`→`f3931e2`; suite 515 pass (+3 baseline `test_pipeline.py` failures). Plan Questions settled with user first (SDK-default temp — Tier-0 actually ran at default 1.0, not temp-0; N=3; scope ends at σ_noise; keep $10). **Findings:** σ_noise Claude **85.7%** / GPT **73.8%** cells stable (both deflated by unpinned-enum label churn); criterion 3 met — **zero Tier-0 string/int errors** (coercion fix works); 18 errors = **3 new classes, reported not patched** (A `int→Decimal` strict rejection — the Step-2 prompt nudge walked GPT into it; B dict-shape cell fed a scalar — anticipated `TimeThresholdCell` failure; C non-optional `FreeTextCell` fed `null` — schema gap, both models 100% of runs); abstention-calibration problem reproduced at chunk scale (GPT abstains on the whole `registration_thresholds` chunk — OH triggers lobbyist status qualitatively, not by dollar threshold); criterion 4 verified (0 practical cells dispatched). **Verdict:** legal-axis direct-read is "qualified yes" for scaling — 3 small schema/adapter fixes + enum-domain pinning + an abstention policy first. Writeup [`results/tier_1/20260520_tier_1_legal_axis_writeup.md`](docs/active/extraction-harness-brainstorm/results/tier_1/20260520_tier_1_legal_axis_writeup.md); convo [`convos/20260521_tier_1_legal_axis_execution.md`](docs/active/extraction-harness-brainstorm/convos/20260521_tier_1_legal_axis_execution.md). **Next:** Tier-2 — the 3 schema/adapter fixes; pin enum domains; design the Phase-2 verifier's abstention-calibration policy.

- **2026-05-20 (Tier-0 direct-read plan: Steps 5–7 of 7 executed — the live smoke run + writeup)** — [extraction-harness-brainstorm] **Ran the dual-model smoke and wrote the architecture verdict; no code changes.** Live dispatch of Claude Opus 4.7 + GPT-5.2 against OH 2025 `enforcement_and_audits` (4 cells), on Dans-MacBook-Air (keys supplied this session via a local `.env.corporate` file). All 4 raw/parsed JSON files written first try; cost ≈ $0.10 (ceiling $5). **Findings:** wiring works (criteria 1–4, 7 met); **criterion 5 FAILED** — Claude emitted `record_cell` with `value="2"`/`"1"` (JSON strings) for the two `practical`-axis `GradedIntCell`s and Pydantic rejected them (root cause: loose `oneOf` `value` schema includes `string`); per the plan this is reported, **not patched** (Tier-1's job). **Practical axis is structurally unanswerable from a statute-only bundle** — GPT correctly abstained on both practical cells, Claude over-reached and scored them; this is a data-*source* gap, not a retrieval gap, so the Citations+retrieval escape hatch is **not indicated**. **Legal axis is viable** — both models scored `penalties…legal=True` correctly (§101.99 verified verbatim); Claude scored `audit_required_in_law,legal=review_only` correctly (§101.72(G)+§101.79 verified), GPT abstained over-conservatively. Sharp model divergence: Claude scores aggressively (4/4 attempted), GPT abstains readily (3/4 unscoreable). **Verdict:** selects neither plan branch — (1) fix the value-typing bug, (2) user decides practical-axis scope, (3) proceed to Tier-1 direct-read on the legal axis across the 6 CPI-2015 de-jure chunks, (4) Phase-2 verifier needs an explicit abstention-calibration policy. Writeup: [`results/20260518_tier_0_direct_read_writeup.md`](docs/active/extraction-harness-brainstorm/results/20260518_tier_0_direct_read_writeup.md). Convo: [`convos/20260520_tier_0_direct_read_execution.md`](docs/active/extraction-harness-brainstorm/convos/20260520_tier_0_direct_read_execution.md). **Addendum (same session):** user clarified the `practical` axis is the de facto axis = Prong 2's job (not an open data-source question); writeup amended accordingly. **Tier-1 plan written:** [`plans/20260520_tier_1_direct_read_legal_axis.md`](docs/active/extraction-harness-brainstorm/plans/20260520_tier_1_direct_read_legal_axis.md) — legal-axis run over the 6 CPI-2015 de-jure chunks + typing fix + σ_noise (N=3). **Next:** execute the Tier-1 plan.

- **2026-05-19 (Tier-0 direct-read plan: Steps 1–4 of 7 executed on Dans-MacBook-Air; Step 5 pending on a keyed machine)** — [extraction-harness-brainstorm] **Pure-code subset of [`plans/20260518_tier_0_direct_read_smoke_test.md`](docs/active/extraction-harness-brainstorm/plans/20260518_tier_0_direct_read_smoke_test.md) shipped (no API dispatch).** Four commits on top of `cce8542`: (1) `62e02c0` relocates `EvidenceSpan` to `src/lobby_analysis/models_v2/citations.py`, breaking the cold-load cycle `chunks_v2 → models_v2.cells → retrieval_v2 → brief_writer → chunks_v2`; `retrieval_v2` re-exports for back-compat. New `tests/test_v2_cold_load.py` (3 tests) catches subprocess-fresh-interpreter load + asserts `models_v2.cells` doesn't pull `retrieval_v2` into `sys.modules` + back-compat identity preservation. (2) `a7fbbb6` `uv add openai` → 2.37.0. (3) `02cad4f` `scripts/tier_0_direct_read_smoke.py` with shared JSON-Schema dicts + SDK wrappers + `parse_response(response, sdk)` returning `list[ParsedToolCall]` (14 parser tests, fixture-shaped to documented Anthropic/OpenAI contracts). (4) `b0a1b2d` smoke-test body — preflight (exit 2 cleanly when keys absent, verified), statute loader (30 OH 2025 sections), cached system prompt, dispatch path for both models, `_instantiate_cell` adapter covering scalar + dict-shape cells, raw + parsed JSON output with structured `provenance` field, side-by-side printer, $1/call cost ceiling. Test deltas: 480 → 497 passing (+14 parser, +3 cold-load); same 3 pre-existing `test_pipeline.py` baseline failures (unrelated). **Plan open questions resolved:** Q1 (EvidenceSpan home → `models_v2/citations.py`), Q3 (cited_section/justification placement → per-cell wrapper dict). Q2 + Q4 deferred. **Caveats for the next agent:** pricing in `_PRICING_USD_PER_MTOK` is opus-4-**6** rates standing in for opus-4-**7** (best-guess until verified); OH 2015 IS on Dans-MacBook-Air now (prior session asserted absent); dict-shape cell adapter path isn't exercised by `enforcement_and_audits` (all 4 cells are scalar Binary/Enum/Graded); cost ceiling aborts whole run if any one call exceeds $1. **Convo:** [`convos/20260519_session_end_steps_1_to_4.md`](docs/active/extraction-harness-brainstorm/convos/20260519_session_end_steps_1_to_4.md). 4 commits pushed to origin/extraction-harness-brainstorm. **Next:** on Dans-MacBook-Pro or tarragon, `git pull && uv run python scripts/tier_0_direct_read_smoke.py` to execute plan Steps 5–7.

- **2026-05-18 → 19 (Tier-0 execution attempted; architecture pivoted to direct-read)** — [extraction-harness-brainstorm] **Attempted to execute the Tier-0 plan; pivoted to direct-read architecture after 4 preconditions failed.** Tier-0 plan named (1) data path `~/data/statutes/OH/2015/` which doesn't exist on this machine (actual canonical: `~/data/lobby_analysis/statutes/OH/<vintage>/`; OH 2015 absent, only 2010 + 2025 present); (2) `ANTHROPIC_API_KEY` in env (the laptop is keyless, as documented in `retrieval_v2/docs.md:78`); (3) "thin wiring script" inlining scoring_v2 (the 1380-line impl plan exists; no shipped code — inlining ≈ 500 lines, not "thin"); (4) `chunks_v2 ↔ retrieval_v2` circular import surfaced under cold-load (test suite missed it because tests import lazily inside functions). User authorized retarget 2015→2025 (commit `2b9528c`: plan retarget + path correction) before the deeper precondition failures forced a stop. **Architecture pivot:** retrieval+Citations+bundle-expansion → escape-hatch; **direct-read with cross-model side-by-side (Claude Opus 4.7 + GPT 5.2)** → YAGNI primary. Rationale: no empirical evidence the 2-call architecture is necessary; statute fits in cached system prompt (~50K tokens); per-chunk user message + `record_cell` tool with free-text section citation + 1-sentence justification gives machine-checkable provenance via downstream verifier (Phase 2). Old plan moved to `plans/_tabled/` with SUPERSEDED banner. **New plan written:** [`plans/20260518_tier_0_direct_read_smoke_test.md`](docs/active/extraction-harness-brainstorm/plans/20260518_tier_0_direct_read_smoke_test.md) — relocate `EvidenceSpan` to `models_v2/citations.py` (fixes cycle structurally) + `uv add openai` + dual-model smoke against OH 2025 `enforcement_and_audits` chunk + hand-eyeball + writeup. **Convo:** [`convos/20260518_tier_0_execution_pivot_to_direct_read.md`](docs/active/extraction-harness-brainstorm/convos/20260518_tier_0_execution_pivot_to_direct_read.md). 2 commits this session (plan retarget `2b9528c` + this finish-convo bundle); no code shipped — pivot fully captured in docs for the next agent. **Next:** execute new direct-read plan on a machine with both API keys (Pro or tarragon).

- **2026-05-18 (Tier-0 plan write, review, follow-ups)** — [extraction-harness-brainstorm] **Two sessions on this branch land the Tier-0 plan + its load-bearing follow-ups.** (1) Walked the 2026-05-16 review synopsis end-to-end → resolved Fork 1 (EvidenceSpan duplication: `retrieval_v2.EvidenceSpan` keeper, `models_v2.EvidenceSpan` retired) + reframed H-F2 (Ralph loop IS the orchestrator) + scoped Tier-0 / Tier-1 against CPI 2015 vs OH 2015 ground truth → wrote `plans/20260518_tier_0_minimal_pipeline.md` (1-chunk OH 2015 smoke test). (2) Reviewed Tier-0 plan critically; surfaced dependency-graph blocker (the "WIP brief-writer" misnamed — scoring_v2 module doesn't exist on this branch) → dispatched subagent to write `plans/20260514_brief_writer_implementation_plan.md` (commit `067dfac`, 1400 lines, 51 named tests across 6 files, full v2 prompt + tool schemas inlined, mirrors retrieval impl plan structure) → resolved EvidenceSpan duplication in code (commit `0979779`: deleted `models_v2/provenance.py` + test file, migrated `CompendiumCell.provenance` to `tuple[retrieval_v2.EvidenceSpan, ...]`; 480 pass / 8 skip / 3 pre-existing `test_pipeline.py` baseline failures unchanged) → collapsed Tier-0 Step 2 + scoring_v2 Phase 7 to verify-already-done (commit `2d8e395`) → applied two Tier-0 fixes (hop=2 lock, prompt-sha tracking). Convos: [`docs/active/extraction-harness-brainstorm/convos/20260518_synopsis_walkthrough_and_tier_0_scoping.md`](docs/active/extraction-harness-brainstorm/convos/20260518_synopsis_walkthrough_and_tier_0_scoping.md) + [`docs/active/extraction-harness-brainstorm/convos/20260518_tier_0_review_scoring_v2_plan_evidencespan_resolve.md`](docs/active/extraction-harness-brainstorm/convos/20260518_tier_0_review_scoring_v2_plan_evidencespan_resolve.md). 4 unpushed commits as of finish-convo. Held for next: 4 Tier-0 issues (Step 2 testing-anti-pattern, success criterion #3 narrowness, Step 6 refactor wording, cost-budget rebaseline) likely cleaner to address after scoring_v2 ships and API surface is concrete. **Next:** implementation session for scoring_v2 (API-launched sub-branch, strict TDD per now-existing plan), or practical-axis brief-writer brainstorm (Q6 deferred sibling).

- **2026-05-14 (harness review dispatch)** — [extraction-harness-brainstorm] **Post-framing audit of the 3 shipped v2 components + `scoring_v2` brainstorm lock against `docs/RESEARCH_ARC.md`; 3 findings logged.** Dispatcher session: spawned a `general-purpose` subagent against [`plans/_handoffs/20260514_post_framing_harness_review_handoff.md`](docs/active/extraction-harness-brainstorm/plans/_handoffs/20260514_post_framing_harness_review_handoff.md); report committed by the subagent at [`docs/active/extraction-harness-brainstorm/results/20260514_post_framing_review.md`](docs/active/extraction-harness-brainstorm/results/20260514_post_framing_review.md) (commit `0ccbb86`). Convo [`20260514_post_framing_harness_review.md`](docs/active/extraction-harness-brainstorm/convos/20260514_post_framing_harness_review.md). **Findings:** (1) **SHOULD-FIX** — `EvidenceSpan` is two incompatible public classes: `models_v2.EvidenceSpan` (section_reference + 200-char quote) and `retrieval_v2.EvidenceSpan` (citation_type + char/page/block indices); the `scoring_v2` brainstorm Q8 locked `CompendiumCell.provenance: tuple[EvidenceSpan, ...]` without naming which class. Cannot be unified by aliasing — statute-axis semantics vs Citations-API provenance shape. Decision needed before the `scoring_v2` impl plan is written. (2) **SHOULD-FIX** — Ralph loop has no home in the 4-component architecture: noise-floor re-runs at fixed prompt-sha + per-rubric loss aggregation across (state, vintage) aren't ownable by any of `models_v2` / `chunks_v2` / `retrieval_v2` / `scoring_v2`. "The orchestrator" is uniformly named as out-of-scope across all 3 brainstorms. Recommendation: name the orchestrator as the next-component-after-`scoring_v2`. Pairs with the parallel compendium-review's BLOCKER finding (per-rubric `Σ |diff|` normalization) — both findings imply the Ralph-loop layer needs explicit named ownership. (3) **OBSERVATION** — Practical-axis seam in `StateVintageExtraction`: legal-only scoring leaves 50 practical-only cells + 5 dual-axis practical halves missing; Q6 defers practical-axis brief-writer correctly, but the cell-space is flat across registry / chunks manifest / `StateVintageExtraction.cells`. Without a typed-state distinction (`partial=True` flag, or separate Legal/PracticalAxisExtraction types merging into StateVintageExtraction), Phase C consumers can silently project from half-filled SMRs. **Q1 (Phase C ergonomics) clean** — dict-access pattern is a natural fit; small accessor helper recommended. **Q3 (practical-axis reuse budget):** ~30-40% structural carry-forward; tools / document-blocks / prompt content are statute-specific. **Q4 (scale failures):** one quiet T2+ mode — unknown tool names get skipped silently after citation-buffer reset (masks model-hallucinated tool names); recommend a minimum-emit-count diagnostic. **Q5 (brainstorm reopen):** only Q8 (provenance shape) needs reopening; Q3 (retrieval annotations in user text) borderline — `ExtractionRun.prompt_sha` semantics need clarification (just prompt-sha, or `(prompt_sha, retrieval_output_sha)`?). **Q6 (pause-and-surface):** already wired into brief-writer handoff lines 72, 192, 210. Sibling parallel review at commit `770f866` (compendium-2.0 audit; different scope, different report path: `docs/historical/compendium-source-extracts/results/20260514_post_framing_review.md`); cross-linked in both RESEARCH_LOG entries. Reviewer subagent left alive (id `a00a165fb531e34aa`) for follow-up clarification without re-spawning. **Next:** user decides 3 open questions (provenance EvidenceSpan shape; orchestrator-as-next-component vs four-components-suffice; prompt_sha includes retrieval_output_sha?); then `scoring_v2` impl plan gets written per [`plans/_handoffs/20260514_brief_writer_impl_plan_write_handoff.md`](docs/active/extraction-harness-brainstorm/plans/_handoffs/20260514_brief_writer_impl_plan_write_handoff.md) with Findings 1-3 folded into scope.

- **2026-05-14 (compendium review dispatch)** — [extraction-harness-brainstorm] **Post-framing audit of Compendium 2.0 against `docs/RESEARCH_ARC.md`; 4 findings logged.** Dispatcher session: spawned a `general-purpose` subagent against [`plans/_handoffs/20260514_post_framing_compendium_review_handoff.md`](docs/active/extraction-harness-brainstorm/plans/_handoffs/20260514_post_framing_compendium_review_handoff.md); report committed by the subagent at [`docs/historical/compendium-source-extracts/results/20260514_post_framing_review.md`](docs/historical/compendium-source-extracts/results/20260514_post_framing_review.md) (commit `770f866`). Convo [`20260514_post_framing_compendium_review_dispatch.md`](docs/active/extraction-harness-brainstorm/convos/20260514_post_framing_compendium_review_dispatch.md). **Findings:** (1) **BLOCKER for Phase C** — Ralph-loop per-state per-atomic-item ground truth exists only for CPI 2015 (700 cells) and Sunlight 2015 (200 cells); the other 6 rubrics validate at sub-aggregate / weak-inequality / zero-US-state granularity, so `loss(prompt) = Σ |diff|` will silently overweight whichever rubric has the most projectable summands unless `phase-c-projection-tdd` picks an explicit per-rubric normalization at kickoff. RESEARCH_ARC §"Three risks" risk #1 names this abstractly; this is the concrete version. (2) **SHOULD-FIX** — TSV has all 181 rows `status=firm`, but `compendium/README.md` + `compendium/NAMING_CONVENTIONS.md` claim "180 firm + 1 path_b_unvalidated"; OS-1's unvalidated status is encoded only via `n_rubrics=0` + a magic string in `rubrics_reading`. Pick a side. (3) **SHOULD-FIX** — Legal-vs-practical gap is queryable for *artifact existence* (5 dual-axis rows) but not for *content-field surfacing*: 35 `lobbyist_spending_report_includes_*` + 13 `lobbyist_reg_form_includes_*` + 9 `lobbying_contact_log_includes_*` rows are legal-only with no practical sibling. Decision belongs to Prong-2 brief-writer at spin-up, not v2 re-opening. (4) **OBSERVATION** — 9 projection mapping docs use pre-rename row IDs (247 occurrences) by §10.1 design; Phase C lookups must route through the resolver. Cheap mitigation: `tools/check_mapping_doc_row_ids.py` as a pre-merge guard. **Q2 (axis seam) clean.** Q5 candidate: OS-1 (`separate_registrations_for_lobbyists_and_clients`); flagged per handoff, not proposed for removal. **Reviewer punted on** PRI 2010 mapping deep-read (919 lines; spot-check only) and on whether `compendium-v2-promote`'s deprecation broke any caller. Sibling parallel review at commit `0ccbb86` (harness internals; different scope, different report path: `docs/active/extraction-harness-brainstorm/results/20260514_post_framing_review.md`) — was already on the branch when this session's reviewer finished; recorded for context. Neither review pushed until this finish-convo. Process note: agent jumped to "clobber" mid-verification on identical basenames before checking parent dirs; corrected within the same turn. **Mid-session continuation (2026-05-15):** consolidated plan written at [`plans/20260515_apply_post_framing_review_recommendations_plan.md`](docs/active/extraction-harness-brainstorm/plans/20260515_apply_post_framing_review_recommendations_plan.md) — 6 phases scoped to this branch covering both reviews' actionable findings; Phases 3 (EvidenceSpan duplication) and 4 (SMR partiality marker) are brainstorm-gated so the fresh agent doesn't silently relock what reviewers flagged; Phase 5 writes handoff notes for deferred findings to other branches. Multi-committer artifact noted in RESEARCH_LOG: `58a8222` absorbed the parallel harness-review session's mid-flight RESEARCH_LOG edit; their finish-convo `e93c7db` (2 minutes later) closed the link graph. **Next:** fresh agent executes the plan; user decides up-front (a) whether Phases 3/4 brainstorm in-session or stay as gates and (b) whether to split the plan into smaller plans for parallel execution.

- **2026-05-14 (research arc doc)** — [extraction-harness-brainstorm + main] **Three-prong project arc + Prong 1 internals + Ralph loop doc landed on main.** Convo [`20260514_research_arc_doc.md`](docs/active/extraction-harness-brainstorm/convos/20260514_research_arc_doc.md). New repo-level doc [`docs/RESEARCH_ARC.md`](docs/RESEARCH_ARC.md) (213 lines, 2 mermaid diagrams). Started as branch-status query, evolved into research-arc review. **Three user-initiated reframes of the agent's mental model:** (1) Phase C is the Ralph-loop evaluation function, not a downstream sanity-check — no 50-state typed-cell ground truth exists, so projecting cells back into each rubric's published scoring is the only tractable accuracy signal for Prong 1; (2) Prong 2 + Prong 3 is the product, Prong 1 is upstream scaffolding (SMR encodes what each state's regime *legally requires*; gap vs. what portals *actually expose* is itself a research artifact; shared typed-cell schema makes Prong 2 dramatically cheaper); (3) Locked rubric order is mostly convenience, not a rigid signal-strength gradient (respects dependencies, starts where ground truth is strongest, but reordering on empirical results is fine). **Ralph-loop concretized:** objective `loss(prompt) = Σ over (state, vintage, rubric) |f_rubric(SMR) − published_score|`; single scalar. Noise floor `σ_noise` from independent re-runs is prerequisite (otherwise loop chases 1σ flukes; distinct from Track A's across-vintage stability check). Three risks named: implicit weighting in `Σ |diff|`; Goodhart on projection-distance (rubrics have degenerate solutions, Track A's across-vintage stability is the only out-of-distribution check); cost asymmetry (single-state OH-only ≈ $4/iter vs 50-state ≈ $150/iter under $50–500/mo budget). **Cross-track milestone surfaced:** first Ralph-loop iteration end-to-end needs `scoring_v2` (this branch) + CPI 2015 C11 projection (`phase-c-projection-tdd`) + OH 2015 statute bundle (`oh-statute-retrieval`) converging across three branches. Walks back agent's earlier overclaim that "harness feature-complete after scoring_v2" — that's extraction-complete, not quality-validated. **Cherry-pick path executed cleanly:** pulled 2 unseen commits on origin/extraction-harness-brainstorm (`5f262e9` + `7d6f20d` — desktop T1 + fixture decouple) → committed doc as `ee75e3a` → main worktree fast-forwarded mid-session by `compendium-naming-docs` PR #10 merge → cherry-picked clean as `86dc02e` → pushed both branches. **README.md discoverability link deferred per user.** No code changes; no extraction-harness component movement; no plan revisions. Next: brief-writer/scorer-v2 impl plan per the standing handoff.

- **2026-05-14 (retrieval impl T1 + fixture decouple)** — [extraction-harness-brainstorm] **T1 desktop gate cleared; parser-test fixture decoupled from integration write path.** Convo [`20260514_retrieval_v2_t1_and_fixture_decouple.md`](docs/active/extraction-harness-brainstorm/convos/20260514_retrieval_v2_t1_and_fixture_decouple.md). Ran `uv run pytest tests/test_retrieval_v2_integration.py` against real Anthropic Citations API on Dans-MacBook-Pro: **3/3 pass, ~21s, ~$0.06** (3 separate `messages.create` calls). Real API attaches citations to text blocks, fires `record_cross_reference` tool calls, parser yields valid `RetrievalOutput` with non-empty `evidence_spans`. Opus 4.7's adaptive `thinking` block empirically confirmed in real responses (parser passes through as plan documented). **3 parser unit tests then went red against the now-overwritten fixture** — exactly the side-effect pre-flagged in the retrieval impl convo's "Open Questions" as "may bite at T2+; not changing until it shows real noise." It bit on first real-API run. Diagnosis: NOT a parser bug; **test design coupling** between integration-test write target and parser-unit-test consume target. Per plan Phase 7's "pause-and-surface, don't silently patch" directive, surfaced to user with 3 options; user locked **Option A: split fixture paths**. One commit (`5f262e9`): rename `sample_response.json` → `sample_response_handcrafted.json` (committed, md5 `d2e3fe0a…`, parser unit tests pin here for edge-case coverage real API may not naturally produce on tiny statute — mixed tool types, multi-tool buffer reset), add `sample_response_real.json` to `.gitignore` (T1 writes here as ad-hoc local-inspection aid; no test consumes it), 2 test-file path updates + docstring cleanup, ruff clean. **Did NOT silently patch the parser or dumb-down the unit tests** — both would have lost real coverage signal. Post-fix: parser unit suite 8/8 green, full retrieval_v2 unit suite 48/48, T1 re-run end-to-end against new path also 3/3 green (verified rename works against live API; second ~$0.06 spent). Surfaced for future work (not done this session): `thinking` block path needs a docstring note in `parser.py`; unresolvable-reference code path is un-exercised against real API on tiny_statute (would need a deliberately-messy fixture to live-test); optional shape-tolerant-against-real-fixture parser test punted under YAGNI (T1 already does this). **Memory note update** — `feedback_pytest_in_worktree.md` got an empirical addendum: `uv run pytest` resolves to worktree venv correctly once `uv sync --extra dev` has been run inside the worktree (uv emits a diagnostic warning and ignores parent shell's `VIRTUAL_ENV`), AND `uv run pytest` matches the pre-approved `Bash(uv *)` rule whereas `.venv/bin/pytest` doesn't match `Bash(pytest *)` (avoid permission-prompt friction). Sequencing unchanged: cells ✓ → chunks ✓ → **retrieval ✓ (T0 + T1)** → brief-writer (brainstorm done in concurrent session, impl plan write next) → practical-axis sibling brainstorm.

- **2026-05-14 (retrieval impl)** — [extraction-harness-brainstorm] **`retrieval_v2` module landed under strict TDD; T1 smoke deferred to desktop.** Convo [`20260514_retrieval_implementation.md`](docs/active/extraction-harness-brainstorm/convos/20260514_retrieval_implementation.md). Executed Phases 1-8 of the [retrieval impl plan](docs/active/extraction-harness-brainstorm/plans/20260514_retrieval_implementation_plan.md) on the laptop (8 commits, `a5d05c5` → `d1fa512`). RED phase: 51 tests written + verified failing (`a5d05c5`). GREEN phases (executed P6 → P4 → P5 instead of P4 → P5 → P6 because brief_writer reads the prompt file at call time): `tools.py` (chunks-coupled enum) → `models.py` (frozen Pydantic: EvidenceSpan/CrossReference/UnresolvableReference/RetrievalOutput) → `retrieval_agent_prompt_v2.md` (chunk-anchored, zero PRI rubric leakage) → `brief_writer.py` (documents + citations + ephemeral cache_control on both system and documents; `claude-opus-4-7` + adaptive thinking + effort=high; no sampling params) → `parser.py` + hand-crafted `sample_response.json` (citation→tool_use proximity pairing with buffer-reset-after-each-tool invariant; polymorphic over SDK Message objects and JSON dicts) → `tiny_statute.txt` fixture for T1 → `__init__.py` exports + `docs.md` + ruff pass. Full suite 400 → **448 pass** (+48 retrieval_v2 = 8 tools + 7 models + 17 brief_writer + 8 parser + 8 prompt invariants), 8 skipped (5 pre-existing + 3 deferred integration), 3 pre-existing `test_pipeline.py` failures unchanged. **2 plan deviations resolved + documented in convo:** (1) phase order P6→P4 because brief_writer's `_PROMPT_PATH.read_text()` requires the file to exist at call time — commit messages preserve plan-numbered phase names; (2) plan's `_format_cell_roster` used `spec.description[:120]` but `CompendiumCellSpec` has only `(row_id, axis, expected_cell_class)` and the compendium TSV has no description column either — adapted to `- {row_id} ({axis}) [{cell_class}]` per cell. **T1 smoke deferred** — laptop has no `ANTHROPIC_API_KEY`; user runs `uv run pytest tests/test_retrieval_v2_integration.py` on Dans-MacBook-Pro to clear T1 gate. Integration test machinery in place: `tiny_statute.txt` 2-sentence fixture committed; skip-state verified (`3 skipped in 0.01s`); plan Phase 7's "Things that may go wrong" section instructs pause-and-surface on docs↔reality divergence (don't silently patch the parser). **Side-effect surfaced for user review:** `test_parser_handles_real_api_response` overwrites `sample_response.json` with the real shape on every successful run — pro (fixture stays in sync; parser re-validates each run) vs con (fixture churn; non-reproducible). Not changing until T2-T4 shows real noise. Sequencing: cells ✓ → chunks ✓ → **retrieval ✓ (T0; T1 desktop-pending)** → brief-writer → scorer-prompt.
- **2026-05-14 (chunks impl)** — [extraction-harness-brainstorm] **`chunks_v2` module landed under strict TDD.** Convo [`20260514_chunks_implementation.md`](docs/active/extraction-harness-brainstorm/convos/20260514_chunks_implementation.md). Executed all 7 phases of the [chunks impl plan](docs/active/extraction-harness-brainstorm/plans/20260514_chunks_implementation_plan.md) end-to-end (7 commits, `087edb6` → `65fa872`). RED phase: 24 tests written + verified failing with `ImportError` (`8450bd6`). GREEN phases: each commit turned exactly its target test file green in sequence (Chunk+ChunkDef → CHUNKS_V2 manifest → build_chunks → __init__ exports → ruff format). Full repo suite went 374 → **400 pass**, 5 skip, 3 pre-existing `test_pipeline.py` `FileNotFoundError`s unchanged from baseline (user-approved). **Deliverable:** `src/lobby_analysis/chunks_v2/` — Chunk + ChunkDef frozen dataclasses (snake_case `chunk_id` regex, non-empty tuple invariants, `axis_summary ∈ {legal, practical, mixed}`); CHUNKS_V2 hand-curated 15-chunk manifest covering 181 TSV rows = 186 cells; `build_chunks(registry=None, manifest=None) -> list[Chunk]` enforcing partition invariant (every cell in exactly one chunk; combined-axis rows co-located per Q3; missing coverage / unknown row_ids / double assignment all raise loudly); 4-symbol public surface via `__init__.py`; module-level `docs.md`. **3 minor plan deviations resolved + documented:** (1) `ChunkDef` co-located with `Chunk` in `chunks.py` rather than in `manifest.py` per the architecture diagram — avoids the circular import that would have hit Phase 4; (2) `build_chunks` default `manifest=CHUNKS_V2` rewritten as `manifest=None` with lazy import inside the function — same caller behavior, no module-load cycle; (3) `chunk_id` snake_case validation switched from `isalnum()`+`isalpha()` to `re.fullmatch(r"^[a-z][a-z0-9_]*$", ...)` because the original lets `"BadCaps"` through. No manifest refinements applied — plan-author's topic-coherence calls hold. **Retrieval impl sub-branch now unblocked** (Phase 0 already at `4c49888` from killed parallel session; this session left `retrieval_v2/` untouched per handoff). Sequencing locked: cells ✓ → **chunks ✓** → retrieval → brief-writer → scorer-prompt.
- **2026-05-14 (chunks-impl handoff)** — [extraction-harness-brainstorm] **Wrote chunks impl handoff after catching retrieval-plan dependency defect.** After retrieval brainstorm finish-convo, user check-question surfaced that the retrieval impl plan imports from `lobby_analysis.chunks_v2` in 3 places, but `src/lobby_analysis/chunks_v2/` doesn't exist on any branch — chunks brainstorm + plan were done (prior session), but chunks impl hadn't been executed. Confirmed via `git log --all -- src/lobby_analysis/chunks_v2/` (zero commits). Also caught: a parallel agent session had picked up the retrieval impl plan and executed Phase 0 (local commit `4c49888 retrieval_v2: scaffolding`) before hitting the same dependency wall and stopping; user killed that session. The 4c49888 commit is clean (`anthropic>=0.102` in pyproject.toml + uv.lock + empty placeholder files); safe to keep. **Remediation:** wrote [`plans/_handoffs/20260514_chunks_implementation_handoff.md`](docs/active/extraction-harness-brainstorm/plans/_handoffs/20260514_chunks_implementation_handoff.md) for the fresh chunks impl agent (state of play, what not to touch, why retrieval is blocked on chunks); added Prerequisite + Phase-0-already-done sections to the top of the retrieval impl plan so any future retrieval impl session verifies chunks shipped first and skips re-running Phase 0; appended addendum to the retrieval-pickup RESEARCH_LOG entry. Implementation sequencing now locked: cells ✓ → chunks → retrieval → brief-writer → scorer-prompt.
- **2026-05-14 (latest, retrieval pickup)** — [extraction-harness-brainstorm] **Retrieval brainstorm completed; Citations API pivot; TDD-shaped impl plan written.** Convo [`20260514_retrieval_brainstorm.md`](docs/active/extraction-harness-brainstorm/convos/20260514_retrieval_brainstorm.md) (Phase 2 expanded). Resumed at Phase 2 per handoff with 4 hunches re-engaged. Re-anchored on accuracy gate before cost optimization (user pushback on aggregate-cost framing: "if it's dirt cheap but only 50% accuracy, that's not actually a win"). User surfaced **Anthropic Citations API** mid-Phase-2 as load-bearing — reshapes Q2 (output schema can't be Structured Outputs since Citations+Structured Outputs returns 400) and Q6 (Python brief-writer becomes mandatory, no longer YAGNI). Locked package: parameterized `chunks: list[str]` dispatch (default per-chunk for experiments); two tools `record_cross_reference` + `record_unresolvable_reference` (citations attach to preceding text blocks as machine-verified provenance); chunk-level Rule 2 anchors; `anthropic>=0.102` added to `pyproject.toml` (current version verified via PyPI WebFetch 2026-05-14); data dir local-only in worktree; `claude-opus-4-7` + adaptive thinking + effort=high. Implementation plan [`20260514_retrieval_implementation_plan.md`](docs/active/extraction-harness-brainstorm/plans/20260514_retrieval_implementation_plan.md) — 10 phases, full v2 prompt markdown inlined (Phase 6, chunks-plan precedent), full tool schemas inlined (Phase 2 with `chunk_ids_affected.enum` sourced from `build_chunks()` — coupling test catches drift), 30+ test signatures listed by name across 6 test files. **Integration test runs automatically on `uv run pytest`** when `ANTHROPIC_API_KEY` set: 2-sentence statute fixture + `max_tokens=2000` ≈ $0.02/run. Empirical validation gates T0-T4; plan implements through T1 (smoke). **Things locking blind on** (both author and implementer first-time Citations users): citation-to-tool-call attribution; cache_control + citations interaction; plain-text char-level citation accuracy on extracted PDFs. **Phase 7 of plan instructs pause-and-surface if real-API diverges** — explicit don't-silently-patch directive. Brief-writer + scorer-prompt brainstorms remain after retrieval lands.
- **2026-05-14 (latest)** — [extraction-harness-brainstorm] **Chunks brainstorm-and-plan end-to-end; retrieval brainstorm started but unfinished (handoff written).** Convo files: chunks [`20260514_chunks_brainstorm.md`](docs/active/extraction-harness-brainstorm/convos/20260514_chunks_brainstorm.md) + retrieval [`20260514_retrieval_brainstorm.md`](docs/active/extraction-harness-brainstorm/convos/20260514_retrieval_brainstorm.md). User locked session strategy at outset: **brainstorm-and-plan all 4 downstream components here with user-in-loop, then launch implementations as parallel API sub-branches that merge back to this branch.** Saves Max subscription tokens for design work; uses API credit for TDD execution. Implementation phase = brainstorm + plan = sequential here = stages that need Dan. **Chunks (complete).** 7 Q's resolved. **Critical revision** to the prior brainstorm's Q1 (5-12 rows/chunk): user surfaced **prompt-caching architecture** — statute in cached `system` block, chunk content in uncached `user` — making per-chunk cost largely chunk-size-independent. Revised lock: **~30 row soft cap, 34 hard cap** (for the natural `lobbyist_spending_report` 34-row cluster as a single chunk). Iter-1's 7-row anchor is now a reference point, not a constraint. Q3 same-chunk for both halves of all 5 combined-axis rows. Q4 `list[Chunk]` frozen dataclass. Q6 `src/lobby_analysis/chunks_v2/` parallel to `models_v2/`. Sub-Q's locked via proposal: hand-curated `tuple[ChunkDef, ...]` manifest, coverage test as partition invariant, `build_chunks(registry=None) -> list[Chunk]`. Implementation plan inlines **complete 15-chunk manifest covering 181/181 rows = 186/186 cells** (verified against real TSV; no duplicates, no typos after 1 rebuild). Spiritual successor to iter-1's 7-row `definitions` chunk is `lobbying_definitions` (15 rows). **Retrieval (in progress).** Plan-sketch with 7 Q's; brainstorm Phase 1 reading complete (v1 prompt's 2 rubric-coupling sites identified: Rule 2 PRI anchor + `rubric_items_affected` output schema; legal-vs-practical per-chunk breakdown computed — 5 fully-practical chunks don't need statute retrieval). **Phase 2 stopped at Q1/Q3/Q6 ask.** User wanted to clarify before locking; 4 hunches floated as starting material for re-engagement (per-chunk retrieval cheap under caching; v1 is identify-only; chunk-level anchors may simplify Q3; per-chunk dispatch may collapse the Q2 output schema). **Handoff for next agent:** [`plans/_handoffs/20260514_retrieval_brainstorm_handoff.md`](docs/active/extraction-harness-brainstorm/plans/_handoffs/20260514_retrieval_brainstorm_handoff.md). **No code this session** — docs only: 6 new files (2 plan-sketches, 2 brainstorm convos, 1 impl plan, 1 handoff). Next: next agent finishes retrieval per handoff; brief-writer + scorer-prompt brainstorms remain.
- **2026-05-14 (earlier)** — [extraction-harness-brainstorm] **v2 cell model layer implementation landed under strict TDD.** Convo [`20260514_v2_pydantic_cell_models_implementation.md`](docs/active/extraction-harness-brainstorm/convos/20260514_v2_pydantic_cell_models_implementation.md). Executed Phases 0-9 of the [TDD plan](docs/active/extraction-harness-brainstorm/plans/20260514_v2_pydantic_cell_models_implementation_plan.md) per the handoff's "write all tests before any implementation" override (10 commits, `62daee7` → `c66d808`). RED phase: 68 tests written + verified failing (`44eee71`). GREEN phases: each implementation commit turned its own tests green in sequence (Phase 1 EvidenceSpan → 2 ABC+BinaryCell → 3 numerics → 4 enum/freetext/6-specialized → 5 registry → 6 extraction container → 7 exports+typed loader). Phase 8 ran full suite (374 pass / 5 skip / 3 pre-existing test_pipeline.py failures unchanged — user-approved). Phase 9 = this entry + RESEARCH_LOG + finish-convo. **Deliverable:** `src/lobby_analysis/models_v2/` (CompendiumCell ABC + 15 concrete subclasses; CompendiumCellSpec + 186-entry registry against real TSV; StateVintageExtraction container with cell_id↔key consistency validator; ExtractionRun provenance; EvidenceSpan citation pointer) + `load_v2_compendium_typed()` in `compendium_loader.py`. v1.1 untouched. **Process findings surfaced to user:** (1) plan's "29 distinct cell_types" claim was off (actual 23 — data-driven parser unaffected); (2) specialized cells' `notes` column carries provenance only, not struct shape — struct shapes live in `docs/historical/compendium-source-extracts/results/projections/{focal_2024,hiredguns_2007,newmark_2017,newmark_2005}_projection_mapping.md`; surfaced all 6 to user, all approved as proposed; (3) unanticipated 7th cell_type `typed Optional[int_months] (or enum)` mapped to existing IntCell per user-approved YAGNI. **Cell model layer unblocks 4 downstream components** (chunk-grouping, brief-writer, retrieval-agent v2 generalization, scorer prompt rewrite) — each its own brainstorm-then-TDD cycle. SDK-vs-subagent decision deferred until first LLM-calling component lands.
- **2026-05-14** — [extraction-harness-brainstorm] **Real brainstorm session ran; first TDD-shaped implementation plan landed.** Executed the kickoff plan sketch's Phases 1-3 agenda (commit `b8ba4d4`). **Phase 1 reading** (7 carry-forward artifacts: `src/scoring/scorer_prompt.md` + `retrieval_agent_prompt.md` on main; `docs/historical/statute-retrieval/plans/20260429_multi_rubric_extraction_harness.md`; `origin/statute-extraction:src/scoring/chunk_frames/definitions.md`; v2 TSV; v2 `compendium/README.md`; iter-1 narrative from RESEARCH_LOG since raw data is in gitignored `data/` on paused `statute-extraction` branch). **Three findings flagged:** (1) `compendium-v2-promote` was unmerged at session start — Dan merged it mid-session as `0a6804f`; this branch was `git merge main`'d as `f1de3ee` matching phase-c's merge-not-rebase pattern; (2) Anthropic SDK is NOT in `pyproject.toml` — iter-1 worked via Claude Code subagent dispatch; v2 harness inherits the subagent-dispatch pattern, no SDK in the first implementation plan; (3) v2 TSV's 5 `legal+practical` rows carry axis-conditional `cell_type` strings (e.g., `"binary (legal) + typed int 0-100 step 25 (practical)"`) — plan-sketch's Open Questions missed this; surfaced as Q0. **Phase 2 locked decisions** (full rationale in convo): Q0 cell-ID space = `(row_id, axis)` flat 186-key (user-confirmed via AskUserQuestion); Q1 hybrid prompt granularity (chunked-by-domain 5-12 rows + chunk-frame preamble, same template across chunks); Q2 carry-forward two-pass retrieval + empirical bundle-size measurement task; Q3 per-(state, vintage) deliverable / per-(state, vintage, chunk) execution; Q4 new `src/lobby_analysis/models_v2/` module (user-confirmed via AskUserQuestion); Q5 conditionality as wrapper fields (`conditional` + `condition_text`), not cell-type variant; Q6 provenance inside the wrapper, matching v1.1 `EvidenceSource` precedent. **Phase 3 output:** `plans/20260514_v2_pydantic_cell_models_implementation_plan.md` — 9-phase TDD plan for v2 Pydantic cell models + `CompendiumCellSpec` registry mapped from the 186-cell roster + `StateVintageExtraction` container; pure-data, no LLM calls, unblocks Phase C (which can adopt `load_v2_compendium_typed()` at its own pace). Documented sycophancy check: 6 specialized cell shapes (`UpdateCadenceCell`, `TimeThresholdCell`, etc.) sketched but not pinned — plan tells implementing agent to read each row's `notes` column before TDD'ing. **Coordination item flagged in convo:** `compendium/README.md`'s `{row_id: typed_value}` projection-input phrasing now inaccurate under Q0; needs follow-up update but not blocking. **No code in this session** — doc-only commit; first implementation session begins next agent run. **Process note:** initially committed without running finish-convo (RESEARCH_LOG was updated but STATUS.md was not); user caught it; remediated this session-summary entry + the Active Research Lines status field update before re-committing.
- **2026-05-14 (post-merge execution)** — [compendium-row-id-renames] **Executed the 15 row-ID renames + 1 README typo fix across the repo.** 4 commits on top of merged-PR-#10: (1) archive `compendium-naming-docs` to historical + STATUS swap; (2) renamer module `src/lobby_analysis/row_id_renamer.py` + CLI `tools/v2_update_names.py` + 36 behavior tests (word-boundary regex for the Candidate-5 substring trap; skip-list protects the renamer module, its tests, `compendium/NAMING_CONVENTIONS.md`, `STATUS.md`, and historical/deprecated paths); (3) NAMING_CONVENTIONS.md surgery — §10's 8 Issues marked **DONE 2026-05-14**, inline §10.1 resolver table with 15 row renames + 1 doc filename mapping verbatim, body cross-refs through §§2/3/6/7/9/11 updated; (4) script applied — 4 files / 19 substitutions (TSV 15, README typo 1, freeze_canonicalize_rows.py 2, union_projection_rows.py 1). Architecture deviates from plan: **no v2→v3** (Dan's "retroactively re-finalize v2" — filename unchanged, content updated in place), **standalone find-and-replace tool** rather than `freeze_canonicalize_rows.py` extension. Test design: byte-identity invariant test made idempotency-aware. Verifications: TSV 181 rows unchanged, 0 old IDs, 15/15 new IDs, 36/36 renamer tests pass, 342/345 full suite (3 pre-existing env failures in `test_pipeline.py` identical to main). Convo [`convos/20260514_rename_execution.md`](docs/active/compendium-row-id-renames/convos/20260514_rename_execution.md). **Handoff for sister-branch absorption** (post-merge to main): each owner runs `git fetch origin && git merge origin/main` (resolve TSV conflict by taking main's version), then `uv run python tools/v2_update_names.py --apply`, commits as `"absorb v2 row-ID renames from main"`, pushes. Per multi-committer rule, owner work — not handoff-agent work — for `extraction-harness-brainstorm` (actively moving; coordinate with Amina), `phase-c-projection-tdd`, `oh-statute-retrieval`.
- **2026-05-14 (sub-3)** — [compendium-naming-docs] **Sharpening pass on plan + NAMING_CONVENTIONS — C3 unit-suffix resolved, LV-1 schema-coverage exception memorialized.** Pre-implementation review of sub-2's plan surfaced 3 things worth doing before the rename-execution branch fires: (a) C7's content-rename framing in the plan, (b) C3's `_<unit>` suffix question punted to execution time, (c) C2's LV-1 schema-coverage-vs-report-contents rule needing persistent memorialization. Dan's calls: leave C7 framing as-is (§1 + §3 of the plan already cover the byte-identity guarantee); pick C3 now and document the logic; yes, memorialize LV-1. **C3 resolution: `_threshold_<measure>_<unit>` is canonical, not drift.** NAMING_CONVENTIONS.md §9 already encoded `_threshold_*_dollars` / `_threshold_time_percent` as the canonical suffix shape; the existing single-measure precedent `lobbyist_filing_de_minimis_threshold_dollars` carries the unit suffix even though `cell_type` also does (intentional redundancy so readers of the row_id alone know the unit). C3 extends to multi-measure via the rule "include the measure word when the family contains multiple measures, omit when there's only one." C3 *fixes* drift in current names; it does not introduce new drift. **4 doc edits:** `compendium/NAMING_CONVENTIONS.md` §5 (LV-1 categorical-exception paragraph), §7 (threshold-suffix convention with single-vs-multi-measure rule + intentional-redundancy rationale), §11 (schema-coverage decision-tree branch added under "What kind of observable is it?", positioned right after spending-report so new-row authors hit the LV-1 rule in the decision tree, not only in the §5 narrative); `plans/20260515_rename_execution_plan.md` §5 E2 (replaced deferred-with-uncertainty wording with the resolved rule + back-reference to NAMING_CONVENTIONS.md §7). Plus RESEARCH_LOG sub-3 entry. **What did NOT change:** C7 framing (Dan's call); §10 candidate descriptions (still candidates — they don't land until the execution branch); §3 testing list (byte-identity on non-ID columns already covers C7's cell_type/provenance preservation); v2 TSV (immutable contract on this branch). Doc-only commit; no code changes; no test run. Convo: [`convos/20260514_plan_sharpening.md`](docs/active/compendium-naming-docs/convos/20260514_plan_sharpening.md). **Handoff: nothing new for the execution agent.** Plan is now fully self-contained; no naming calls remain for branch-cut time.
- **2026-05-14 (sub-2)** — [compendium-naming-docs] **Rename-candidate walkthrough + rename-execution plan drafted.** Successor session to the audit-v1 kickoff. Walked Dan through all 8 §10 rename candidates one at a time (accept / defer / reject), each accompanied by a pre-scanned downstream-consumer fan-out. **All 8 accepted** → 15 row renames + 1 README typo fix. Candidate-2 LV-1 sub-decision: `lobbyist_filing_*` (semantic faithfulness — LobbyView schema-coverage observable) over `_spending_report_*` (cluster uniformity); pushback delivered on consistency-axis framing. Downstream-consumer pre-scan resolved a key question: v1.1 Pydantic models in `src/lobby_analysis/models/` are **row-ID-agnostic** (no candidate row IDs hard-coded in `compendium.py`, `state_master.py`, or `compendium_loader.py`) → merge-cost consumer is the **future** v2 Pydantic work on `extraction-harness-brainstorm`. **Deliverables:** [`convos/20260514_rename_review_and_plan.md`](docs/active/compendium-naming-docs/convos/20260514_rename_review_and_plan.md) (per-candidate decisions + LV-1 sub-decision rationale + downstream-consumer pre-scan); [`plans/20260515_rename_execution_plan.md`](docs/active/compendium-naming-docs/plans/20260515_rename_execution_plan.md) (7-section execution brief: rename set with old→new tables per cluster, downstream-consumer fan-out per cluster with Phase 0 sister-branch grep as gating step, test-first behavior tests, 7-phase execution sequence, 7 edge cases, immediate-swap canonical strategy, 5 questions-for-execution-time). Plus minor doc-fix on this branch: §10 Issue 2 header in `compendium/NAMING_CONVENTIONS.md` corrected from "(5 rows)" → "(6 rows)" — the body table was already 6 rows but the header was a kickoff-session typo. Execution timing: deferred to a future branch off main when `extraction-harness-brainstorm` Pydantic-model work and `phase-c-projection-tdd` projection-function rollout settle. Doc-only commit; no code changes; no test run (no behavior changes on this branch). Convo `convos/20260514_rename_review_and_plan.md`. **Handoff for execution agent:** the plan is self-contained; the executor cuts a new branch off main, runs Phase 0 (grep sister branches for the 15 old row IDs), then proceeds test-first through Phases 1-7.
- **2026-05-14 (later still)** — [compendium-naming-docs] **Kickoff session: row-naming audit v1 shipped (GH issue #9).** Branch cut off main `f364973`; scope locked to **audit + flag, defer renames** to keep merges clean on sister branches (`phase-c-projection-tdd`, `extraction-harness-brainstorm`, `oh-statute-retrieval`). Three artifacts in one commit `0e94f37`: (1) **`compendium/NAMING_CONVENTIONS.md`** (362 lines) — the deliverable, 13 sections: ID shape, top-level prefix families (20 1-tok / 19 3-tok ≥2-member covering 147 of 181), the def/actor triangle (D25), the α form-type split (D3), joint-actor rows (D7), three-threshold framework (D4), axis conventions, cell-type suffix hints, **8 rename candidates flagged in §10**, prefix-choice decision tree §11; (2) `results/20260514_prefix_survey.{py,md}` reproducible prefix-histogram + family membership + cross-tabs; (3) `results/20260514_provenance_table.{py,md}` per-row provenance generator + table programmatically crediting each of 181 rows to its `first_introduced_by` doc + any of D1–D30 that names it (60/181 have explicit D-refs; 121 survived the freeze unchanged with provenance fully in their projection-mapping doc — making it machine-readable rather than hand-curating 181 entries). **8 rename candidates** surface two main patterns: (a) D3's PRI-only rename rule missed 5–6 FOCAL/Newmark/LobbyView-introduced `_report_*` rows that should be `_spending_report_*`; (b) the three-threshold framework (D4) didn't back-rename the lobbyist-status threshold trio (`compensation_/expenditure_/time_threshold_for_lobbyist_registration` — 3 high-traffic rows including the 6-rubric `compensation_threshold` that's load-bearing for HG Q2 D22). Plus 1 reversed `principal_or_lobbyist_*` ordering, 2 family-fit misfits, 1 plural-drift pair, 2 defensible-but-outside-`def_*` definition rows, 1 README filename-drift incidental. **Rename execution stays deferred** per kickoff decision; sister branches read-only on `compendium/` at this commit. Branch pushed to `origin/compendium-naming-docs`; no PR, no GH issue #9 comment, no test run (doc-only). Convo `convos/20260514_naming_taxonomy_kickoff.md`. **Handoff for next session:** walk Dan through the 8 rename candidates in §10 of `compendium/NAMING_CONVENTIONS.md` one-at-a-time (accept/defer/reject), draft `plans/20260515_rename_execution_plan.md` with the accepted set + each rename's downstream-consumer fan-out (TSV regen via `tools/freeze_canonicalize_rows.py`, projection-mapping doc cross-refs, future `extraction-harness-brainstorm` Pydantic models, prompt strings).
- **2026-05-14 (later)** — [compendium-v2-promote + extraction-harness-brainstorm + phase-c-projection-tdd] **v2-promote landed; `⛔` PRI-out-of-bounds banner cleared; plan sketches landed on both compendium-consumer successors.** Multi-branch session. Coordination check between the two parallel-running compendium-consumer branches (`extraction-harness-brainstorm` Track B / `phase-c-projection-tdd` Phase C) surfaced shared-infrastructure conflict surface (v1 CSV deprecation, v2 TSV path, loader, models, STATUS.md banner wording). Cut `compendium-v2-promote` off main to land the deprecation work in one place. **File moves (`git mv`):** v1 CSVs → `compendium/_deprecated/v1/` (preserved, not deleted, per "mv over rm for research artifacts"); v2 TSV → `compendium/disclosure_side_compendium_items_v2.tsv` (181 rows × 8 columns; promoted from `docs/historical/...`); `.MOVED.md` forwarding stub left at old historical path with new-path + update instructions + warning. **New docs:** `compendium/README.md` (v2 source-of-truth declaration; row-shape contract; regeneration via `tools/freeze_canonicalize_rows.py`); `compendium/_deprecated/v1/README.md` (v1 deprecation context; cites 2026-05-02 v3 audit's 186 concerns / 109 of 141 rows flagged / 24.2% inter-auditor agreement). **Code:** `load_compendium` renamed to `load_v1_compendium_deprecated` with deprecation docstring; new minimal `load_v2_compendium() → list[dict[str, str]]` (raw dicts on purpose — typed Pydantic v2 models belong to `extraction-harness-brainstorm`'s surgery per the model-shape = extraction-output-shape principle); 4 dependent path-constant files updated (`scripts/build_compendium.py`, `src/scoring/orchestrator.py`, `tests/test_compendium_loader.py`, `tests/test_smr_projection.py`). **Tests:** 300 pass / 5 skip (3 new v2-loader smoke tests via real RED-GREEN TDD cycle above 297 baseline); v1-loader regression 34 pass / 5 skip (same as pre-rename). 3 pre-existing `test_pipeline.py` failures (portal-snapshot fixture data not bundled) identical on main — not introduced here. **Banner removal:** entire `## ⛔ AGENT-CRITICAL: PRI 2010 IS OUT OF BOUNDS (2026-05-02)` section deleted from STATUS.md. Substantive "no rubric privileged" content survives as ⭐ success criterion #4 (per-rubric projections are sanity checks, not the goal). Banner was emergency posture for the bootstrapping-from-PRI failure mode; that mode is structurally resolved by the v2 cell-typed observable shape. **Phase C now operates** — `papers/text/PRI_2010__state_lobbying_disclosure.txt` is reading material for the PRI 2010 projection function (rubric #2 in locked Phase C order), no longer rule-1-blocked. **Process notes raised by user mid-session:** initially skipped reading `using-skills/SKILL.md` and skipped putting the Nori checklist itself into TodoWrite — remediated by reading 4 skills fresh during the session; initially wrote `load_v2_compendium()` before tests — remediated with a real RED-GREEN TDD cycle. **Post-merge actions for successor branches:** update RESEARCH_LOG row-freeze-contract path links to `compendium/disclosure_side_compendium_items_v2.tsv` (applies to `oh-statute-retrieval`, `extraction-harness-brainstorm`, `phase-c-projection-tdd`). **Plan-sketch follow-up (same session, separate commits on the respective branches):** after the v2-promote commit landed (`8958c0b`), Dan flagged that the two compendium-consumer successors had only skeleton `RESEARCH_LOG`s and empty `plans/` directories. Sketched plans for both: **`extraction-harness-brainstorm`** commit `235ed50` — brainstorm-first plan covering 6 architectural questions to resolve before TDD (prompt granularity, retrieval, iteration unit, Pydantic shape, conditional/materiality cells, provenance); recommended first TDD-able component is v2 Pydantic cell models. **`phase-c-projection-tdd`** commit `d864c0d` — concrete TDD plan starting with CPI 2015 C11 (5-tier de facto scoring; per-item TDD cycles + empirical aggregation-rule fit against 700-cell + 50-state ground truth); 5-phase agenda through all 8 rubrics with PRI-MVP retirement after rubric #2 and cross-rubric agreement audit at the end. Both branches' Row-freeze contract links migrated to new `compendium/` path in same commits. Doc-only commits; no code changes on harness/phase-c branches. Handoff sentences for kickoff agents captured in this session's convo summary on the v2-promote branch.
- **2026-05-14** — [main] **Post-merge archive + successor-cut session.** Picked up `compendium-source-extracts` Phase 4 (archive) + Phase 5 (3 successor branches) per [`docs/historical/compendium-source-extracts/plans/_handoffs/20260514_factual_audit_and_merge.md`](docs/historical/compendium-source-extracts/plans/_handoffs/20260514_factual_audit_and_merge.md), which the prior session left at "Phases 1-3 done, merge commit `cac1469` on main." **Pre-archive cleanup (commit `7a107ea`):** renamed `data/compendium/` → repo-root `compendium/` so `data/` is fully gitignored and the use-worktree skill's `data/` symlink convention works without conflict (previously failed because `data/compendium/` was a tracked exception making `data/` exist as a real dir in every worktree). Updated 5 load-bearing path constants in `src/lobby_analysis/compendium_loader.py`, `src/scoring/orchestrator.py`, `scripts/build_compendium.py`, `tests/test_compendium_loader.py`, `tests/test_smr_projection.py`; updated `.gitignore` (dropped `!data/compendium/` exception) + `docs/COMPENDIUM_AUDIT.md` (2 future-output path refs). Targeted tests 34 pass / 5 skip. **Phase 4 archive:** `git mv docs/active/compendium-source-extracts → docs/historical/` (151 files); STATUS.md updated — removed compendium-source-extracts from Active table, added archived row with full Phase-A/B/freeze/audit summary, prepended this session entry, updated the 2026-05-02 Current Focus paragraph with a 2026-05-14 update line. **Phase 5 successors:** cut 3 worktrees off main with seeded `docs/active/<branch>/` skeletons (RESEARCH_LOG.md stub + empty convos/plans/results dirs), no kickoff plans (each kickoff session's work): `oh-statute-retrieval` (Track A; OH 2007 + OH 2015 added to OH 2010 + OH 2025 multi-vintage set + HG 2007 ground-truth retrieval), `extraction-harness-brainstorm` (Track B; design the single extraction pipeline per Compendium 2.0 success criterion #2 "ONE extraction pipeline"; inherits prompt-architecture from now-dead `statute-extraction` iter-2), `phase-c-projection-tdd` (per-rubric projection TDD in locked order CPI 2015 C11 → PRI 2010 → Sunlight 2015 → Newmark 2017 → Newmark 2005 → Opheim 1991 → HG 2007 → FOCAL 2024 — 8 score-projection rubrics; LobbyView is schema-coverage). All 3 branches pushed to origin.
- **2026-05-13 (factual-audit pre-merge)** — [compendium-source-extracts] **FACTUAL-ACCURACY AUDIT LANDED.** Phase 1 of the merge handoff per [`plans/_handoffs/20260514_factual_audit_and_merge.md`](docs/active/compendium-source-extracts/plans/_handoffs/20260514_factual_audit_and_merge.md). Direct-read audits (no subagent) of the 3 load-bearing source-rubric summaries against `papers/text/` extractions: **Newmark 2017** (1,173 lines, all 11 verifiable numerical claims confirmed exactly — including the load-bearing **r=0.04 CPI↔PRI-disclosure correlation** at paper p.421-422 that anchors the rubric-unification rationale, plus r=0.27 / 0.40 / 0.52 / 0.54, 19-item index, range 7-19, mean 12.96, SD 2.63, alpha 0.67, top/bottom-7 state lists, AZ 6→16 mover, factor analysis 4-6 factors); **FOCAL/Lacy-Nichols 2024** (1,468 lines, 14/16 confirmed exactly — 1,911 records / 15 frameworks / 248 items / 8-cat × 50-indicator / 19-of-109-GDB / financials 14/15 / scope 13/15 / 3 weighted frameworks / Chile contact-log exemplar / enforcement-11/15 / integrity-6/15; **1 real factual error fixed** — restored "contact log in 13/15" with the false parenthetical claiming "the paper does not state a count for contact log" deleted, since paper p.1077-1079 explicitly does state it; **1 clarity tightening applied** — "openness/data accessibility in 9/15" → "openness in 11/15 (open-data sub-theme specifically in 9/15)" since the FOCAL Openness category is in 11 frameworks p.886-887 while the narrower open-data sub-theme is in 9 p.809; both numbers in paper); **PRI 2010** (2,095 lines read in 3 chunks, all checked claims confirmed exactly — methodology rules B1/B2-reverse-scored-per-footnotes-85/86 / E "higher-of-E1/E2 + F/G-double-count + separate-J" / sub-component max counts A:11+B:4+C:1+D:1+E:20=37 / C 0/1-gate-with-3-sub-criteria / D 0/1-gate-with-2-sub-criteria, plus all top/bottom state lists for both rubrics, all averages, SC rank-3-on-disclosure / rank-46-on-accessibility, CT rank-18-on-laws / rank-1-on-accessibility, all CA $ figures $552.6M / $92.6M / $131.4M; one paper-side counting curiosity at "11 states below 50%" vs my Table 5 count of 12 documented but not actioned — paper-side issue, summary faithful). **Newmark 2017 sourcing nit** added to PAPER_INDEX Audit Notes: the parenthetical "lower than 2005's 0.71" is sourced from Newmark 2005 (separate paper, also in collection), not from the 2017 PDF — flag for Phase C agents. PAPER_SUMMARIES.md line 230 corrected; PAPER_INDEX.md Audit Notes block updated with full per-summary verdict. **Branch is now MERGE-READY**; same-session continues to Phases 2-5 (audit commit + push → merge → archive → cut 3 successor branches `oh-statute-retrieval`, `extraction-harness-brainstorm`, `phase-c-projection-tdd`). Convo `convos/20260513_factual_audit_and_merge.md`. **Headline: the framing rationale for the rubric-unification project rests on accurately-reported data — Phase C TDD agents can trust the projection-arithmetic values in these 3 summaries.** Audit was much cleaner than the handoff anticipated: 1 real correction in FOCAL, 1 clarity tightening in FOCAL, 1 sourcing note in Newmark 2017, 0 in PRI 2010.
- **2026-05-13 (paper-audit pre-merge)** — [compendium-source-extracts] Ran `auditing-paper-summaries` skill — last pre-merge gate per the 2026-05-13 row-freeze handoff. **Inventory reality check:** handoff said 17 PAPER_INDEX vs 18 PDFs + ~16 branch-added; actuals were 17 vs **37 PDFs** + **20 branch-added** (handoff numbers were stale by 3+ weeks of parallel paper additions). Dispatched survey subagent over the 20 unindexed papers (single-pass reads, MEDIUM-HIGH confidence) → 20 stub entries with category placement + 1-line index + 2-3-sentence summary. **PAPER_INDEX.md restructured with 2 new categories**: International / Comparative Lobbying Regulation (14 entries: AccessInfo 2015, ALTER_EU 2013, Bednářová 2020, Carnstone 2020, CouncilEurope 2017, GlobalDataBarometer 2022, Hogan/Murphy/Chari 2008, Holman/Luneburg 2012, IBAC 2022, McKay/Wozniak 2020, Mihuț 2008, Roth 2020 [web-source-only], SOMO 2016, TI 2016); Lobbying & Political Outcomes (empirical) (Flavin 2015, Strickland 2014/2018). Existing categories grew: Lobbying Data Infrastructure +LaPira & Thomas 2014; Compliance & Disclosure Quality +Lacy-Nichols 2023 + Chung 2024; State Lobbying Regulation Measurement +CPI 2015 SII. **Roth 2020 orphan investigated and kept** — text is a WebFetch capture of a Google Sites page (lobbymeter.eu); intentionally no PDF. **Lacy-Nichols suppl naming mapping documented in PAPER_INDEX.md Audit Notes** (per user direction: PAPER_INDEX is durable, not just convo). **Factual spot-checks on Newmark 2017 / FOCAL / PRI 2010** originally deferred to Phase C; **user redirected post-`31ba9ce`** — do them BEFORE merge in a fresh session per successor handoff [`plans/_handoffs/20260514_factual_audit_and_merge.md`](docs/active/compendium-source-extracts/plans/_handoffs/20260514_factual_audit_and_merge.md). Convo `convos/20260513_paper_summaries_audit_premerge.md`. **Branch is one factual-audit session away from merge-ready.**
- **2026-05-13 (late-late-late-late eve)** — [compendium-source-extracts] **COMPENDIUM 2.0 ROW FREEZE LANDED.** Full Phase 1-5 brainstorm per `plans/_handoffs/20260513_row_freeze_brainstorm.md`. 30 numbered decisions D1-D30 in `results/projections/20260513_row_freeze_decisions.md`; v2 TSV at `results/projections/disclosure_side_compendium_items_v2.tsv` (181 rows; 180 firm + 1 path-b unvalidated; was 186 v1 → -5 net). **Headline: `lobbyist_spending_report_includes_total_compensation` is now 8-rubric-confirmed** (canonicalization merged PRI E2f_i `lobbyist_report_includes_direct_compensation` + CPI #201 `lobbyist_spending_report_includes_compensation` into the canonical row; doc narratives all cross-cite, mechanical union had under-counted at 6). Key naming canonicalization decisions (D1-D8): D1 + D2 merge compensation cluster (lobbyist + principal sides); D3 renames PRI E1/E2 prefix to `*_spending_report_includes_*` for α-split convention compliance (~30 rows); D4 renames `materiality_threshold_*` (PRI D1) to `lobbyist_filing_de_minimis_threshold_*` per Sunlight three-threshold framework; D5 renames `compensation_broken_down_by_client` to `_by_payer`; D6 splits `def_target_legislative_or_executive_staff` into `legislative_staff` + `executive_staff`; D7 keeps `contributions_received_for_lobbying` combined (YAGNI); D8 renames `lobbyist_disclosure_*` to `lobbyist_reg_form_*`. LobbyView candidates (D12-D15): LV-1 IN; LV-2/3/4 OUT (user overrode the LobbyView mapping recommendation on LV-3 — single-rubric typed-enum complexity not justified). OpenSecrets-distinctive (D16-D18): OS-1 IN under path-b (real distinguishing observable; unvalidated); OS-2/3 stay tabled. Atomization meta-rule (D20): keep current per-rubric atomization — PRI binaries stay binary (cadence 12; Q7a-o 15), FOCAL set-types stay set (scope.1 9-actor; scope.4 8-activity), no collapsing. All 132 single-rubric rows KEEP per Decision Rule 1 (D25-D30 batch by family: actor_*, PRI E1/E2 contents, PRI exemptions/govt_agencies/public_entity_def/law_*, HG-distinctive practical-axis, FOCAL-distinctive, CPI-distinctive). HG-1 (def_target_executive_agency carve-out split) DEFER (D21); HG-2 (Q2 'make/spend') projects as min(comp_thresh, exp_thresh) where both non-null (D22); Opheim catch-all OUT-of-scope (D23); ~7 remaining projection-logic Open Issues deferred to Phase C (D24). 3 semantic cell_type conflicts resolved (D9-D11): downloadable-in-analytical-format → single binary; lobbyist_registration_required + registration_deadline both stay two-axis. v2 generation via idempotent `tools/freeze_canonicalize_rows.py` (~250 lines; encodes D1-D19 as data structures). **Next:** `auditing-paper-summaries` pass → merge `compendium-source-extracts` → main → cut 3 successor branches in parallel (OH statute retrieval [Track A]; extraction harness brainstorm [Track B]; Phase C projection TDD with locked order CPI → PRI → Sunlight → Newmark 2017 → Newmark 2005 → Opheim → HG → FOCAL). LobbyView is schema-coverage, not Phase C.
- **2026-05-13 (late-late-late eve)** — [compendium-source-extracts] Executed §§1-2 of the post-Phase-B forward-planning handoff: union step + pre-merge audit. Pushed back at session start on the handoff's session-scope (union + audits + draft 3 successor plans in one session); surfaced the ordering tension — Option B's whole rationale for row-freeze BEFORE merge is that the 3 successor tracks need a stable row-set contract, but drafting plans against v1 of the union TSV before row-freeze regenerates to v2 creates rework. User scoped to union + audits only this session. Wrote `tools/union_projection_rows.py` (~360 lines) parsing the 9 mapping docs' Summary sections with composite-row expansion (Opheim's 2-cell + Newmark 2005's 8-cell OR-projections expanded into reads-of-PRI-cadence-cells rather than phantom row_ids), cell-type whitespace normalization, hardcoded LobbyView freeze-candidate handling. **Output: `results/projections/disclosure_side_compendium_items_v1.tsv` (186 rows: 182 firm + 4 LV freeze-candidates).** **Headline finding: count is ~73 rows higher than the running ~111 estimate** because mapping doc narratives systematically undercount their own table contributions — PRI doc claims 47 distinct disclosure-law rows but table has 61 (with internal arithmetic error 11+4+4+3+19+18=59); HG doc claims 22 NEW (table has 29; doc says "8 practical Q31/Q32 only" but Q28-Q30 + Q33-Q38 are also NEW); Newmark 2017 doc claims 14 but table has 15. Single-rubric mass is 135 of 182 = **74%** — driven by PRI 81 first-introduced rows (mostly fine-grained PRI-distinctive: A-family actors, B-family exemptions, Q7a-o search-filters, E-family principal/lobbyist parallels), FOCAL 34 (mostly per-meeting contact_log + descriptors), HG 29 (mostly practical-availability access-tier cells). Top cross-rubric rows at 6-rubric: gifts/entertainment lobbyist+principal pair; def_target_executive_agency; compensation_threshold; total_compensation. **Spot-check surfaced cross-rubric naming drift** — the total-compensation observable has 3 distinct row_ids across docs (CPI: `_includes_compensation`; PRI E2f_i: `_includes_direct_compensation`; later mappings: `_includes_total_compensation`). Doc-narrative "7-rubric-confirmed" claims depend on human-author canonical understanding; TSV's mechanical match gives 6. Cross-rubric counts in TSV are lower bounds; canonicalization is row-freeze-brainstorm work. **Audit-docs caught + fixed a structural bug:** `convos/20260503_pm_acquisition_and_descriptives.md` session entry body existed in RESEARCH_LOG (lines ~865-931) but its `### 2026-05-03 (pm) — ...` heading was missing — restored as `### 2026-05-03 (pm) — Blue Book / COGEL acquisition + cross-rubric descriptive stats`. RESEARCH_LOG now has 17 session headers matching 17 convos, all reverse-chronological. **Branch-hygiene flagged ~9MB of embedding `.npy` binaries in branch history** (`embed_vectors__openai__text-embedding-3-large.npy` 6.25MB + 1.6MB across two commits; `embed_similarity_matrix__openai__text-embedding-3-large.npy` 1MB + 72KB) — user decision: purge via `git filter-repo` after this checkpoint (embedding-based assembly was an early exploration superseded by the "one compendium to spawn them all" framing). PAPER_INDEX 17 entries vs 18 PDFs + 16+ branch-added papers flagged for a future `auditing-paper-summaries` session before merge. ~35 results .md files lack explicit `<!-- Generated during: X -->` provenance per skill literal — most have implicit provenance via `**Plan:**` / `**Source artifact:**` / methodology sections; deferred as low-priority busywork not blocking merge. **Next:** filter-repo purge of embedding bloat → row-freeze brainstorm (Option B sequencing) → draft 3 successor plan docs AFTER row-freeze (against canonical row set).
- **2026-05-13 (late-late eve)** — [compendium-source-extracts] **Phase B CLOSED.** Two pieces in one session: (1) FOCAL-1 resolved — `revolving_door.1` pulled IN scope per pushback recommendation (4 grounds: observable shape ≠ FOCAL category label, closes load-bearing US LDA tolerance, trivial compendium cost, symmetric with rows already in scope); FOCAL mapping doc updated in 7 in-place edits + Correction 4 added; NEW row `lobbyist_reg_form_includes_lobbyist_prior_public_offices_held` (binary; legal); US LDA Phase C tolerance closes from +6pt to 0pt on raw points (≤1pp residual percentage delta from denominator shift only); FOCAL mapping row count 57 → 58, NEW 35 → 36, reuse rate 38.5% → 37.9%. (2) **LobbyView 2018/2025 schema-coverage mapping shipped — 9th + FINAL Phase B mapping** (307 lines; different shape from the 8 score-projection mappings). 46 schema fields classified into 5 coverage statuses: 12 COVERED + 2 COVERED-PARTIAL + 4 NOT_COVERED candidate NEW rows (LV-1 in-house-vs-contract; LV-2 amendment flag; LV-3 standardized issue-code taxonomy; LV-4 full-text-search-index split; LV-5 bill_client_link recommended OUT as operational) + 5 OPERATIONAL_METADATA + 25 EXTERNAL_ENRICHMENT (Congress.gov / CRS / Bioguide / GovTrack / Compustat / Kim 2025 GNN features). **Federal_US LDA disclosure-observable coverage = 14/18 = 78%.** Three watchpoints walked: W1 (`contributions_from_others` final promotion check) NO PARALLEL in LobbyView — Newmark 2017's row stays single-rubric across the entire 9-rubric contributing set; compendium 2.0 freeze recommendation KEEP per Newmark-distinctive-observable rationale; W2 (`def_target_*` 4-cell extension validation) CONFIRMED via LDA §17 + 2 USC §1602(4) "covered legislative branch official" definition — all 4 cells `TRUE` for federal jurisdiction; W3 (FOCAL-1 row validation) **CONFIRMED via LDA §18 `covered_official_position`** — the FOCAL-1-introduced row is now 2-rubric-confirmed (FOCAL + LobbyView) within the same session; the FOCAL-1 decision was validated by an independent rubric within hours of being made. Compendium 2.0 row inventory standing at **~111 disclosure-side rows** post-Phase-B-close. Commits: `1ecaf86` (FOCAL-1 update to focal_2024 mapping), `e5ba35b` (LobbyView schema-coverage mapping), `6cc8c3b` (initial checkpoint convo). **Post-checkpoint forward planning landed same session (Option B decided):** next session = union step + pre-merge audits + 3 plan-doc drafts (OH statute retrieval, extraction harness brainstorm, Phase C projection TDD for 8 rubrics); next-next = row-freeze brainstorm BEFORE merge. Forward-planning handoff at `plans/_handoffs/20260513_phase_b_close_and_post_b_plan.md` (self-contained brief for the next-session agent; supersedes the now-stale 2026-05-11 handoff). Three parallel successor tracks are post-union-independent — once union TSV exists, harness brainstorm + projection TDD + OH retrieval can run on separate branches by separate agents.
- **2026-05-13 (late eve)** — [compendium-source-extracts] Phase B FOCAL 2024 projection mapping shipped (8th of 9 rubrics; LobbyView is the final remaining mapping). 48 atomic items in scope per disclosure-only qualifier (revolving_door.* DEFERRED per strict plan reading; revolving_door.1 flagged Open Issue FOCAL-1 — lobbyist's prior public offices disclosed is statutorily disclosure-shaped, scope-qualifier deviation queued for user review). **57 distinct compendium rows touched, 22 reused / 35 new = 38.5% reuse rate (new single-mapping low; below HG's ≥70% prediction)** — driven by FOCAL's per-meeting contact_log atomization (9 NEW rows: beneficiary-org / official-name / institution-dept / attendees / date / communication-form / location / materials-shared / topics-discussed; PRI E1i/E2i is the coarse parent) and per-lobbyist descriptors atomization (5 NEW reg-form-side cells: full-name / contact-details / legal-form / business-id / sector; α form-pair with PRI E2b's spending-report-side cell). Other NEW rows: 5 openness quality features (ministerial-diaries-online, no-user-registration, open-license, unique-identifiers, linked-data, changes-flagged) + 5 financials-distinctive (income-by-source-type, FTE-count, time-spent-reporting, principal-side-total-expenditures, expenditure-per-issue, trade-association-dues) + 2 set-typed cells (scope.1 = 9 organizational actor types; scope.4 = 8 activity types; atomization to 17 binary cells flagged Open Issue FOCAL-2) + 1 staff-target cell (`def_target_legislative_or_executive_staff` extends `def_target_*` family to 4 cells for FOCAL scope.3 partly-tier discrimination) + 1 ministerial-diary cadence (US-N/A but kept for cross-jurisdictional compendium completeness) + 1 2025-only `principal_report_lists_lobbyists_employed` (Lobbyist-list indicator added in Lacy-Nichols 2025 application). **All three handoff watchpoints walked:** (1) `contributions_from_others` parallel in FOCAL `financials.*` battery — **NO PARALLEL** across all 11 items (.3 income-source-types reads OWN income sources not third-party-INCOMING; .9 trade-association dues reads money OUT to third-party orgs not money IN earmarked-for-lobbying; .11 OUTGOING campaign contribs); Newmark 2017's row now single-rubric across **8 of 9** contributing rubrics, LobbyView the last check. (2) Federal LDA validation = 81/182 = 45% per audit doc; with revolving_door deferred, projection max reaches 75/175 = 43% — known 6-point under-scoring fully attributable to revolving_door.1 deferral. (3) 2024 → 2025 application differences encoded in projection logic (timeliness.1+.2 merged read same compendium cell `lobbyist_directory_update_cadence`; 2025-only Lobbyist-list indicator separately documented as a new row not in 2024 source TSV). Promotion-level shifts: `lobbyist_spending_report_includes_total_compensation` stays **7-rubric-confirmed** (most-validated row); bill_or_action_identifier α-pair to **5-rubric-confirmed** (Sunlight + HG + PRI + Opheim + FOCAL); position_on_bill to **3-rubric-confirmed** (Sunlight + Opheim + FOCAL via β AND); employment_type / business_associations_with_officials / campaign_contributions / business_association_disclosure to **2-rubric-confirmed**. 11 Open Issues (FOCAL-1 through FOCAL-11) + 3 Corrections to predecessor mappings (def_target_* extension, employment_type 2-rubric-confirmed, Phase B count update 7 → 8) + Promotions section for compendium 2.0 freeze planning. **`contributions_from_others` compendium 2.0 freeze recommendation: KEEP** per Newmark-distinctive-observable rationale (real but unusual observable across US states) even though likely 1-rubric-confirmed at Phase B close. Result doc: `results/projections/focal_2024_projection_mapping.md` (957 lines); convo `convos/20260513_focal_phase_b_mapping.md`. **Phase B is now 89% complete (8 of 9 mappings done, OS tabled); only LobbyView remains** (schema-coverage check — different shape; 46 schema fields per Kim 2018 + Kim 2025).
- **2026-05-13 (eve)** — [compendium-source-extracts] Phase B continued: shipped HiredGuns 2007 projection mapping (7th rubric; largest single mapping in the remaining set). 38 atomic items in scope per disclosure-only qualifier (2 def + 8 ind-reg + 15 ind-spending + 2 emp-spending + 3 e-filing + 8 pub-access; 9 enforcement + 1 cooling-off OOS); 38 distinct compendium row families touched, **16 reused / 22 new — 42% reuse rate, lowest of any single Phase B mapping so far**. Cross-rubric grep workflow ran cleanly with 4 parallel greps; no rework. **All 7 handoff watchpoints walked:** (1) α form-type split on Q5/Q20 (canonical motivating case) — APPLIED, pre-existing 6 Sunlight α rows reused; (2) Q11/Q14/Q15 item-2 stack — APPLIED, Q15's 5-tier read on `expenditure_itemization_de_minimis_threshold_dollars` is finer than Sunlight's 2-tier; (3) Q13/Q27 compensation pair — APPLIED, Q13 reuses the now-7-rubric-confirmed `lobbyist_spending_report_includes_total_compensation`; (4) Q2 5-tier on CPI #197 typed cell — APPLIED, finest read in contributing set; (5) Q39-Q47 enforcement + Q48 cooling-off OOS; (6) `contributions_from_others` parallel in HG — NO PARALLEL (Q24 is OUTGOING campaign-contributions disclosure, not third-party-INCOMING; row stays single-rubric across 7 of 9 contributing rubrics); (7) `def_actor_class_*` 4th-reader check — NOT a 4th reader (HG Q1/Q2/Q3/Q4 read target/threshold/gateway/deadline, not actor-class definitional inclusion; row family stays 3-rubric-confirmed). **22 NEW compendium rows introduced** — most of any single mapping: 14 legal-axis (Q7 amendment deadline, Q8 photograph, Q9 reg-form employer list, Q10 employment type, Q16-Q19 itemized-detail × 4, Q21 household members, Q22 business associations, Q24 outgoing campaign contributions, Q25 null/no-activity report, + Q12 session-calendar metadata) + 8 practical-axis Q31/Q32 access-tier underlying cells (photocopies / pdf / searchable / downloadable × 2 form types) + 5 other practical cells (Q28/Q29/Q30 e-filing, Q33 copy-cost, Q34 sample forms on web, Q35/Q36/Q37 aggregate-totals provision, Q38 update cadence). Q31/Q32 are the FIRST projection-friendly readers of Sunlight item-4's underlying observables (which were kept in compendium 2.0 even though Sunlight item-4 was excluded from projection). **Discovered: HG plan/handoff item-count was wrong** — both say "47 items, Q1-Q38 + Q49-Q56 in scope" but `items_HiredGuns.tsv` has 48 items (Q1-Q48), disclosure-side scope = Q1-Q38 = 38 items; documented as Correction 1 in mapping doc. **Phase C tolerance issue documented:** Q23 (gifts; disclosure read 1 pt of 3 max) + Q24 (campaign contribs; disclosure read 1 pt of 2 max) give a maximum systematic under-scoring of 3 pts per state on the 100-point HG scale; practical-availability cells (Q28-Q38, 13 cells) require portal observation, not statute extraction — Phase D dependency. 7 row-design Open Issues (HG-1 through HG-7) + 3 systemic issues flagged for compendium 2.0 freeze; non-blocking. Result doc: `results/projections/hiredguns_2007_projection_mapping.md`; convo `convos/20260513_hiredguns_phase_b_mapping.md`. Phase B 78% complete (7 of 9 mappings done, OS tabled); 2 rubrics remain (FOCAL 2024 → LobbyView). Next: FOCAL 2024 (50 indicators, weighted aggregation, 1,372-cell L-N 2025 ground truth; the last chance besides LobbyView to promote `lobbyist_or_principal_report_includes_contributions_received_for_lobbying` from single-rubric).
- **2026-05-13 (pm late)** — [compendium-source-extracts] Phase B continued: shipped Opheim 1991 projection mapping (6th rubric; explicit predecessor to Newmark 2005). 15 atomic items in scope (7 def + 1 freq + 6 information-category + 1 catch-all un-projectable; 7 `enforce.*` items OOS); 14 distinct row families touched, **all reused (100%; zero new rows introduced)**. Four watchpoints walked: (1) **β AND-projection on `disclosure.legislation_supported_or_opposed`: APPLIED** — reads `bill_id AND position` from compendium (both pre-existing from Sunlight's α form-type split); **second concrete use of β** after 2026-05-11 Sunlight locking — pattern is now established convention. (2) **`def_actor_class_*` row family 3-rubric-confirmed** (Newmark 2017 + Newmark 2005 + Opheim) — Open Issue 1 from Newmark 2017 mapping pulled forward to compendium 2.0 freeze planning per handoff's explicit guidance. (3) **`disclosure.frequency` reads PRI E1h/E2h cadence at finer cut** — only 2 monthly cells (lobbyist + principal) of the 8-cell family vs Newmark 2005's >annual cut; tri-annual omitted from Opheim prose → 0. (4) **No `contributions_from_others` parallel** in Opheim's 7-item info-category battery — "sources of income" + "total income" map to Newmark's compensation pair, not third-party-contributions; Newmark 2017's distinctive row stays single-rubric across 6 of 9 contributing rubrics. **One un-projectable item:** `disclosure.other_influence_peddling_or_conflict_of_interest` catch-all is operationally undefined per `items_Opheim.md` §7; first contributing-rubric item with `unable_to_evaluate` projection (excluded from partial, not zeroed). Three row-family promotions for compendium 2.0 freeze: `def_actor_class_*` 3-rubric-confirmed; three lobbyist-status threshold cells (comp + exp + time) 3-rubric-confirmed; gifts bundle 4-rubric-confirmed. Most-validated row in the compendium going into HG is `lobbyist_spending_report_includes_total_compensation` with 6+ readers. Opheim's 1988-89 vintage extends contributing-rubric ground-truth coverage to ~28 years (1988-89 through 2015). Phase C utility is temporal-depth validation: paper publishes per-state index totals only (Table 1, 47 states; MT/SD/VA missing), no sub-aggregate breakdown — direct tolerance check reduces to weak inequality (`projected_partial ≤ paper_total`, max partial = 14/22). Result doc: `results/projections/opheim_1991_projection_mapping.md`; convo `convos/20260513_opheim_phase_b_mapping.md`. Phase B is 67% complete (6 of 9 mappings done, OS tabled); 3 rubrics remain (HG → FOCAL → LobbyView). Next: HiredGuns 2007 (47 items, disclosure-side only; largest single remaining mapping).
- **2026-05-13 (pm)** — [compendium-source-extracts] Phase B continued: shipped Newmark 2005 projection mapping (5th rubric; predecessor to Newmark 2017 which shipped the same day). 14/14 in-scope rows reused (100%; zero new rows introduced); 5 items OOS (4 `prohib.*` + 1 `penalty_stringency_2003`). Three watchpoints from the 2026-05-13 handoff walked: (1) PRI A-family overlap check on `def_actor_class_*` resolved NO OVERLAP — PRI A1–A11 are structural/institutional-actor observables (governor's office *as institution* lobbying), distinct from Newmark's individual-actor-class observable (state senator *personally* lobbying); both row families distinct and both already in the compendium; (2) three-threshold-cell verification CONFIRMED against 2005 paper lines 120–121 — compensation + expenditure + time as three separate definitional components, reuses Newmark 2017's three typed cells via `IS NOT NULL`; (3) `penalty_stringency_2003` excluded per disclosure-only Phase B qualifier. Structural delta: Newmark 2005 has 6 disclosure items (vs 2017's 7) — falsifies the Newmark 2017 mapping's speculation that 2005 confirms `lobbyist_or_principal_report_includes_contributions_received_for_lobbying`; that row stays Newmark-2017-distinctive. Newmark 2005 also has a standalone `freq_reporting_more_than_annual` item 2017 omits, which projects from existing PRI E1h/E2h cadence row family via 8-cell OR-projection (no new rows needed). Newmark 2005's Phase C utility structurally weaker than 2017's: paper publishes per-state main-index totals only (Table 1, 6 panels 1990–2003), no sub-aggregate breakdown; with disclosure-only scope, direct tolerance validation reduces to a weak inequality. Actual quality signal is temporal-coverage validation across the 6 panels — only multi-vintage ground truth in the current contributing-rubric set. Result doc: `results/projections/newmark_2005_projection_mapping.md` (includes "Corrections to predecessor mappings" section flagging the 2017 speculation falsification + handoff remaining-rubric count decrement 6 → 5). Next: Opheim 1991 (next in Phase B order — structurally adjacent to Newmark 2005, significant overlap expected).
- **2026-05-13** — [compendium-source-extracts] **Newmark 2017 projection mapping shipped (4th rubric to actually land on disk; OpenSecrets 2022 was attempted earlier in the day and tabled — see next entry).** Cross-rubric grep over the 10 contributing-rubric files + 3 existing projection mapping docs run BEFORE drafting any row (8 parallel greps; locked workflow from 2026-05-11). 14 atomic items in scope (7 def + 7 disclosure; 5 `prohib.*` items explicitly excluded per disclosure-only Phase B qualifier); 14 distinct compendium rows touched, **8 reused / 6 new** (57% reuse rate, in line with Sunlight's 11-of-13 cross-rubric-overlap from 2026-05-11). New rows: `def_actor_class_elected_officials`, `def_actor_class_public_employees` (proposed third row family alongside CPI's `def_target_*` and PRI's `actor_*` — Open Issue 1 at compendium 2.0 freeze); `expenditure_threshold_for_lobbyist_registration` + `time_threshold_for_lobbyist_registration` (parallel typed cells to CPI #197's compensation; the structured time cell accommodates federal LDA's 20%-of-work-time threshold); `lobbyist_spending_report_includes_total_expenditures` (granularity-split from `_required`); `lobbyist_or_principal_report_includes_contributions_received_for_lobbying` (the single Newmark-distinctive observable in current contributing set). Pushback on handoff watchpoint #4 documented: the 2026-05-11 handoff's "Should read the existing CPI #197 cell" for all three def.*_standard items is shorthand for "follow the CPI #197 typed-cell pattern" — not literal one-cell. Three concepts are independently extant in state statutes (federal LDA itself has compensation + time but no expenditure threshold). `disclosure.expenditures_benefiting_officials` row design resolved (handoff explicitly flagged for review): keep PRI bundle (gifts ∪ entertainment ∪ transport ∪ lodging × lobbyist/principal), projected as OR over actor sides; HG Q23 gifts-specific granularity flagged for compendium 2.0 freeze, not split now. Honest Phase C validation scope: Newmark 2017 publishes only sub-aggregate per-state data (Table 2), so direct Newmark validation is 50 states × 2 sub-aggregates = 100 ground-truth cells; per-item validation has to come from cross-rubric overlap with PRI 2010 (per-item per-state in `docs/historical/pri-2026-rescore/`) and CPI 2015 C11 (700-cell `results/cpi_2015_c11_per_state_scores.csv`). Two no-variation items (`def.legislative_lobbying`, `disclosure.expenditures_benefiting_officials`) — extracted normally; Phase C accounts for constant +2. No mistakes recorded; cross-rubric grep workflow ran cleanly first attempt. Convo summary at `convos/20260513_newmark_2017_phase_b_mapping.md`; mapping doc at `results/projections/newmark_2017_projection_mapping.md`. **Next: Newmark 2005** (predecessor; 18 items; expected heavy overlap with Newmark 2017 row set).
- **2026-05-12 → 2026-05-13** — [compendium-source-extracts] OpenSecrets 2022 attempted as 4th Phase B mapping; **tabled** (not permanently dropped) after the mapping draft empirically confirmed the original 2026-05-07 Phase A1 DROP audit's structural finding: OS has no published per-tier scoring definition for scores 0–3 (Cats 1/2/3), no Cat 1 score-5 anchor, no inter-coder reliability, no per-state per-category numerical data accessible (behind JS-rendered state-map widget). The 2026-05-07 recheck overturned the original drop on a "few-shot calibratable" criterion softer than the branch's projection-vs-published-data success criterion; this reversal re-establishes the original. Tabled at `results/_tabled/opensecrets_2022_tabled.md` with documented reinstatement triggers (per-tier sub-anchor publication / state-map widget JS pull / third-party reverse-engineering / project framing change); reversal addendum appended to the recheck doc. 3 OS-distinctive row candidates (separate-registrations, exact-vs-ranges-compensation, per-individual-vs-aggregate-compensation) tabled alongside pending organic pickup by other rubrics or project-internal data-infrastructure need at compendium 2.0 row-set freeze. In-session/out-of-session cadence split NOT tabled (Opheim 1991 covers it; Opheim mapping introduces the row family). Cat 4 portal sub-facets fold into existing CPI/FOCAL/HG rows. Handoff `plans/_handoffs/20260511_phase_b_continued_remaining_7.md` renumbered 7 → 6 rubrics; next is Newmark 2017. Convo summary at `convos/20260512_opensecrets_phase_b_tabled.md`. Mistakes recorded: (1) executed handoff at session start without re-anchoring to STATUS.md success criterion — the recheck's weaker criterion should have been a session-opening flag; (2) attempted `rm` on the projection-mapping doc instead of `git mv`-with-SUPERSEDED-banner — user pushback ("are you deleting datafiles?") caught it; the `rm` had been cancelled by parallel-tool rollback so the file was still on disk and was properly moved to `_tabled/opensecrets_2022_projection_mapping_superseded.md`; (3) "agent overcorrection in response to prior user pushback" is a real failure mode visible in the recheck doc itself — should be flagged when reading prior session artifacts.
- **2026-05-11** — [compendium-source-extracts] Third Phase B per-rubric projection mapping shipped: Sunlight 2015 at `results/projections/sunlight_2015_projection_mapping.md` (13 distinct compendium rows × 4 in-scope items, item 4 excluded per 2026-05-07 audit). **11 of 13 rows have cross-rubric readers** — Sunlight's compendium contribution is validation redundancy on heavily-shared rows, not novel structure. Three new conventions locked across remaining mappings: α (split reg-form vs spending-report content cells; HG Q5/Q20 is the canonical case) + β (Opheim's `legislation_supported_or_opposed` projection reads `bill_id AND position` AND'd from compendium; source TSV stays unedited) + "collect once, map many" annotation discipline (every candidate row entry gets `[cross-rubric: <other readers>]` annotation seeding compendium-2.0 dedup). Three distinct threshold concepts named explicitly for first time: lobbyist-status (CPI #197), filing-de-minimis (PRI D1), itemization-de-minimis (Sunlight #3 / HG Q15) — must stay separate. Mistake recorded: drafted Sunlight item 1 (lobbyist activity reporting) as Sunlight-unique before cross-rubric grep, missing 11 readers across HG/Newmark/Opheim/FOCAL/PRI; user pushback caught it. Workflow fix encoded: cross-rubric grep BEFORE drafting any row, mandatory for remaining 7 mappings. Per-state distributions table built via 30-line Python script (`tools/sunlight_distributions.py`) rather than bash loop. Sunlight Phase C validation: per-item only recommended (cannot reproduce `Total`/`Grade` with item 4 excluded). Next-session plan handoff at `plans/_handoffs/20260511_phase_b_continued_remaining_7.md` covers remaining 7 rubrics (OpenSecrets → Newmark 17/05 → Opheim → HiredGuns → FOCAL → LobbyView) with per-rubric watchpoints + cross-rubric reuse hints. **Side artifact:** bash-loop permissions investigation produced (a) project memory file `~/.claude/projects/-Users-dan-code-lobby-analysis/memory/feedback_use_preapproved_bash_patterns.md` and (b) dotfiles note `~/code/dotfiles/notes_bash_loop_permissions.md` documenting four existing loop-backdoor ALLOW_RULES (`xargs *`, `find *`, `awk *`, `sed *`) and proposing `Bash(for *)` / `Bash(while *)` addition to DENY.
- **2026-05-07 (late eve)** — [compendium-source-extracts] Second Phase B per-rubric projection mapping shipped: PRI 2010 at `results/projections/pri_2010_projection_mapping.md` (919 lines, 83 atomic items × 69 distinct compendium rows touched — ~52 NEW on top of CPI's 21, matching handoff prediction). Single-pass execution per user direction a1; doc-conventions block from CPI session held without modification. Mapping covers all 22 accessibility items (Q1-Q6 baseline + Q7a-o 15-criteria search-filter battery + Q8 ordinal_0_to_15) + 61 disclosure-law (A1-A11 actor-side registrant taxonomy + B1-B4 govt-exemption + C0-C3 public-entity-def + D0/D1/D2 materiality with typed cells + E1a-j 19 principal-side + E2a-i 18 lobbyist-side). Decisions: PRI A (actor-side) is distinct from CPI #196 (target-side) — two row families; E1/E2 parallel pairs are two compendium rows each (per granularity-bias, regimes regulate actors asymmetrically); E1h/E2h cadence options become 6 binary rows per actor with CPI's enum becoming a derived projection (Open Issue 4 — retroactive change to CPI mapping); D1/D2 use typed-cell pattern with present-flag derived as `IS NOT NULL`; Q8 partition treated as Open Issue analogous to CPI 25/75 per user direction a2. Aggregation rule structure verified empirically: accessibility max=22, disclosure-law max=37; spot-check Alabama/Alaska percentages match published values to ≤0.2%. PRI's published per-state ground truth is sub-aggregate-level only (650 cells: 5 disclosure + 8 accessibility sub-components × 50 states) — per-atomic-item validation impossible against PRI alone, must use CPI cross-rubric where rows overlap. Mapping references 3 strict + 32 loose consensus clusters as provenance hints. 9 Open Issues surfaced for design-team review (Q8 partition, Q3/Q5/Q6 undefined operationalizations, B1/B2 scoring direction, E1h/E2h enum-vs-binary, E1j independent aggregation, C1-C3/D_value in-compendium-but-not-projected, de-jure/de-facto staging, sub-aggregate-only ground truth, two-row vs one-row-with-actor-enum). No mistakes recorded; conventions held. Compendium row design converging across rubrics — PRI adds rows but doesn't contradict CPI's design. Next: Sunlight 2015 (4-of-5 items) per Phase B handoff order, or any of the remaining 7.
- **2026-05-07 (eve)** — [compendium-source-extracts] First Phase B per-rubric projection mapping shipped: CPI 2015 C11 at `results/projections/cpi_2015_c11_projection_mapping.md`. 14 items → 21 distinct compendium rows under the granularity-bias convention (split on every distinguishing case for max downstream flexibility — e.g., 5 separate `def_target_*` rows from IND_196 alone, aligned with v1.1's `RegistrationRequirement.role` Literal). Three iteration cycles with user surfaced reusable conventions: (1) cluster IDs are provenance hints not row IDs; (2) typed-cell-on-`MatrixCell.value` is the v2.0 schema-migration pattern (replaces named-scalar `de_minimis_*` PRI-shape vestigials); (3) evidence-companion field already exists in v1.1 — Open Issue 5 retracted as misdiagnosis, replaced with the actual hard concern (de-facto evidence-circularity in 2015 round-trip if cells populated from CPI's own scores). Mid-session re-extracted full scoring rules from `papers/CPI_2015__sii_criteria.xlsx` (TSV previously truncated at ~300 chars; openpyxl + uv pip install). Bonus: xlsx contains per-state per-indicator scores → `results/cpi_2015_c11_per_state_scores.csv` (700 ground-truth cells, 6 with data-quality glitches at ~0.9%). **Discovery: 8 de-facto items use 5-tier scoring (0/25/50/75/100), not 3-tier as published criteria suggest** — CPI graders awarded 25 and 75 as scorer-judgment partial credit between published 100/50/0 anchors; doc-wide cell-type correction landed before commit. Phase B per-item template handles compound items (1 rubric-item → N compendium-rows) cleanly without modification. Cluster IDs disambiguated via `strict-c_NNN` / `loose-c_NNN` notation. Worked through 2 concrete examples (IND_205 access, IND_208 audits) on user request. Next: PRI 2010 Phase B mapping (83 items × 50 states — stress-test the template).
- **2026-05-07 (pm)** — [compendium-source-extracts] Phase A of `plans/20260507_atomic_items_and_projections.md` executed end-to-end via 4 parallel `general-purpose` audit subagents (A1 OpenSecrets / A2 LobbyView / A3 Sunlight / A4 Lacy-Nichols 2025 supplementary). OpenSecrets verdict overturned drop → KEEP partial after user-prompted recheck found 5 named worked-example states + statistical anchors in the article's Rankings narrative (75% score-mass projectable; Cat 1 binary, Cats 2/3 few-shot anchored, Cat 4 decomposed). LobbyView KEEP (46 federal LDA schema fields, superset of items_Kim2018). Sunlight 4-of-5 KEEP — user applied "drop what we can't cleanly map" rule per-item, dropping item 4 (compound + near-typo). Lacy-Nichols supplementary extraction: pre-flight error misidentified `Lacy-Nichols-Supple-File-1-IJHPM.pdf` as the 2025 paper's supp; it was the 2024 IJHPM paper's. First A4 pass populated 50/50 with Figure 3 max-observed inferred weights (US sanity check passed); Wiley web-fetch attempt blocked on every route (403 / timeout / archive.org refused / PMC embargoed); user manually downloaded Suppl File 1 + 2 (.docx); pandoc-converted; second extraction agent populated all 50 indicators with verbatim weights, closed all 8 weight-UNKNOWNs, caught 2 weight-decomposition conflicts (`financials.3`, `financials.8` 1→2), populated 50-row prior-framework mapping CSV, reconciled 1,372-cell per-country matrix (Finland weighted total drops 70 → 46 after correcting 13 cells visual-read as 0 to "/" / unassessable), flagged Suppl Table 5 "TOTAL out of 100pts" row as wrong vs Figure 3 (Figure 3 is authoritative; Table 5 raw × Table 4 weights reproduces Figure 3 exactly). Phase B handoff written at `plans/_handoffs/20260507_phase_b_handoff.md`. Saved feedback memory `feedback_use_preapproved_bash_patterns.md` after early `cd+git` heuristic violation. Locked: 9 score-projection rubrics + 1 schema-coverage (LobbyView); Phase C order CPI 2015 first → PRI 2010 second; Phase C scaffolding paths fixed. Two open Phase A leftovers: OpenSecrets state-map widget JS pull (closes Cat 1 if Phase C validation needs it); Sunlight 2011 predecessor data (would re-open Sunlight verdict if surfaced). Next: Phase B per-rubric projection mappings, starting CPI 2015 C11.
- **2026-05-07 (am)** — [compendium-source-extracts] Executed the 2026-05-06 3-way × 3-run consensus plan: 9 parallel `general-purpose` subagents (M1 cluster-anchored / M2 blind / M3 FOCAL-anchored × 3 runs) ran in ~9 min wall clock, all 9 outputs lossless and valid. Consensus tool (`tools/consensus_grouping.py`) produced 24 strict clusters / 63 items at ≥8/9, 39 loose / 106 items at ≥6/9, 468 pairs in 3-5/9 human-review band; both within-method instability and between-method spread metrics computed separately per user direction. M2 blind was most stable (14.4%), M1 cluster-anchored noisiest (19.1%, surprising — the embedding prior was treated very differently across runs), M3 high (45.6%) but driven by big-group amplification. Strict consensus dominated by Newmark2005/2017/Opheim within-tradition battery; FOCAL ↔ PRI semantic mismatches are the dominant disagreement signal. Mid-session realized CPI 2015 C11 was a placeholder, not atomic data — located `PublicI/state-integrity-data` GitHub repo, pulled `2015/criteria.xlsx`, extracted 14 atomic C11 items (6 de jure + 8 de facto) into `results/items_CPI_2015_lobbying.tsv`. Compare/contrast doc `results/20260507_cpi_2015_c11_vs_consensus.md`: de jure items mostly fold into existing clusters; de facto items map onto v1.1 schema's `practical_availability` axis (validates two-axis design). **Projection-success criterion landed and then sharpened to four explicit commitments:** ONE compendium + ONE extraction pipeline + multi-year reliability + source rubrics as SANITY CHECKS on extraction accuracy via per-rubric projections validated against published prior data. **Late-session additions:** verified Lacy-Nichols 2025 (`papers/Lacy_Nichols_2025__lobbying_in_the_shadows.pdf`) applies FOCAL to 28 countries with US federal LDA scored 81/182 = 45% + per-indicator cell-level ground truth in Suppl. File 1 — **FOCAL flips from design-only to standard validation rubric**; jurisdiction scope expanded to `{50 US states} ∪ {Federal_US (LDA)}` (LobbyView is federal-only too — same jurisdiction). Plan written: `plans/20260507_atomic_items_and_projections.md` (Phase A atomic-item audit incl. L-N 2025 Suppl. retrieval; Phase B disclosure-first projection mappings; Phase C projection function implementations + validation against published prior data). Note: this entry was authored on `main` directly (Dan's curating commits to keep main's STATUS up-to-date for other fellows while the branch was running parallel) and merged in via the `compendium-source-extracts` → `main` merge 2026-05-14.
- **2026-05-06 (late)** — [compendium-source-extracts] 3-method × 3-run consensus design session. Critically reviewed `plans/20260506_comp_assembly_via_regex.md`; user pivoted twice during the conversation (regex plan → manual LLM-judgment grouping → 3-method consensus). Final design: M1 cluster-anchored + M2 blind + M3 FOCAL-anchored, 3 runs each = 9 Task-tool subagent dispatches (local, MAX plan, NOT Anthropic API). Uniform long-format output (`source_paper, source_id, source_text, group_id, group_label`); `tools/consensus_grouping.py` computes per-pair agreement (0–9) → strict (≥8/9) / loose (≥6/9) / human-review (3–5/9) views. Design separates method instability (within-method 3-run variance) from method disagreement (between-method variance). FOCAL chosen as M3 anchor after correcting the assistant's mischaracterization of FOCAL's category-coverage gaps as a blind spot — Prohibitions/Personnel are out of project scope (statutory restrictions, not SMR-populating disclosure mechanisms). CPI_2015 filtered upfront (15/16 rows are non-lobbying-domain noise). PRI in scope per 2026-05-06 partial clearance, loaded from `items_PRI_2010.tsv` and joined with the 8-rubric `cross_rubric_items_clustered.csv` (~252 items total post-filter). Regex assembly plan superseded with banner; preserved for traceability. ~150 expected output size flagged as gut anchor not derived target. Plan: [`plans/20260506_comp_assembly_3way_consensus.md`](docs/active/compendium-source-extracts/plans/20260506_comp_assembly_3way_consensus.md). Convo: [`convos/20260506_3way_consensus_design.md`](docs/active/compendium-source-extracts/convos/20260506_3way_consensus_design.md). No code or data artifacts produced — planning conversation; plan is the deliverable. Next agent executes the dispatch.
- **2026-05-06 (pm)** — [compendium-source-extracts] Compendium assembly via embeddings (te3-large) + first candidate item sets. Ran OpenAI `text-embedding-3-large` over the 509 rubric atomic items (vectors + index + similarity matrix preserved at `results/embed_*__openai__text-embedding-3-large.*`). Sweet-spot threshold sim ≥ 0.68 (28 clusters / 10 spanning ≥3 rubrics / 3 spanning ≥5). Within-tradition consolidation strong (state side: HG+Newmark2017+Opheim+Sunlight on qualification thresholds; multiple Newmark2005↔Newmark2017↔Opheim 3-rubric clusters. Euro side: 5-rubric "what to register" + 5-rubric "open-data accessibility" + 4-rubric "code of conduct"); cross-tradition (state↔euro) bridging sparse — only 1 cluster forms ("timely disclosure / reporting frequency," 5 rubrics). Coverage analysis: HiredGuns + FOCAL + Newmark2017 cover the state-tradition well (89% at sim≥0.55), with the real gap being Opheim's enforcement battery (6 items, COGEL Blue Book Table 31 — Newmark dropped these in 2005, Strickland inherited the drop) + 2 income-disclosure items. Built first compendium-2.0 candidate set in two iterations: `comp_assembly_embed_v1.tsv` (126 items: HG + FOCAL + Newmark2017 + 10 Opheim coverage extensions) and `comp_assembly_embed_v2.tsv` (209 items: v1 + all 83 PRI atomic items, sourced from historical pri-2026-rescore transcriptions). **PRI partial clearance recorded 2026-05-06**: PRI now permitted as `ext_pri_2010_*` coverage extension; still blocked for structural anchoring, "match PRI" calibration, row-frame seeding. Late-session naming reframe: artifact files renamed `comprehensive_set` → `comp_assembly_embed_v{1,2}` to make the method-vs-conclusion distinction durable (parallel `comp_assembly_via_regex` is next; the actual comprehensive set is whatever falls out of reconciling those candidates, not what any single method produces). Three new feedback memories saved (`don't volunteer comparisons`, `old compendium not a target`, `name by method not conclusion`). Side-experiments: regex framing-strip normalizer at `tools/normalize_state_items.py` produced a 134-item state-normalized embedding (in `results/state_normalized/`); modest within-tradition tightening, not load-bearing for the candidate set. Convo summary at `convos/20260506_comp_assembly_via_embeddings.md`. Handoff plan written for the next agent at `plans/20260506_comp_assembly_via_regex.md` (regex-based parallel assembly, USA-tradition rubrics only).
- **2026-05-06** — [compendium-source-extracts] Brief session resuming after the 2026-05-03 work landed. Two follow-ups: (1) HathiTrust paths documented for the 1990 COGEL Blue Book 8th edition (catalog `Record/002470321`, volume `mdp.39015077214750`, page-structure shortcuts noted) for the user's personal-machine retrieval — institutional auth dependent; (2) sentence-embedding script committed at `tools/embed_cross_rubric.py` (commit `4eed8f5f`). Script is the local-machine companion to the 2026-05-03 TF-IDF analysis — the sandboxed env this branch runs in cannot reach `huggingface.co` (egress proxy excludes it), so the cross-rubric clustering re-run with semantic embeddings ships as a runnable script for the user's desktop. Default model `all-MiniLM-L6-v2` (~80MB, 384-dim, fast on CPU). Reads the 509-item `cross_rubric_items_clustered.csv`; writes similarity matrix + threshold-summary CSV + human-readable cluster dump. Predictions to falsify documented in script docstring + this convo's Post-session continuation block. Convo summary updated in-place with a Handoff section for the desktop agent. No content drift on existing files.

- **2026-05-03 (pm cont'd)** — [compendium-source-extracts] Resumed same-day session to execute Plan Step 4 (Blue Book / BoS / COGEL acquisition mapping) + a cross-rubric descriptive-stats pass the user requested directly. **Acquisition headline: "CSG Blue Book" and "COGEL Blue Book" are the same publication series** (joint CSG+COGEL series, full title *Campaign Finance, Ethics, Lobby Law & Judicial Conduct: COGEL Blue Book*, OCLC 80682979). Phase-1 comprehensive volumes (1990 8th ed. on HathiTrust, public-domain Google-digitized, mdp.39015077214750) split ~1996/1997 into thematic Updates (Ethics / Lobbying / Campaign Finance / FOI), each annual. The Phase-2 Lobbying Update (Gross 1997–) is a legislation+litigation news series, not the comparative-tables format Strickland 2014 used. **Recovered 1990 Lobby Laws TOC**: Tables 28 (Definition / Registration / Prohibited Activities), 29 (Reporting Requirements), 30 (Report Filing), 31 (Compliance of Selected Agencies = enforcement), 32 (Education and Training). Cell-level content not retrieved (HathiTrust babel returns 403 from this env even on PD content). Strickland's full extraction notes re-read: he applies Newmark 2005's 18 items unchanged + extends 1988-2003 via biennial COGEL/BoS volumes + 2004-2013 via State Capital Handbook. **Implication: Newmark dropped Opheim's 7 enforcement items even though Blue Book Table 31 explicitly provides them; Strickland inherited the drop. Table 32 (Education and Training) is unused by all three.** Book of the States low-priority per user (items already in `items_Newmark2005.tsv`/`items_Opheim.tsv` at the row-label level). State Capital Handbook is commercial-only (~$200/yr Thomson Reuters / SCG Legal); not retrievable via web_fetch. Recommended next-step paths: email Adam Newmark (highest yield); ILL for HathiTrust 1990 8th ed.; used-copy purchase eBay/AbeBooks; defer Handbook indefinitely unless project subscription budget. Acquisition findings doc at `results/20260503_blue_book_bos_cogel_acquisition.md`. **Cross-rubric descriptive stats**: 661 atomic items total (509 in 17 rubrics + 152 non-rubric); coverage matrix across 14 meta-domains × 17 rubrics; TF-IDF clustering at 5 thresholds (0.20-0.50). Headline finding overturns the implicit assumption — paraphrase variants ARE present but cluster mostly within author-family (Opheim → Newmark2005 → Newmark2017) or within geographic-tradition (AccessInfo / CouncilEurope / ALTER_EU / FOCAL); **across the broader 17-rubric corpus, items are expressed in idiosyncratic vocabulary even when measuring the same thing**. Sentence-embedding fallback blocked by egress proxy on HuggingFace. Topic-frequency histogram has no natural elbow — Method A (frequency threshold) is on weaker ground than the synthesis plan acknowledged. Long-tail diagnostic candidates surfaced: Opheim's enforcement battery (`enforce_subpoena`/`enforce_review_quality`), SOMO's legislative footprint, HiredGuns's e-filing battery, Sunlight's de-minimis-threshold question. Descriptive-stats doc at `results/20260503_cross_rubric_descriptive.md` + supporting CSVs. Convo summary at `convos/20260503_pm_acquisition_and_descriptives.md`.
- **2026-05-03 (late)** — [compendium-source-extracts] Post-checkpoint addendum to the same session. README rewrite (dropped 5-8 / all-50 contradiction; new "What we deliver" + "Project state" sections; data-layer-not-rubric framing made explicit). v1.1 schema audit at user's prompting confirmed PRI-shape under the surface despite row-level `FrameworkReference` abstraction: `RegistrationRequirement.role` 11-role Literal = PRI A1-A11, `ReportingFrequency` 7-cadence Literal = PRI E1h/E2h, named scalar `de_minimis_*` fields on SMR. Same vocabulary-vs-structure pattern the compendium audit caught. README updated to make explicit that v1 is *not* the foundation for v2.0; v2.0 will be a complete overhaul, not an incremental fix. Rubric inventory settled at 14 fully extracted + 12 non-rubric extracts + 4 known-of-but-not-fully-retrieved (TI-UK 4-criterion, CII methodology, Roth 2020 thesis, Chari et al. books). Plan written for next session at `docs/active/compendium-source-extracts/plans/20260504_compendium_2_0_synthesis.md`: acquire missing papers, extract newly-acquired rubrics, cross-rubric descriptive stats, brainstorm subset-selection methods + item-family clustering. Compendium 2.0 design itself + schema v2.0 stay deferred to follow-up plans. Commits `6cef788` (README rewrite), `9682efd` (README v2.0-overhaul clarification + plan).
- **2026-05-03** — [compendium-source-extracts] Per-paper extraction plan executed end-to-end with user-modified sequencing (no template-first; citation-collection + download wave before extraction; README headline → "LobbyView, for all 50 states"). Predecessor citation-collectors against FOCAL 2024 (26 entries) + Newmark 2017 (25 entries) → ~37 candidate downloads. Download wave: 17 retrieved (14 PDFs + 2 HTML + 1 text), 3 verified-on-disk, 13 paywalled, 1 not-found, 3 books. User author-page hunt round 1 added 5 PDFs (Strickland, Mihut, Chung, LaPira & Thomas 2014, CPI 2015 Kusnetz). **26 papers extracted total** in 4 waves of parallel agents (test 3 + wave 1 of 6 + wave 2 of 6 + wave 3a of 6 + wave 3b of 5). Headline findings: BoS-tradition deeper than expected (5/26 pure secondary-source-coding via CSG Blue Book / Book of States / COGEL / State Capital); only ~13/26 are independent measurement rubrics; FOCAL Table 2 not authoritative on category structure (3 wrong + 3 approximate + 10 right); federal-vs-state cross-walk gap consistent across Kim 2018 / Chung 2024 / LaPira & Thomas 2014 (LobbyView's federal infrastructure has no state equivalents); CDoH parallel measurement universe surfaced (Lacy-Nichols 2023's 7 frameworks zero overlap with FOCAL 2024's 15). 22 new acquisition targets surfaced for Tasks #10 (email authors) + #11 (CSG/COGEL/State Capital + CII methodology + GDB Handbook + TI-UK rubric + Piotrowski & Liao + Keeling et al. + Chari/Murphy/Hogan 2007 PQ + CDoH 7-framework corpus + 9 lit-review-surfaced predecessor candidates). Mistakes: cwd confusion caused 5 PDFs to land in main worktree's `papers/` instead of compendium-source-extracts (recovered by copying); spec error on Sunlight 2015 (4-tier assumption — actually 2/4/5-tier mix; agent caught and corrected); CPI Hired Guns path inaccuracy in agent prompt (recovered fine). Convo summary at `docs/active/compendium-source-extracts/convos/20260503_per_paper_extraction_execution.md`. User review of 26 extracts pending; compendium-2.0 design plan deferred until after review.
- **2026-05-02 (pm)** — [statute-extraction → compendium-source-extracts] Phase 0 of the v3 compendium audit ran end-to-end: two parallel auditor subagents + reconciliation produced canonical concerns doc (186 concerns / 109 of 141 rows flagged / 24.2% inter-run agreement) and reconciliation note at `docs/active/statute-extraction/results/20260502_compendium_audit_{concerns,reconciliation}.{md,csv}`. Walked through tag-disagreements and 5-concern hot spots; recognition that the compendium is **structurally PRI-shaped**, not just lexically (4-row DEF_PUBLIC_ENTITY family from PRI Q-C; 12-row FREQ_* from PRI E1h/E2h; 11-row REG_*-A-series from PRI A1–A11; 8-row RPT_*_NON_COMPENSATION etc. from PRI E1f i/ii/iii/iv split; literal `?.` formatting artifact on ~24 rows). User pivoted from "audit + patch" to "rebuild from sources, no PRI." New branch `compendium-source-extracts` created off `origin/main`; Phase 0 deliverable retained as historical evidence (not a fix-list); per-paper extraction plan written at `docs/active/compendium-source-extracts/plans/20260502_per_paper_source_extraction.md`; awaiting user review. The PRI-out-of-bounds notice at the top of this file landed this session — load-bearing for all future agent sessions until compendium-2.0 lands. Conversation context exhausted by accumulated agent mistakes (assistant repeatedly missed structural PRI privilege; dispatched template extraction at wrong path/format before format was locked); user stopped execution and asked for the plan deliverable.
- **2026-05-01 (pm)** — [statute-extraction] Phases 0–3 of `plans/20260501_statute_extraction_harness.md` shipped under TDD (v1.3 schema bump + v2 scorer prompt + extraction_brief module + ExtractionRunMetadata + sha helpers + extract-prepare/finalize orchestrator subcommands). Iter-1 dispatched: 3 temp-0 claude-opus-4-7 runs against OH 2025 `definitions` chunk (7 rows × 3-regime expansion = 15 emitted tuples per run). **14/15 = 93.3% inter-run status agreement**; the materiality-gate canary captured `required_conditional` + verbatim `condition_text` across all three regimes; OH has no quantitative inclusion thresholds (unanimous `not_addressed`). Single disagreement on `DEF_ADMIN_AGENCY_LOBBYING_TRIGGER × retirement_system` traced to row-name direction-ambiguity (target vs actor axis). **Iter-2 prep shipped**: tightened all 7 `definitions` row descriptions to lead with explicit axis labels, wrote a per-chunk preamble at `src/scoring/chunk_frames/definitions.md` (auto-inlined into the brief), drafted handoff brief at `plans/_handoffs/20260501_compendium_item_audit_handoff.md` for a future agent to develop a v3 audit covering all 141 rows for axis explicitness + curation fidelity. 8 commits: `44fc762`, `d8e5187`, `c398020`, `878133a`, `a703566`, `0ac7800`, `67ccb4b`, `2d32c39`. Suite 337/337 green.
- **2026-05-01** — [filing-schema-extraction] PR #5 review session. Verified per-rubric atomic-item counts (19/18/22/48/7) against papers + dedup-map + per-rubric tables; walked the 23 NEW rows and 7 `domain="definitions"` rows. Pushed back on D11: `DEF_*_STANDARD` and `THRESHOLD_LOBBYING_*_PRESENT` row descriptions overlapped, giving the harness no disambiguation rule. User produced the cross-axis statute case ("primary time on lobbying, unless expenses < $1k") that justifies keeping both rows. Tightened 4 row descriptions to inclusion-only / exemption-only with explicit cross-pointers; appended a D11 "Disambiguation rule" addendum to the audit doc. Reconciled audit-doc arithmetic (Summary OOS 41 → 27, enforcement 14 → 12, Opheim folded 14→15 / excluded 8→7, CPI folded 22→21 / excluded 10→11). Moved `results/20260430_compendium_audit.md` → repo-level `docs/COMPENDIUM_AUDIT.md` per its planned lifecycle. Tests 24/24 still green. PR #5 merged.
- **2026-04-30 → 2026-05-01** — [filing-schema-extraction] Executed the v2 compendium audit per `plans/20260430_compendium_expansion_v2.md`, then landed v1.2 schema bump per `plans/20260501_data_model_v1_2_definitions_domain.md` (option (A): promote 5 notes-flagged rows to new `domain="definitions"` + add 2 inclusion-framed symmetry rows). Walked 114 atomic items across 5 rubrics (Newmark 2017/2005, Opheim 1991, CPI Hired Guns 2007, OpenSecrets 2022): 41 OUT_OF_SCOPE (prohibitions/penalties/enforcement), 51 EXISTS/MERGE, 21 NEW rows. Compendium 118 → 139 (v2) → **141 rows** (post-v1.2); dedup map 140 → **254 entries**. v1.2 added `"definitions"` to `CompendiumDomain` Literal; 7 rows now in that domain (5 migrated + 2 new symmetry-gap rows `DEF_EXPENDITURE_STANDARD` / `DEF_TIME_STANDARD`). Mid-session rename of 5 threshold rows from PRI-vocabulary IDs to rubric-neutral after user pushback on PRI privilege. Curation-gap fix on D0 (Newmark/Opheim/CPI cross-refs added). Durable audit report `docs/active/filing-schema-extraction/results/20260430_compendium_audit.md` (Decision Log D1-D11). Kickoff plan re-scoped: MVP target ~90 → **108 statute-side rows** (141 total = 108 statute-side + 33 accessibility-side). Test additions: 5 per-rubric curation-drop tests + 7 v1.2 invariant tests; **24/24 compendium tests pass** (TDD: red-green-refactor).
- **2026-04-30 (pm)** — [filing-schema-extraction] Review session of the kickoff plan (`plans/20260430_filing_schema_extraction_kickoff.md`) opened a deeper upstream gap: the 118-row compendium represents only 4 of 9 consumer-relevant rubrics in `papers/`. User reframe — "we can only extract data which is required to be disclosed" — scoped expansion to disclosure-side rubric items only (prohibitions / penalties / enforcement / cooling-off explicitly out of scope). Produced new plan `plans/20260430_compendium_expansion_v2.md` (per-rubric audit walking Opheim 1991, Newmark 2005, Newmark 2017, CPI Hired Guns 2007, OpenSecrets 2022) and result doc `results/20260430_book_of_states_gap_analysis.md` (the disclosure-only scoping rationale). Final report deliverable lives at repo-level `docs/COMPENDIUM_AUDIT.md` on merge — designed as the anti-loop artifact (decision log + excluded-items table + coverage matrix) responding to user's stated context loss after a laptop drive crash. Audit not yet executed; kickoff plan's harness brainstorm is dependency-blocked on v2 expansion.
- **2026-04-30** — [statute-retrieval] Executed `plans/20260430_oh_2026_baseline_smr.md` end-to-end as a PRI-projection MVP. Source vintage was 2025 not 2026 (Justia tops out at 2025 for OH; user confirmed for an April-2026 calibration baseline). Added `("OH", 2025)` to LOBBYING_STATUTE_URLS via Justia URL-convention probe (commit `ee4ffdd`); retrieved 30 sections (~143 KB, +1.8% vs OH 2010 core); ran 3 concurrent opus-4-7 scorer subagents (run_ids `26b1ab90b0eb`, `e7846593ebb5`, `736087384f85`); inter-run disagreement 21.3% (13/61 items, flagged); built SMR from r2 (`e7846593ebb5`) for 2010-convention continuity; diffed vs OH 2010 SMR. **Headline: OH disclosure law is structurally near-unchanged 2010 → 2025 as PRI projection sees it** — only one structured-data flip (`governors_office.required: False → True`); all 22 field_requirements + both reporting_parties + both de_minimis nulls identical. Real text-level changes exist (casino-control commission added to definitions in §101.70 likely 2012; 8 procedural sections grew 15-60%) but don't move the rubric needle. Mid-session reframes: (1) by direct statute reading no 2025 run is fully correct — r1 reads §101.70(F) qualitative materiality test correctly but over-scores conjunctive E-series, r2/r3 read E-series correctly but miss qualitative materiality; (2) the rubric isn't the right product anyway — extraction needs a filing-field schema, not diagnostic yes/no answers. User decision: ship MVP, split future work into one branch on filing-schema extraction + parallel branch on actual disclosure pulling. Result doc `results/20260430_oh_2025_vs_2010_diff.md` (commit `ce10674`).
- **2026-04-29 / 30** — [statute-retrieval] Phase 1 calibration analysis (Sunlight 2015 ↔ PRI 2010 on opus-4-7 + files-read-enforced runs for CA/TX/OH). Reframed twice mid-session: unit of analysis is the disclosure item not state ranking; the actual product is the SMR not per-rubric scores. Joint per-item agreement: 12/12 activity cells, 6/6 expenditure-itemization cells, 5/6 compensation cells match Sunlight across CA/TX/OH. OH cleanest (Δ=−1 vs PRI total; harness correctly identified OH does not require lobbyist compensation, Sunlight independently confirms). Architectural redesign: compendium is the universe, SMR is keyed to it, PRI/Sunlight/etc. are projections from a populated SMR (not data sources for it). Verified `CompendiumItem` schema exists (data-model-v1.1) but compendium has never been populated. Decisions: skip Phase 2 (Sunlight as second scoring rubric); accept OH as the calibration baseline; populate the compendium as the actually-load-bearing missing piece. New plan `plans/20260430_compendium_population_and_smr_fill.md` (Stage A: compendium population from PRI ∪ Sunlight ∪ FOCAL ≈140 items; Stage B: temporary OH/CA/TX SMR fill via PRI projection through compendium; Stage C reference: MatrixCell + statute-extraction harness). Earlier "Path 1 SMR projection" plan was abandoned (rubric-shaped SMR was wrong direction). Result doc: `results/20260429_sunlight_pri_item_level.md`.
- **2026-04-29 (pm)** — [statute-retrieval] Bumped scorer to claude-opus-4-7; diagnosed dispatch variance (4–43 tool calls per sub-agent on same statute) as dominant noise source; added files-read enforcement (sidecar `files_read.json`, locked-prompt Rule 7, orchestrator fail-closed). Variance collapsed from ±7 to ±1 on TX. Narrowed Rule 6 C0 to coverage-side definitions only — fixed TX C0; CA C0 still over (open). Three-state apples-to-apples (opus + enforcement): TX 22-23/29 (−7/−6), CA 29/23 (+6), OH 25/26 (−1). Gap structure asymmetric → chasing TX agreement would worsen CA/OH. Surfaced `papers/Sunlight_2015__state_lobbying_disclosure_scorecard_data.csv` as second ground-truth dataset. **Reframed project goal**: harness produces `StateMasterRecord` disclosure-requirement extractions; PRI 2010 + Sunlight 2015 are multi-rubric calibration signals, not optimization targets (Newmark 2017 PRI×CPI r=0.04). Updated four docs (RESEARCH_LOG Purpose, plan Goal/Architecture, calibration_comparison header, this STATUS Track A line + branch row). Shipped new plan `plans/20260429_multi_rubric_extraction_harness.md` with 4 phases. Commits: `fc644b5` + `904b110`.
- **2026-04-29 (am)** — [statute-retrieval] Built two-pass statute retrieval pipeline (retrieval agent → scoring agent). TDD'd enriched manifest format (StatuteArtifact with role/hop/retrieved_because/referenced_from), retrieval agent prompt, brief builder, ingest_crossrefs, expand-bundle/ingest-crossrefs orchestrator subcommands. Ran retrieval + scoring on CA/TX/OH. C_public_entity_def matches 3/3 states; D_materiality 2/3; A/E still iterating on scorer prompt. TX E1 cascade (E1a=0 zeros 15 items) is the dominant remaining error. 269 tests passing.
- **2026-04-22 (docs)** — [main] Shipped `docs/LANDSCAPE.md` (fellow-facing landscape report, ~4.9k words, with Layer 1 architecture diagram, OCDEP list verified against upstream opencivicdata repo, Plural framing corrected, FOCAL/PRI/CPI/Newmark/Sunlight/OpenSecrets cross-rubric comparison, 4-axis framing, field-level compendium rationale, positioning vs. LobbyView / OpenSecrets / Sunlight / CSG / F Minus / commercial tier). Companion swap: "real-time" → "up-to-date" across `README.md` (3×), `CLAUDE.md` (1×), and archived `pri_2026_methodology.md` (1×) to match the framing set in LANDSCAPE §2.
- **2026-04-22 (pm late)** — [main] Merged `pri-calibration` to main (34 commits: Justia retrieval, statute bundle construction, calibration harness, consistency metrics, Phase 3 baseline results, all convos + plans + results) and archived to `docs/historical/pri-calibration/`. Previously on `pri-calibration` branch: 2026-04-22 pm reflection session reframed the calibration charter from PRI-only to multi-rubric (PRI + CPI Hired Guns + Sunlight + Newmark + optional OpenSecrets), rationale: Newmark 2017's r=0.04 cross-rubric correlation means single-rubric agreement could mask overfitting. New plan doc shipped: `plans/20260422_multi_rubric_calibration.md` (now at `docs/historical/pri-calibration/plans/...`). Fellow-task split articulated: Track A (verify lobbying laws) extends pri-calibration's infrastructure on a new branch; Track B (pull disclosure data) starts fresh. Active Research Lines table on main remains empty pending successor branches.
- **2026-04-22 (housekeeping)** — [main] Archival sweep: moved `docs/active/{scoring,pri-2026-rescore,focal-extraction}/` → `docs/historical/` (all three were merged to main but had not been archived per the lifecycle).
- **2026-04-22 (pm)** — [data-model-v1.1] Implemented all five v1.1 gap closures per the TDD handoff — availability/evidence fields, `FrameworkReference` generic migration, new `compendium.py` + `pipeline.py`, `ConfigDict(extra="forbid")` on migrating models. Three `TestStateMasterRecord` tests in `tests/test_models.py` migrated to `framework_references=[...]`. Full suite 128/128 green. Merged to main and archived.
- **2026-04-22 (am)** — [data-model-v1.1] Cut branch from main `2afe77f4`. Wrote plan `plans/20260422_v1.1_gap_closures.md` (five gap buckets) and red-test spec `tests/test_models_v1_1.py`. TDD handoff only — no `src/` changes on the branch yet.
- **2026-04-22 (am)** — [pri-calibration] Landscape brainstorm across 5 architectural pieces (Popolo, OCD, spatula, PRI, FOCAL) + federal context (GAO, LDA, Lacy-Nichols 2025). Three reframings landed: (1) project identity is **data infrastructure** (bulk state lobbying data; N × 50 × 2 companion matrix), not rubric-scoring; (2) 4-axis framing (Required × Legally Available × Practically Available × downstream-compliance/enforcement); (3) compendium unions 9+ frameworks — we catalog, don't grade. Key empirical anchor: Newmark 2017 r=0.04 CPI↔PRI-disclosure correlation justifies union-not-pick. Data model v1.0 unfrozen; five gaps scoped on new branch `data-model-v1.1`. Landscape report deferred to fresh-context session.
- **2026-04-21 (eve)** — [pri-calibration] Rubric-unification handoff plan written (`plans/20260421_rubric_unification_and_handoff.md`): 4 phases to item-level-map PRI disclosure-law (61) + PRI accessibility (22) + FOCAL 2024 (50 indicators). Context-full end-of-session; scoped for fresh-context agent. Sequencing locked: pri-calibration pilot finishes against *existing* PRI 2010 rubric (don't change rubric mid-calibration); rubric unification runs in parallel; fellow scale-out waits for both. (Framing superseded 2026-04-22 am by compendium-first reframe.)
- **2026-04-21** — [lobbying-data-model] v1.0 accepted (no changes requested from v0.1 review). Schema archived to `docs/historical/lobbying-data-model/`; pydantic models remain in `src/lobby_analysis/models/` as the extraction-pipeline contract. `scoring` + `lobbying-data-model` branches merged into main in the same session.
- **2026-04-20/21** — [pri-calibration] Phase 3 live baseline dispatch (30 subagents, 5 states × 3 runs × 2 rubrics). Fixed load-bearing path-normalization bug in `build_statute_subagent_brief` mid-session. Baseline: disclosure-law 0% exact-match vs PRI 2010; per-state sub-aggregate A_registration dominates error (WY 1/7, TX 1/7). Post-dispatch TX statute walkthrough confirmed **H1 (bundle scope)** via §311.005(2) "person includes government or governmental subdivision or agency" — ch. 305 alone doesn't tell the scorer this. Decision: extend `LOBBYING_STATUTE_URLS` with hybrid retrieval-guide + metadata shape; finalize pilot with bundle fix before scaling to 3-fellow × 17-state rollout. Accessibility formally descoped from calibration branch.
- **2026-04-19** — [pri-calibration] Phase 2b harness + Phase 3 scaffolding: derived PRI sub-aggregate rollup from paper §III.B-E + §IV (line-cited spec, 9 methodology differences documented); shipped `calibrate`, `calibrate-prepare-run`, `calibrate-finalize-run`, `calibrate-analyze-consistency` subcommands + `StatuteRunMetadata` model; refactored `compute_consistency` to support both portal + statute paths; multi-run `calibrate` renders per-run + cross-run variance. 130/130 tests green.
- **2026-04-18** — [pri-calibration] Phase 2a statute retrieval pipeline + live 5-state retrieval. Architectural redesign: curated `LOBBYING_STATUTE_URLS` per (state, vintage) instead of per-state title-slug mapping — because 5 states showed 5 distinct Justia URL conventions. Shipped retrieve-statutes + export-statute-manifests subcommands; 21 section files retrieved (~260 KB); committed manifest provenance to docs/.
- **2026-04-17/18** — [pri-calibration] Branch kickoff (off `scoring`) + Phase 1 execution end-to-end. Phase 0 audit of PRI 2010 methodology found no published IRR (single-coder + state-official review); convergence target deferred to post-baseline. Retrieval architecture decided: **Justia-unified** for both 2010 and 2026 vintage via `/codes/<state>/<year>/` stable URLs. Cloudflare's JS challenge required **Playwright with fresh-browser-per-request**. TDD'd justia_client + audit + `audit-statutes` subcommand; 26 new tests red-then-green. Ran 50-state audit: **49/50 eligible for 2010 calibration** (CO excluded, earliest Justia year 2016); 33/34 PRI responders eligible; all 50 eligible for 2026-scoring.
- **2026-04-17 (pm)** — [scoring] Executed Phase 3 pilot (CA/CO/WY × 3 temp-0 runs × 3 rubrics = 27 cells + the 2026-04-14 CA dry-run as CA's 3rd run). Inter-run disagreement flags: PRI disclosure-law flagged on all 3 states (CA 37.7%, CO 11.5%, WY 11.5%); CO focal flagged at 13.0%; PRI accessibility stable on CO+WY, flagged on CA (11.9%) via WAF-stub variance. Root cause for disclosure-law: snapshot same-host capture excludes state code — subagents disagree on whether portal silence means `unable_to_evaluate` or `score=0`. Pivoted to PRI 2010 ground-truth calibration. Phase 4 paused. Lessons: 21 concurrent subagents triggered org-level Anthropic API rate-limit and killed 20 of 21; safe batch size ≈ 4. Subagents must be dispatched with explicit Read-tool-only constraint.
- **2026-04-17 (am)** — [scoring] Brief desktop-side inventory: confirmed `data/` (snapshots + CA dry-run scores) lives only on laptop. Decision: continue scoring on laptop. Created gitignored `data/` placeholder on desktop; no code changes.
- **2026-04-13 (eve)** — [pri-2026-rescore] Phase 4 data-collection: 50/50 states discovered via Stage 1 subagents (role-labeled URL JSONs, zero fabricated URLs, 15 states with flagged seeds) and snapshotted via Stage 2 subagents (curl + Chrome UA, 981 artifacts, ~350 MB, 17 `suspicious_challenge_stub` flags, 54 honest skips). Architectural pivot mid-session from Python SDK pipeline to native Claude Code subagents. Coverage tiers: 40 clean, 8 partial WAF/SPA, 2 near-empty (AZ, VT).
- **2026-04-13 (pm)** — [pri-2026-rescore] Phase 3: drafted 2026 accessibility rubric (59 items = 22 retained + 37 new) and 2026 disclosure-law rubric (61 items retained, B1/B2 reverse-scoring made explicit). User approved; Phase 3 gate passed.
- **2026-04-13** — [pri-2026-rescore] Phase 1+2: transcribed PRI 2010 accessibility rubric (22 atomic items) and disclosure-law rubric (61 atomic items) to CSVs with deferred scoring; transcribed 2010 per-state scores (50 states × sub-component breakdown) for both rubrics; programmatic reconciliation of sub-component sums vs published totals passes on all 50 states both rubrics.
- **2026-04-13** — [focal-extraction] Created `focal-extraction` branch. Executed the FOCAL extraction plan end-to-end: transcribed Lacy-Nichols Table 3 into `focal_2024_indicators.csv` (50 indicators × 8 categories, all three validation spot-checks passed), wrote methodology note.
- **2026-04-12 (pm)** — [research-prior-art] Planning-agent session: reviewed prior session's blindspots, authored PRI 2026 re-score plan (7 phases, all 50 states, both accessibility + disclosure-law rubrics, Sonnet-subagent scoring) and FOCAL extraction plan (3 phases, scoring deferred). Merged branch to main; cut `pri-2026-rescore`.
- **2026-04-12** — [research-prior-art] Paper-fetch retry (Libgober-Jerzak ingested via arXiv; Ornstein still unreachable), merged main into branch (brought in 7 papers from other agent's pull), read Libgober-Jerzak Tasks 1+2 and Lacy-Nichols FOCAL framework table, consolidated four-rubric landscape (PRI 2010 + FOCAL 2024 + F Minus + GAO-25-107523) into scoring-rubric-landscape.md as planning-agent handoff.
- **2026-04-10** — [research-prior-art] Scoping kickoff: verified the prior-art gap, profiled state infrastructure tiers (8-state shortlist: CA/CO/NY/WA/TX/WI/IL/FL), analyzed OCDEP 5/6 + withdrawn Disclosures + draft Campaign Finance Filings proposals, decided to depend on Open States, drafted schema-design questions, ingested Bacik 2025 + Kim 2025 as the first two papers, deferred 5 more papers to follow-up.
- **2026-04-09** — Repo initialized with research-first scaffolding (CLAUDE.md, STATUS.md, PAPER_INDEX.md, PAPER_SUMMARIES.md, papers/, docs/active/, docs/historical/).



