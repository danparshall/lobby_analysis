# Sub-2 Stream 2 plans: Newmark 2017 + Newmark 2005

**Date:** 2026-05-18
**Branch:** phase-c-projection-tdd
**Predecessor convos:** [`20260514_rubric_plans_drafting.md`](20260514_rubric_plans_drafting.md) (Sub-0 — playbook gap audit + data-year audit), [`20260514_sub_1_sunlight_opheim_plans.md`](20260514_sub_1_sunlight_opheim_plans.md) (Sub-1 — Stream 1 plans)
**Sub-session 2 of 5** in the multi-sub-session structure laid out by Sub-0.

## Summary

Drafted the two Stream 2 plans (Newmark 2017 rubric #4, Newmark 2005 rubric #5) per the Sub-0 charter. Both plans are self-contained per the write-a-plan skill, carry STOP clauses for spec-doc-vs-v2 drift, and bake in the 7 Sub-0 conventions. Stream 2's intra-stream ordering is **helper-sharing**, not row-introduction: all 6 Newmark-2017-NEW rows are already in v2 (the row-freeze locked them via `compendium-v2-promote`); the dependency is at the helper-import level (Newmark 2005 imports `project_gifts_actor_agnostic_or` from `newmark_2017`, plus the threshold IS-NOT-NULL dispatcher pattern).

A Phase-0-style cross-check run against `load_v2_compendium()` surfaced **15 spec-doc-vs-v2 row-name renames** total — 7 for Newmark 2017 (3 long-form threshold renames `_threshold_for_lobbyist_registration` → `lobbyist_registration_threshold_*` family; 2 gifts `_report_` → `_spending_report_`; 1 `_by_client` → `_by_payer` inherited from Sunlight Sub-1; 1 contributions-received `_report_` → `_spending_report_`) and 8 cadence renames for Newmark 2005 (`_report_cadence_` → `_spending_report_cadence_` family). All resolved cleanly; both plans bake the rename tables inline.

After the original drafting pass, a separate review session walked the Newmark 2017 plan and surfaced two substantive concerns that landed as tightening edits (8 edit points across the plan): (1) the 50-state sub-aggregate validation is **xfail-only at landing time** because 6 of 14 input rows have no upstream source — it's a future-validation harness, not landing-time evidence; (2) PRI 2010 was listed as a fixture source despite being wrong-vintage (2009–10 vs Newmark's 2015), which would introduce vintage drift that masks real cross-rubric drift signals post-Phase-D. PRI 2010 dropped from the harness; landing-time evidence rebased to per-item + ground-truth + cross-rubric-on-OLD-rows. A third concern (Opheim's eventual helper import direction) was retired during review — Opheim is tabled and recorded as blocked on 1988-89 statute data; that import is far enough in the future to not warrant pre-design.

## Topics Explored

- **Pre-flight:** re-confirmed `compendium-naming-docs` + `compendium-row-id-renames` have shipped (15 renames live; `compendium/NAMING_CONVENTIONS.md` exists); confirmed Sunlight 2015 (rubric #3) shipped + CPI 2015 drift fix landed; confirmed Opheim 1991 (rubric #6) flagged blocked on 1988-89 statute data (commit `e62910b`). Sub-2 (deferred during Sub-1 pending GH #9) is unblocked.
- **Pulled `phase-c-projection-tdd` worktree forward** from `1450eb4` (Sub-1) to `e62910b` (current head) — 30 commits across the v2 row-ID renames, Sunlight 2015 implementation, CPI drift fix, and Opheim blocking caveat.
- **Phase-0 cross-check executed.** Ran `load_v2_compendium()` against the 15 expected Newmark 2017 rows + 8 additional Newmark 2005 cadence rows. Surfaced 15 renames (7 for Newmark 2017, 8 for Newmark 2005). All resolved.
- **Newmark 2017 plan drafted** (`plans/20260518_newmark_2017_plan.md`, ~440 lines post-tightening). Declarative `_ATOMIC_SPEC` table dispatcher mirroring `pri_2010.py`. 14 in-scope items (5 prohib OUT). Medium validation regime — per-state sub-aggregate ground truth from Newmark Table 2 (50 states × 4 columns; def.section_total + disclosure.section_total cells, 100 validation-usable cells). Rubric-agnostic `project_gifts_actor_agnostic_or` helper introduced for Newmark 2005 + (eventual) Opheim reuse. Honest counterfactual handling of the 2 no-variation items (`def.legislative_lobbying`, `disclosure.expenditures_benefiting_officials` — Newmark observed no cross-state variation in 2015, but the projection reads cells normally rather than special-casing).
- **Newmark 2005 plan drafted** (`plans/20260518_newmark_2005_plan.md`, ~550 lines). Declarative `_ATOMIC_SPEC` table dispatcher mirroring `newmark_2017.py`. 14 in-scope items (4 prohib + 1 penalty OOS). Weak-inequality validation regime only (Newmark 2005 publishes per-state totals only — no sub-aggregates). Imports `project_gifts_actor_agnostic_or` from `newmark_2017`. Introduces NEW rubric-agnostic `project_cadence_more_than_annual_or` helper (8-cell OR over sub-annual cadence). 2003-panel-first validation strategy with `xfail` markers for the other 5 panels pending Track A retrieval scope expansion. Falsified-2017-speculation regression-guard (Newmark 2005 has 6 disclosure items, NOT 7 — Newmark 2017 mapping doc had speculated a 2005 parallel for `disclosure.contributions_from_others`; the 2005 mapping doc correction 1 falsified it). Explicit no-sub-aggregate-validation discipline (Newmark 2005 doesn't publish sub-aggregates, so module exposes them only as `_informational` fields with module-docstring caveat).
- **Cross-rubric reuse opportunity surfaced for Opheim 1991.** When Opheim is unblocked, its `project_opheim_disclosure_gifts` can refactor to import the rubric-agnostic helper from `newmark_2017`, and its `project_opheim_disclosure_frequency` is naturally a monthly-only variant of `project_cadence_more_than_annual_or`. Plan flags this as a follow-up; does NOT modify Opheim's drafted plan.
- **Review pass on Newmark 2017 plan** (separate session, same day). Three concerns surfaced; two landed as edits, one retired. (1) 50-state validation framing: original plan implied `xfail` was the exception ("for states where input cells are unavailable, mark as xfail"); reality is `xfail` is the rule — 6 of 14 input rows are NEW with no upstream source, so every state × sub-aggregate xfails at landing. Plan reframed as future-validation harness. (2) PRI 2010 listed as fixture source: PRI is 2009–10 vintage, Newmark is 2015, state law changes between those years would produce vintage drift masking real cross-rubric drift signals post-Phase-D. PRI 2010 dropped from the harness; CPI 2015 + Sunlight 2015 only. (3) Opheim's eventual helper-import direction: retired during review — Opheim is tabled and recorded as blocked, that's far future.

## Provisional Findings

- **Stream 2 ordering is helper-sharing, not row-introduction.** All 6 Newmark-2017-NEW rows are already in v2 (the row-freeze locked them in `compendium-v2-promote`). The dependency between Newmark 2017 and Newmark 2005 is at the helper-import level (`project_gifts_actor_agnostic_or` + threshold IS-NOT-NULL pattern). Sub-4 headless launch script must enforce intra-stream ordering: Newmark 2017 before Newmark 2005.
- **Newmark 2005 has structurally weaker validation than Newmark 2017** — by a meaningful margin. Newmark 2017 contributes 100 sub-aggregate cells of full-tolerance ground truth from a single 2015 vintage; Newmark 2005 contributes 300 weak-inequality cells across 6 panels, but the inequality has `[0, 4]` headroom from the OOS prohibitions and no per-item / sub-aggregate decomposition. Newmark 2005's role is **temporal-coverage validation**, not direct validation utility — the plan frames this honestly via the validation-regime declaration.
- **Falsified-2017-speculation is a regression-guard hazard.** The Newmark 2017 mapping doc speculated a 2005 parallel for `disclosure.contributions_from_others`; Newmark 2005 mapping doc correction 1 falsified it. The Newmark 2005 plan bakes in an explicit regression-guard test asserting the `contributions_received_for_lobbying` row is NOT referenced in the module — a future copy-paste error from `newmark_2017.py` would re-introduce the falsified item silently.
- **Rubric-agnostic helper naming pays off here.** `project_gifts_actor_agnostic_or` (rather than `project_newmark_gifts_or` or `project_opheim_gifts_or`) lets Newmark 2005 + (eventually) Opheim import without redefinition. Same pattern for the new `project_cadence_more_than_annual_or` — Opheim's monthly-only variant becomes a natural neighbor.
- **No-sub-aggregate-validation discipline is a new convention.** Newmark 2005's plan introduces an explicit "module must not expose sub-aggregates as validation outputs, but MAY expose them as `_informational` fields with module-docstring caveat." This is finer than the playbook's binary "validation regime tier" — it's a per-rubric output-API decision separate from input-axis decisions. Worth folding into the playbook if HG / FOCAL surface similar shapes.
- **Future-validation harness pattern is now explicit** (review pass output). Newmark 2017's 50-state aggregation test is the first Phase C test committed as a deliberate `xfail`-at-landing harness — every parameterized case xfails because the input fixture has no source for the 6 NEW rows. When Phase D extraction lands, XFAIL→XPASS/PASS flips and signals "remove marker, this validates now". Distinct from the more typical "tests pass at landing" pattern. The plan documents the landing-time evidence (per-item + ground-truth + cross-rubric on 8 OLD rows) so reviewers don't mistake xfails for missing coverage.
- **Same-vintage fixture discipline matters at the rubric boundary** (review pass output). PRI 2010 cells are *correct for PRI* but *wrong for Newmark 2017* — different vintages, different statute snapshots. Cross-rubric checks at the *per-item layer* survive vintage drift (different rubrics may agree-on-most-states even at different years), but cross-rubric checks at the *aggregation layer* don't — sub-aggregate equality requires the same statute snapshot underneath. Locking fixture sources to same-vintage rubrics (CPI 2015 + Sunlight 2015 for Newmark 2017) preserves the signal distinction between vintage drift and cross-rubric drift.

## Decisions Made

- **Sub-2 plans are committed-ready as-is.** Self-contained per write-a-plan; carry the 7 Sub-0 conventions; STOP clauses for spec-doc-vs-v2 drift; Phase-0 cross-checks specified inline; rename mapping tables baked in.
- **Helper-sharing pattern:** `project_gifts_actor_agnostic_or` lives in `newmark_2017.py` (Newmark 2005 imports it). `project_cadence_more_than_annual_or` lives in `newmark_2005.py` (Opheim's eventual monthly-only variant will live alongside). Refactor to a shared `lobby_analysis.projections._shared` module only when HG / FOCAL need a third helper.
- **Newmark 2005's panel strategy:** 2003 panel-first validation at Phase C launch; `xfail` markers for the 5 earlier panels pending Track A retrieval scope expansion. Track A scope expansion is a separate conversation on `oh-statute-retrieval` branch — NOT in Phase C scope.
- **No-sub-aggregate-validation convention:** when a rubric publishes per-state totals only (no sub-aggregates), the projection module may expose sub-aggregates as `_informational` fields for debugging / Phase 4 audit, but tests do NOT assert equality against any published sub-aggregate value. Convention documented inline in Newmark 2005 plan; consider hoisting to the playbook if HG / FOCAL share the shape.
- **Newmark 2017 50-state validation is a future-validation harness** (review-pass decision): every state × sub-aggregate `xfail` at landing pending Phase D extraction of the 6 NEW rows. Landing-time evidence comes from per-item helper tests + ground-truth loader self-consistency + cross-rubric overlap on the 8 OLD rows. Markers flip XFAIL → XPASS/PASS when Phase D populates the NEW rows.
- **Same-vintage fixture sources only for Newmark 2017's aggregation harness** (review-pass decision): CPI 2015 + Sunlight 2015 (both 2015-vintage). PRI 2010 (2009–10) explicitly excluded to avoid vintage drift masking real cross-rubric drift signals post-Phase-D. PRI 2010 cross-rubric checks at the *per-item layer* remain useful (informational).

## Results

The Sub-2 deliverables are plan docs (in `plans/`), not analysis results, so no entries in `results/`. Plan paths:

- [`../plans/20260518_newmark_2017_plan.md`](../plans/20260518_newmark_2017_plan.md) — ~440 lines (post-tightening). Declarative `_ATOMIC_SPEC` table dispatcher; 14 in-scope items (5 prohib OUT); Medium validation regime; 50-state validation as future-validation harness; `project_gifts_actor_agnostic_or` rubric-agnostic helper for Newmark 2005 + Opheim reuse; honest counterfactual handling of the 2 no-variation items.
- [`../plans/20260518_newmark_2005_plan.md`](../plans/20260518_newmark_2005_plan.md) — ~550 lines. Declarative `_ATOMIC_SPEC` table dispatcher mirroring `newmark_2017.py`; 14 in-scope items (4 prohib + 1 penalty OOS); Weak-inequality validation regime only; imports `project_gifts_actor_agnostic_or` from `newmark_2017`; introduces `project_cadence_more_than_annual_or` (8-cell OR cadence helper); 2003-panel-first validation; falsified-2017-speculation regression-guard; no-sub-aggregate-validation discipline.

## Open Questions

7 surfaced across both plans for the implementing agent to confirm before launch:

- **Phase 0 Table 2 extraction path** (Newmark 2017): `papers/Newmark_2017__lobbying_regulation_revisited_table2.csv` (tracked, recommended) vs `data/...` (untracked).
- **`project_gifts_actor_agnostic_or` import path** for Newmark 2005: `from lobby_analysis.projections.newmark_2017 import ...` initially; refactor to `_shared` only when HG/FOCAL need a third caller.
- **Newmark 2017 50-state input-cell strategy** (post-review): CPI 2015 + Sunlight 2015 only; every state × sub-aggregate `xfail` at landing pending Phase D; PRI 2010 explicitly excluded for vintage drift.
- **Cross-rubric drift surfacing format** (Newmark 2017): per-rubric `results/` doc now; consolidate into Phase 4 audit doc when prototype lands.
- **`OOS_ITEMS` typing consistency** (Newmark 2017): `frozenset[str]` matching Opheim / Sunlight / PRI conventions.
- **Newmark 2005 Table 1 extraction path:** `papers/Newmark_2005__lobbying_regulation_in_the_states_table1.csv` (tracked, recommended). 50 states × 6 panels + 1 informational 2003-with-penalty column.
- **Newmark 2005 panel-1 scope** for 2003-panel-first validation: confirm Track A's `oh-statute-retrieval` scope (currently 2007/2010/2015/2025) before the implementing agent expects 2003 statutes to be retrievable. If not, all 6 panels xfail at landing.

## Next steps

**Stream 2's plans are ready for Sub-5+ headless implementation once Sub-4's launch infrastructure exists.** Recommended sequencing per the locked rubric order: (1) Newmark 2017 implementation (must land first — Stream 2 ordering); (2) Newmark 2005 implementation (imports helpers from Newmark 2017). After Stream 2 ships, Phase 4 cross-rubric agreement audit prototype becomes substantially more powerful — the `lobbyist_spending_report_includes_total_compensation` 8-rubric mega-row gains 2 more readers (CPI + PRI + Sunlight + Newmark 2017 + Newmark 2005 = 5 module-level readers).

**Sub-3 (FOCAL plan-set + HG plan with retrieval gate)** is the next plan-drafting session; **Sub-4 (launch infra + Sunlight canary)** follows. Opheim remains blocked on 1988-89 statute data — when unblocked, its plan benefits from refactoring to import `project_gifts_actor_agnostic_or` from `newmark_2017` and from co-locating its monthly-only cadence helper alongside `project_cadence_more_than_annual_or` in `newmark_2005`.
